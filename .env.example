# J-Quants APIキーのテンプレート（V2 は APIキー方式）。
# このファイルを `.env` にコピーして値を埋めてください（`.env` は .gitignore 済み）。
# 秘密情報はコミット・共有しないこと。

# J-Quants ダッシュボードで発行した APIキー（リクエストヘッダー x-api-key に使用）
J_QUANTS_API_KEY=

# api アクセスのインターバル（秒）。APIの利用規約に従い、適切な値を設定してください。過度なリクエストはアカウント停止の原因となります。
J_QUANTS_MIN_INTERVAL="0.7"
