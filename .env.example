# J-Quants 認証情報のテンプレート。
# このファイルを `.env` にコピーして値を埋めてください（`.env` は .gitignore 済み）。
# 秘密情報はコミットしないこと。

# Web ポータルで取得したリフレッシュトークン（推奨。1週間有効）
J_QUANTS_REFRESH_TOKEN=

# もしくは メール＋パスワード（リフレッシュトークンを使わない場合のみ）
# J_QUANTS_MAILADDRESS=
# J_QUANTS_PASSWORD=
