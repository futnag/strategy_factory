# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# venv
.venv/
venv/

# data & artifacts (do not commit market data or trial DBs)
# NOTE: root-anchored (/data/*) so it does NOT ignore the invest_system/data package.
# We ignore the *contents* of data/ (not the dir itself) so a single index file can be
# re-included below. Re-inclusion is impossible if the parent dir itself is excluded.
/data/*
!/data/README.md
*.parquet
*.duckdb
*.db
*.sqlite

# secrets
.env
*.key

# OS
Thumbs.db
.DS_Store
