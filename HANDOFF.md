# HANDOFF — TOB/大量保有イベント戦略（C1/C2）実装の引き継ぎ（v2・2026-06）

このファイルは Claude Code セッションの起点。新規データ層を足して未裁定イベント戦略
C1（TOB リスクアーブ）・C2（大量保有アクティビスト追随）を検証可能にする。
v1 からの主な更新：TOB データ取得完了、EDINET DB コネクタ採用、C2 のデータ源と
位置づけ確定（下記「確定事項」参照）。

## 現状サマリ

調査 2 系統を裁定台帳と突合した結果、未裁定で実装価値があるのは「開示文書を一次
データとするイベント族」のみ。J-Quants 範囲は 26 scope・711 試行で掘り尽くし済み。
**増やすのは仮説でなくデータ層**。

設計根拠ドキュメント（docs/ 配下）：
- `docs/07-event-strategies-candidates-2026-06.md` 戦略候補 C1–C4 と裁定計画
- `docs/08-event-strategies-edinet-api-notes.md` EDINET API 仕様＋データ調達マトリクス
- `docs/09-event-strategies-c1-tob-diagnostics.md` C1 診断と事前登録ドラフト
- `docs/10-event-strategies-c2-taihou-method.md` C2 実データ検証・取得方針・コネクタ評価（§6）
- `data/manual/maonline/parse_maonline_md.py` TOB パーサ（動作確認済み）
- `<registry>/activist_registry.csv` アクティビスト名簿ドラフト（後述）

---

## 確定事項（v2 で固めた方針・最重要）

1. **C1（TOB）と C2（大量保有）は別データ源・別ルート**。
   - C1 のイベント表＝M&A Online 手動コピー（**取得完了**）。コネクタ対象外。
   - C2 の大量保有＝**EDINET DB コネクタ（正規 API）で 2021 年以降を自動取得**。

2. **C2 は 2021 年以降のみで進める。新規の手動取得作業はゼロ**。
   - 2016–2021 の歴史補完（M&A Online 手動）は**今回見送り**。将来サンプルを増やす
     オプションとして保留（docs/10 §6.2）。
   - コネクタ制約＝2021-03 以降のみ／filer グループ別「最新 1 件」（docs/10 §6.2,§6.3）。
     同一 filer の保有割合推移や初回 5% 報告日が厳密に必要なときだけ、公式 EDINET API
     直叩き（トラック A の edinet.py）で補完。

3. **役割分担：C1 を厳格裁定で先行、C2 は記述的評価＋フォワード観察で並走**。
   - C1：検証可能ユニバース **697 件**（2016+・上場・確定）＝検出力あり。
     scope `tob_arb` で事前登録し K 一括判定（docs/09 §3）。
   - C2：名簿 9 グループの 2021+ 新規参入は概算 ~150 件＝小サンプル。厳格 DSR 判定では
     検出力不足/少数大当たりの両リスク。**Phase 2 フォワードスリーブ**として開始し、
     記述統計（参入後ドリフトの符号・大きさ、スタイル別比較）に留める。

4. **スクレイピング不可**。M&A Online は規約で自動巡回禁止（手動コピーのみ）。
   自動取得は EDINET DB コネクタと公式 EDINET API の 2 正規ルートに限定。

---

## データ取得の進捗

- [x] EDINET API キー発行済み
- [x] **M&A Online TOB 取得完了**：一覧 2008–2026（1201 件）＋詳細 1201 件。
      parse_maonline_md.py で構造化済み。突合 1201/1201・不一致 0・URL 失敗 0。
      検証可能ユニバース 697 件（2016+・上場・確定）。コメント 508 件・バンプ 32 件。
- [x] **EDINET DB コネクタ採用**：search_shareholders / get_shareholders 実証済み。
- [—] **大量保有 手動 CSV（中断・保管のみ）**：提出日の新しい順に全件取得した
      スナップショット（名簿で未絞込・全保有者 2219／全対象 2518）。範囲は
      2025-09〜2026-06 の約 9 ヶ月で中断。列・コードは整備済みだが品質より範囲が問題。
      **本番は使わない**（C2 は EDINET DB コネクタで 2021+ を取得＝上位互換：遡及が長い・
      名簿狙い撃ち・保有目的/種別フラグ付き）。この CSV は (1) コネクタ取得結果の
      独立検証、(2) 名簿に無いアクティビスト発見の母集団、(3) PDF/書類 ID 突合辞書、
      として `data/`（gitignore）に保管。**手動でこれ以上範囲を広げない**。
      ラグ異常値（最大 12967 日・負 2 件）はパーサで弾く。
- [x] **名簿 registry 確定（ローダー化）**：`activist_registry.csv`（34 グループ）。
      `activists.py` を CSV ローダーに作り直し（registry/resolve/style_of/
      canonical_group_of/is_important_proposal）。テスト 7 件パス。旧 API（is_core 等）撤去。
      ユニバース定義は registry でなく **purpose='重要提案行為等' フィルタ**が主役（docs/10 §7）。
- [x] **EDINET TOB 本体バックフィル完了（422/422）**：`data/edinet/tob_deals.parquet`。
      成立 319・不成立 14（残りは進行中/出口書類未提出）。`examples/edinet_tob_backfill.py`。
- [x] **C2 大量保有 本体抽出を EDINET ネイティブで実装（STEP2・トラックA）**：
      `large_holdings.py`＋`examples/edinet_lvh_backfill.py`＋`tests/test_large_holdings.py`
      （6 件パス）。docs/10 §8 参照。保有目的フィルタ＝`is_important_proposal`。
- [x] **C2 ユニバース母集団スキャン完走**：formCode 010000・4,872 件 body_ok →
      `data/edinet/large_holdings.parquet`。新規×重要提案 545 件、保有割合<30% の
      **コア母集団 359 件 / ユニーク提出者 ~110 / 23 グループ**（年別 2022:51〜2025:110）。
      高保有≥30%(186 件)は買収 SPV 偽陽性で除外（docs/10 §8.1b/§8.3）。registry 名寄せ補修で
      コア一致率 45%→69%（249/359）。変更報告も要るなら
      `python examples/edinet_lvh_backfill.py --form-codes 010000,010002`。
- [x] **STEP2 後半 完了**：新規参入後ドリフト記述統計（`examples/research_c2_drift.py`・
      docs/11）。247 件（5–30%）・T+1 始値→TOPIX 超過。**結論：中央値超過 ≈0・勝率~50%＝
      系統的エッジ無し、正の平均はテール（少数大当たり）駆動。レジーム依存大（2022 のみ突出）**。
      確定事項 3 を実証。Phase 2 フォワード＋イベント条件付け（買い増し/エスカレーション）が次筋。
- [x] **STEP3（C1）診断 完了**：`tob_arb.py`＋`examples/research_c1_tob_arb.py`（docs/09 §5）。
      M&A Online 716×EDINET 当初価格×株価。**T+1 ストップ高ロックで 22%(145/667) が約定不能
      ＝DP15 を実証**、EDINET 価格抽出エラー（新株予約権単価）を表示価格突合で根治。約定可能
      522 件で**スプレッド×不成立が単調増加（5→13%）＝高スプレッド=不成立セレクション確認**、
      レジーム 2016-19=2.9%/2020-26=7.0%。
- [x] **C1 scope `tob_arb` 正式 DSR 裁定 完了**：`examples/judge_c1_tob_arb.py`（docs/09 §6）。
      汎用エンジン(Path A)はテンダー決済を表現できず棄却→**イベントリターン直接 DSR(Path B)**。
      成立=買付価格テンダー／不成立=ギャップ／始値エントリー／ロック23%スキップ／等加重
      キャッシュドラッグ。**K=12（格子8+scan4）で registry に登録済み**。2016-2019 全PASS・
      2020-2026 は(競合含む,spread>=3%)のみPASS、**PBO=0.54（過学習警告）・レジーム依存・
      容量律速**。含みのあるPASS＝小サイズ前向き運用が条件（docs/09 §6.5）。
- [~] **C1 テンダー執行前提：按分は実証・執行可能性は未検証**（`examples/verify_c1_tender.py`・
      docs/09 §6.4）。前提は2分解：(i) **按分リスク＝実証済みで無視可能**（成立の4%のみ・平均影響
      −0.17%・Sharpe不変）／(ii) **テンダーを実行できること自体＝未検証**。
      **未検証の実務前提（運用前に要確認）**：個人の TOB 応募実務手続き／公開買付代理人の口座要件
      （小型案件はマイナー証券のことあり・例ソレキアの三田証券／応募締切に口座開設が間に合うか）／
      公表 T+1 から応募締切までに応募し損ねるリスク。
      **構造的警告**：執行リスクは small-capital に偏在＝**エッジの居場所と重なる**。按分検証が潰した
      のは「エッジの居ない中大型のリスク」で「エッジの居る小型フル買収の執行可能性」は別物。
      → ペーパートレードで実務確認するまで「テンダー執行可能」を既定にしない。容量も算出済（p25 ¥2-3M）。

---

## 実装トラック（依存で 2 分割）

### トラック A — データ不要・今すぐ着手可
1. `data/sources/edinet.py`：公式 EDINET 書類一覧 API の by-date ミラー
   （docs/08 §1,§3）。増分=seqNumber、追記専用スナップショット、429 スロットル
   （`EDINET_MIN_INTERVAL`）。C2 の初回 5% 報告日補完にも使う将来資産。
2. `examples/edinet_probe.py`：docs/08 §7 の確認 5 点を実測。
3. `data/sources/maonline.py`：大量保有・TOB 共通の md→構造化ローダ。
   増減 5 分類（**種別列を主**・docs/10 §1.2）・提出日アンカー・EDINET コード/
   書類 ID 抽出・icon_active.png をフラグ列化（docs/10 §1.1b）。
   TOB は既存 parse_maonline_md.py を流用。
4. `<registry>/activist_registry.csv` を確定（スタイルタグ＝engagement_hard/soft、
   1 ファンド複数 EDINET コードの dedup、3D・オアシスの名寄せ）。docs/10 §2 の
   4 タイプ分類（アクティビスト/政策保有/オーナー/トレーディング）で本則を限定。
5. EDINET DB コネクタ経由の C2 取得スクリプト：名簿 → search_shareholders で
   保有先 → get_shareholders で各 issuer の filer 別最新 → 構造化 CSV。
   ※コネクタはチャット側 MCP。Claude Code 側は同等の EDINET API 直実装か、
     コネクタ取得済み CSV の取り込みのどちらか（docs/10 §6.5）。
6. 各バリデータ（doc_id 突合・件数照合）。

### トラック B — データ依存
7. TOB 詳細 697 件（2016+）の parse 済みデータ → スプレッド × 不成立の相関診断
   （docs/09 §4-2）。高スプレッド帯のセレクション定量化＝C1 prior の検証。
8. C1 を scope `tob_arb` で事前登録 → K=8 セル一括判定（docs/09 §3）。
9. C2：2021+ 大量保有 → ユニバース確定 → 新規参入後ドリフトの**記述統計**
   → Phase 2 フォワードスリーブ登録（厳格裁定はしない・確定事項 3）。

---

## 踏んではいけない地雷（PIT・規律）

- **提出日アンカー厳守**。TOB＝公表日 T+1／大量保有＝提出日 T+1。報告義務発生日・
  開始日は使わない。大量保有は特例報告で提出ラグ最大 148 日の実例あり
  （docs/10 §1.1）＝報告義務発生日エントリーは重大ルックアヘッド。
- **`_displayed` 列をエントリーに使わない**。M&A Online の買付価格は最終(バンプ後)
  価格。当初価格はコメント/目的欄パーサ＋株価段差検出で復元（docs/08 §8.2）。
- **レジーム分離**。TOB 不成立率は 2020 年前後で約 5 倍に変化。単純平均禁止。
  サブ期間 2016–2019 / 2020–2026（docs/09 §2.1）。
- **C2 ユニバースは purpose='重要提案行為等' フィルタで定義**（docs/10 §7）。
  手編集名簿でファンドを選ぶのは in-sample 選択＝禁止。registry は名寄せ・タグの補助のみ。
  大量保有 DB は全種類の保有者が混在するが、保有目的テキストで機械的に峻別する。
  - **偽陽性に注意**：purpose フィルタは TOB 後スクイーズアウトの買収 SPV も拾う
    （高保有割合＋重要提案）。C2 シグナルは保有割合バンド（例 5–30%）または tob_deals 突合で
    買収者を除外（docs/10 §8.3）。これはシグナル定義側で事前登録。
- **新規/変更/訂正は本体タイトルで判別（formCode 不可）**。旧記載「formCode 01xx=新規」は
  誤り（`010002` は変更報告書）。新規イベントは一般新規 `010000` に集中、特例報告(02/03)は
  アクティビスト無し（docs/08 §7.1 point1・docs/10 §8.2）。`large_holdings.report_class()`。
- **C2 を厳格 DSR 裁定にかけない**。小サンプル（~150 件）。記述＋フォワードに留める
  （確定事項 3）。これは規律緩和ではなく、検出力不足での誤判定を避けるための設計。
- **K 規律**。裁定済み FAIL scope（日経入替§6.14・配当/自社株買い§6.17）の再試行は
  K を払う。後知恵の閾値追加・事後のレジーム選択は封印。
- **スクレイピング禁止**。M&A Online 自動巡回不可（手動コピーのみ・取得済み）。
  自動化は EDINET DB コネクタと公式 EDINET API のみ。
- **外部データはコミットしない**。M&A Online md・大量保有・EDINET 取得物は
  `data/`（gitignore）。名簿 registry と EDINET 様式コードリストのみコミット可。
- **PDF は当面取得しない**（MVP に不要。書類 ID は突合用に温存）。

## 規約

M&A Online＝自動巡回禁止・手動コピーのみ・再配布不可。
EDINET（公式 API・EDINET DB コネクタ）＝自動取得可・429 礼儀スロットル。
TDnet＝公式 API なし（C3 は将来のフォワード蓄積。当面着手不要）。

## 次の一歩（推奨順）

STEP1・STEP2（前後半）・STEP3（診断＋正式 DSR 裁定）は完了。C1 は registry に K=12 で登録済み。

1. **C1 の Phase 2 小サイズ前向き運用化**（docs/09 §6.4）：(a) 容量算出済＝**小資本限定**
   （p25 ¥2-3M @参加率10%・エッジは低流動銘柄に偏在）、(b) 2020-2026 単独のフォワード継続で
   PBO 高・レジーム依存を実地検証、(c) **単一仕様に事前固定**してセル選択の多重性を増やさない
   （再裁定は K を払う）。実質 small-capital ニッチ＝大型運用は非スケール。
2. **C2 エスカレーション検証 完了**（`examples/research_c2_escalation.py`・docs/12）：凍結4条件
   （①買い増し②複数アクティビスト③高保有⑤hard）を記述統計で検証（変更報告 010002 をコア110
   filer 限定で 2,903 件バックフィル＝`large_holdings_changes.parquet`）。結論：
   - **② 複数アクティビスト同時参入のみ芽**（x60 中央 +5.8%・勝率75-79%・現行レジーム維持・但し
     N=17）→ **registry scope `c2_escalation_multi` に事前登録済**（status=preregistered・K非算入・
     20/60日併記・選択バイアス未解明）。**次：フォワードで N 積み増し→将来 K を払い厳格判定**。
   - **③ 高保有到達=追随の禁忌（確定知見・docs/12 §2.1）**：≥20% 後 x60 −4.7%（現行 −5.82%）＝
     素朴な「高保有追随」ロングは台帳で却下済み（再検証不要）。
   - ①（レジーム依存）⑤（docs/11 追認）は不登録。
3. **C1 の Phase 2 小サイズ前向き運用化**（docs/09 §6.4-6.5）：(a) 容量算出済＝小資本限定、
   (b) 2020-2026 フォワード継続、(c) 単一仕様固定、(d) **テンダー応募の実務手続き・代理人口座要件を
   ペーパートレードで確認**（執行可能性は未検証・small-capital に偏在）。
4. データ拡張（任意）：C1 の 2016–2019 当初価格を EDINET 窓外ぶん補完（M&A Online コメント段差検出）。
5. registry の取りこぼし追記はタグのみ・母集団不変（in-sample 選択にならない）。
