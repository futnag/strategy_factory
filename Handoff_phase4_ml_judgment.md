# 作業依頼：Phase 4 — GKX 型 ML クロスセクション予測器と「一度きりの DSR 判定」

## 0. 位置づけ（最重要の心構え）

これは GKX 化プロジェクトの**本丸であり最終段**。Phase 1–2 ＋拡充 A/B でデータと特徴量を全て整え、プリフライトで瑕疵（分割・廃止・トータルリターン・先読み）を潰した。ここで初めて、**全特徴量を 1 つの ML モデル群に入れてクロスセクションの将来リターンを予測し、その経済価値を「事前登録した単一の deflated DSR 判定」で一度だけ評価する**。

**この判定は貴重な一回**。リポジトリ全体の反 p-hacking 機構（CPCV・デフレート DSR・永続レジストリ・purge/embargo）は、この瞬間のために存在する。**結果が芽が出ない（エッジが手数料・デフレーションを越えない）のは、正当で受け入れるべき結論**であり、通るまで特徴量を足す/ユニバースを変える/再チューニング/再判定する行為は**プロジェクトの価値を破壊する**。ここだけは厳守。

---

## 1. スコープ

### やること

- **設計行列の組立**：全特徴量を 1 つの X 行列へ（winsorize→[-1,1]ランク → 中立 0 埋め → セクター中立 → 特性 × マクロ交互作用）。
- **推定器スイート**：OLS/Lasso/ElasticNet（**ベースライン先行**）→ RF/GBRT → 浅い NN。拡張窓 train/val/test、ハイパラは**内側 CV（検証）でのみ**決定。
- **評価ハーネス（開発中は throwaway）**：OOS R²（GKX 流）・月次 IC/rank-IC・分位ロング・ショートのスプレッド・回転率・手数料後・廃止封筒。
- **単一の事前登録 DSR 判定**：CPCV ＋ purge/embargo で OOS 評価し、**宣言した試行数でデフレート**して**一度だけ**レジストリに登録。
- 事前登録メモ・結果 docs・テスト。

### やらないこと

- **新規特徴量を作らない**（特徴量セットは凍結。足りなくても足さない＝ p-hacking 回避）。
- 本番トレーディングシステム化（Phase 4 の後）。Phase 3 アナリスト系・Phase 5 Premium 延伸は対象外。
- **判定の事後いじり一切**（後述）。

---

## 2. 確定済みの設計判断（凍結された入力・あなたとの Q&A で確定）

- **ユニバース**：本番＝流動（price≥¥100・mcap≥¥100 億・ADV60≥¥5000 万、config 駆動／`universe.liquid_universe_mask`）。**診断は全上場**で「除外テールのアルファ量」を測る。mcap 床は診断を見て後調整可。
- **リターン**：**トータルリターン**（配当込み・`panel.total_forward_returns`＋`dividend_per_share_asof`）。価格リターンは診断比較。
- **上場廃止**：本番＝ last-price 既定（`panel.impute_delisting`）。**封筒（last-price ↔ 全 −100%）を常時併報**。点推定は heuristic ＋`tob_deals`/`large_holdings` 突合に寄せてよいが、封筒は必ず出す。
- **ラベル/ホライズン**：月次リバランス・**1M 先・超過リターン**（基準＝**本番ユニバースの断面等加重平均**）・close[t]→close[t+1]・廃止補完済み。
- **マクロ PIT**：特性 × マクロ交互作用を**含める**。**公表日アンカー**、統計系（CPI/GDP 等）のみ 1 ヶ月ラグ、日次観測可（金利・株価・出来高）はラグ無し＝**系列別ラグ規約**を docs に明記。flows.py も状態変数化。
- **sector-PIT**：S33 は現スナップショット**許容＋明記**。頑健性として**業種変更銘柄を除く部分標本で主結果が定性的に不変か**を確認。

---

## 3. 最重要の規律（判定段ゆえ最も厳格に）

1. **判定は一度きり・事前登録**：universe/label/特徴量/モデル族/検証プロトコル/**判定指標**/**デフレート用試行数**を、**OOS 結果を見る前に**事前登録メモ（docs）に凍結する。
2. **ベースライン先行**：OLS/Lasso/ElasticNet を先に確立し、**ML の増分**を線形比で測る（国際研究で OLS が ML を上回る件・線形を超えて初めて ML を正当化）。
3. **チューニングは検証のみ**：ハイパラは内側 CV（train 内）で決定。**テスト/OOS フォールドでチューニングしない**。
4. **試行数を正直に申告**：試したモデル族・主要設定の実効数を DSR デフレーションに渡す（「実際は 5 族試したのに 1 と申告」は重大違反）。
5. **負の結果を受容・p-hacking 禁止**：deflated DSR・手数料・廃止封筒を越えなければ、それが結論。**通すために特徴量追加/ユニバース変更/再チューニング/再判定をしない**。
6. **リーク遮断**：feature[t]≤t・ラベル前向き（結合パネル先読みテスト PASS 済）。train/test 境界に **purge ＋ embargo**（既存 `validation.purge_embargo`）。
7. **開発診断は throwaway／登録判定は 1 つ**：IC・スプレッド等の探索は K 不変の使い捨て。レジストリに刻むのは事前登録した 1 判定のみ。
8. 既存インフラ（`validation.{dsr,cpcv,purge_embargo,registry}`・`factors` 標準化・`universe` PIT・各 as-of パネル）を**再利用**し再実装しない。

---

## 4. 設計行列とラベル

### 特徴量（凍結セット）

Phase 1 価格/流動性＋原 value/quality/size、Phase 2 EDINET（FCF 利回り・ROIC・資産成長・accruals・gross_profitability・ebitda_margin・leverage・**分割調整済 net_share_issuance**・rd_intensity）、拡充 A（信用/空売り・微細構造）、B（SUE・予想改訂・成長・安定度・持続可能成長・52 週高値・季節性・Dimson β）。

**重複の解消（docs/18 準拠）**：JQ 版 accruals と EDINET 版 accruals は符号逆（相関 −0.32）＝**片方のみ採用（推奨：三表明細から計算した EDINET 版）**。cf_yield≡cf_to_price は 1 つに。Amihud は de-dup 済。

### 標準化パイプライン（一元化済みを使う）

毎月クロスセクションで `factors.winsorize_cross_sectional`(1/99%) → `rank_and_fill`([-1,1]・欠損中立 0) → 必要に応じ `sector_neutralize`。**符号規約の混在（Phase1 house / EDINET raw）は ML が学習するので可**だが、線形ベースラインの解釈用に docs/18 の規約表を参照。

### 特性 × マクロ交互作用

PIT 整合のマクロ状態変数（金利・ターム/クレジットスプレッド・市場ボラ・225 IV・海外フロー、系列別ラグ）と特性の**Kronecker 交互作用**。**線形モデルには明示的に交互作用列を作る**。**木/NN には作らない**（生の特性＋マクロを入れてモデルに交互作用を学習させる＝次元爆発回避）。

### ラベル

`y[t]` = 月次 1M 先・トータル・超過（本番ユニバース断面平均控除）・廃止補完・close[t]→close[t+1]。

### ラギッド被覆の扱い

YoY 系は ~2017 立上り、季節性は ~2021。**rank_and_fill の中立 0 で早期欠損を吸収**しつつ、**OOS/テスト期間は被覆が十分な区間に置く**。学習窓・欠損方針は事前固定（探索しない）。

---

## 5. 推定器と検証プロトコル

- **拡張窓 train/validate/test**：時系列順に拡張。各推定で内側 CV（検証）でハイパラ決定、テストは触らない。
- **推定器**（sklearn 中心・既存依存内）：
  1. **線形ベースライン**：OLS、Lasso、ElasticNet（交互作用列込み）。
  2. **木**：RandomForest、HistGradientBoosting（生特性＋マクロ）。
  3. **NN**：浅い MLP（GKX 流の小規模）。
- **OOS 評価**：`validation.cpcv`（コンビナトリアル・パージド CV）＋ `purge_embargo`。GKX 流 **OOS R²**（個別銘柄リターン予測）と、**経済評価＝分位ロング・ショート（または上位デシル）ポートフォリオの手数料後・廃止補完後リターン**。
- **線形 vs ML の増分**を必ず併記（ML が線形を超えるか）。

---

## 6. 判定（一度だけ・事前登録・deflated DSR）

1. **事前登録**（`docs/19-phase4-preregistration.md`）：上記の全設定＋**判定指標**＝「本番ユニバース・手数料後・廃止 last-price 補完の L/S（または上位デシル）ポートフォリオの、CPCV-OOS デフレート Sharpe」＋**デフレート用試行数**（モデル族 × 主要設定の実効数）を、結果を見る前に凍結。
2. **一度だけ実行**：`validation.dsr`（デフレート Sharpe）を、宣言試行数で deflate して算出。`validation.registry` に**1 判定として登録**（ここで K が増える＝事前宣言ぶんのみ）。
3. **封筒併報**：判定 Sharpe を**廃止 last-price と全 −100% の両端**で出し、頑健性を示す。
4. **正直な報告**：閾値（例：deflated Sharpe>0 かつ手数料後で有意）を**越えても越えなくても**結果を docs に記録。**越えない場合に通すための事後変更は禁止**。

---

## 7. 現実性・頑健性

- **取引コスト・回転率**：月次回転率 × コストで手数料後リターン。GKX のアルファは小型偏在＝コストで削れるので、**流動ユニバースでもネットで見る**。
- **廃止封筒**（§6-3）：last-price ↔ −100% で Sharpe の幅。
- **頑健性（throwaway 診断）**：(a) 業種変更銘柄を除く部分標本（sector-PIT 影響）、(b) 全上場診断での除外テールのアルファ量（mcap 床調整の材料）、(c) レジーム別（利上げ局面 等）、(d) ホライズン頑健（1M シグナルを 1M vs 3M 保有）。
- **mcap 床感応度**：¥100 億 vs ¥30–50 億で結果がどう動くか（本番閾値の再検討材料）。

---

## 8. 成果物（Deliverables）

1. `equities/design_matrix.py`（or 適所）：全特徴量 →winsorize→rank_and_fill→sector_neutral→（線形用）マクロ交互作用、ラベル組立、ラギッド被覆/重複解消込み。
2. `research/phase4_estimators.py`：線形/木/NN の拡張窓 train/val/test ＋内側 CV。
3. `research/phase4_evaluate.py`：OOS R²・IC・L/S スプレッド・回転率・手数料後・廃止封筒（**throwaway 診断**）。
4. **事前登録メモ** `docs/19-phase4-preregistration.md`（結果前に凍結）＋ **結果** `docs/20-phase4-results.md`（OOS R²・線形 vs ML 増分・判定 deflated Sharpe ＋封筒・頑健性・正直な合否）。
5. `examples/run_phase4_judgment.py`：事前登録プロトコルで**一度だけ**判定を実行しレジストリ登録。
6. テスト：設計行列の PIT 先読み不変・重複解消・交互作用次元・廃止/超過ラベルの整合・purge/embargo 境界。既存（現行 419）green を維持。
7. レジストリは**事前宣言ぶんのみ K 増加**（事後の追加登録なし）を `registry_status.py` で確認。

---

## 9. 受け入れ基準（Definition of Done）

- 設計行列が全特徴量で PIT・winsorize・[-1,1]ランク・中立 0・セクター中立・（線形）マクロ交互作用込みで組め、結合パネルの先読み不変が green。
- 線形ベースライン → 木 →NN が拡張窓で走り、**OOS R² と線形 vs ML 増分**が出る。
- **事前登録メモが結果前に凍結**され、**単一の deflated DSR 判定**が CPCV-OOS・手数料後・廃止封筒で算出・レジストリ登録される。
- 判定が**合否いずれでも正直に**記録され、**事後の通すための変更が無い**（diff とレジストリで確認可）。
- 頑健性（部分標本・除外テール・レジーム・mcap 床・ホライズン）が docs に揃う。
- K が**事前宣言ぶんのみ**増加。既存 419 テスト不変＋新規 green。

---

## 10. 留意点

- 純 Python・OS 非依存・LF・float dtype。既存 validation/factors/universe/as-of を再利用。
- マクロ交互作用の次元爆発は「線形のみ明示交互作用・木/NN は生入力」で回避。正則化（Lasso/ElasticNet）を効かせる。
- **NN は sklearn MLP 等・既存依存内**で（重い深層フレームワークを増やさない）。再現性のため seed 固定・決定的。
- 判定指標・試行数・窓・閾値は**結果前に凍結**。少しでも事後に動かしたくなったら、それは p-hacking のサイン＝**やらない**。
- 不明点（CPCV の分割数・embargo 長・交互作用に使うマクロ系列の確定・デフレート試行数の数え方）は推測で固めず、**事前登録メモの草案をユーザに見せて確認**してから判定を実行する。

---

### 一言サマリ（Claude Code へ）

> GKX 化の本丸。全凍結特徴量を 1 つの設計行列（winsorize→[-1,1]ランク → 中立 0→ セクター中立 → 線形用マクロ交互作用）にまとめ、**OLS/Lasso/ElasticNet ベースライン先行**→RF/GBRT→ 浅い NN を拡張窓 train/val/test（ハイパラは内側 CV のみ）。評価は OOS R² ＋手数料後・廃止補完の L/S ポートフォリオ。そして **universe/label/特徴量/モデル族/判定指標/試行数を結果前に事前登録**し、**CPCV-OOS の deflated Sharpe を宣言試行数でデフレートして一度だけレジストリ登録**＝これが唯一の判定。判定は **last-price↔−100% の廃止封筒併報**、部分標本/レジーム/mcap 床の頑健性も diag（throwaway）。**新規特徴量を作らず、通すための事後変更を一切せず、負の結果も正直に記録**。確定済み入力（流動ユニバース/トータルリターン/月次 1M 超過/last-price 廃止/マクロ PIT 系列別ラグ/sector スナップショット許容）は §2 のとおり。既存 validation/factors/universe を再利用、K は事前宣言ぶんのみ増やす。
