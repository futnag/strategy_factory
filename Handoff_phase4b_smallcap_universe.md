# 作業依頼：小型寄りユニバースでの GKX 再検定（独立した新・事前登録判定）

## 0. 位置づけ（規律上の決定的な前置き）

これは **Phase 4 の FAIL 判定（phase4_gkx_judgment, DSR=0.905）の事後修正では断じてない**。前判定は FROZEN・不可逆・終了で、閾値も N も族も触らない。

本タスクは、前判定の結果が示唆した方向（弱い非線形エッジが存在・GKX アルファは小型に偏在）を踏まえ、**「mcap 床を下げた小型寄りユニバースでは、GKX 型 ML エッジが認定基準を越えるか」という独立した新しい経済仮説**を立て、**新たに事前登録し、新たな K 枠を 1 つ消費して一度だけ検定**する。これは p-hacking ではなく、López de Prado 機構が想定する「新仮説＝新試行＝ K 加算」の正当な使い方。

**ただし強い自己規律**：これは「前回 FAIL を別ユニバースでリトライ」に外形が似るため、**最も p-hacking に滑りやすい**。下記を厳守する。

---

## 1. スコープ

### やること

- **唯一の変更点＝ユニバースの mcap 床を下げる**（¥100 億 → 候補を事前固定）。それ以外（特徴量・モデル族・ラベル・コスト・CPCV・閾値・廃止・マクロ）は**前判定と完全に同一に保つ**。
- 新しい事前登録メモ（`docs/21-phase4b-preregistration.md`）を結果前に凍結。
- **一度だけ**検定し、`docs/22-phase4b-results.md` に合否を正直に記録。新スコープで K+=1。

### やらないこと

- 床以外のパラメータを動かさない（特徴量追加・モデル再チューニング・閾値変更・ホライズン変更は全て禁止＝それをやると「前回 FAIL を多変数で nudge」になる）。
- 床の候補を結果を見てから選び直さない（候補は結果前に固定）。
- 前判定（docs/19/20・registry の phase4_gkx_judgment）には一切触れない。

---

## 2. 唯一動かす変数：mcap 床（候補を結果前に固定）

- **新ユニバース**：price≥¥100・ADV60≥¥5000 万 は据え置き、**mcap 床のみ下げる**。
- **床の候補**：再検定を**多重に増やさない**ため、**単一の床に事前 commit**する。既定 **¥30 億**（最も小型を取り込み、エッジ偏在仮説を最も強く検定）。
  - **¥30 億 と ¥50 億 の両方を別判定として走らせない**こと（それは N と K を増やす多重検定）。1 つに決めて事前登録。¥50 億 にしたい場合はそれ単独で。
- **新ユニバースで増える銘柄**を `universe.liquid_universe_mask` の config で表現（前判定の config と並存・別キー）。

---

## 3. 前判定と同一に保つもの（変更禁止・コピー）

- 特徴量セット（凍結済み・前判定と同一。新規追加なし）。
- モデル族 N=6（OLS/Lasso/ElasticNet/RF/HGBRT/MLP）・**OOS 最良選択**・内側 CV でハイパラ。
- ラベル：月次 1M 先・超過・トータルリターン・廃止 last-price 補完・close[t]→close[t+1]。超過の基準は**新ユニバースの断面等加重平均**（基準もユニバースに合わせて更新＝ここは整合のため必須）。
- CPCV：Combinatorial k=2（n_splits=6, n_test=2）→φ=5、V[SR]＝選択族パス間分散、embargo gap_after=12 ヶ月（深窓は因果 PIT・purge で特徴量窓除去）。
- 簿記：DSR を N=6 でデフレート、**register 1 判定・K+=1**（φ・N は K 非算入）。
- 閾値：**DSR≥0.95 ∧ net Sharpe>0 ∧ −100% 封筒 net Sharpe>0**（前判定と同一・変更禁止）。
- コスト：15bps 片道・デシル L/S・月次・等加重。OOS：2018-01〜。

---

## 4. 小型化で新たに要る検証（重要・小型はバイアスが増幅する）

mcap 床を下げると、前判定で抑えていたリスクが増える。**判定前に診断（throwaway・K 不変）で必ず確認**：

1. **流動性・取引コストの現実性**：小型は ADV が薄く 15bps 片道が楽観になりがち。**ADV 対比の建玉サイズと、より保守的なコスト（例 30bps）での感応度**を診断併報。エッジが 15bps 前提でしか出ないなら docs に明記。
2. **廃止/生存者バイアスの増幅**：小型は廃止が多い。新ユニバースでの**年別廃止件数と封筒幅（last-price↔−100%）**を再測。前判定の +0.22%/月 がどれだけ増えるか。
3. **データ品質**：小型は EDINET 充足率・株数単位差・流動性ゼロ日が悪化。**因子別被覆と外れ値（winsorize 後）が劣化していないか**確認。
4. **容量（capacity）**：小型寄りは容量が小さい。**AUM 別の実現 Sharpe 曲線（マーケットインパクト込み）**を診断で出し、「紙の Sharpe」と「実装可能 Sharpe」の差を可視化。
5. **本番 vs 全上場診断**：新床で**何を取り込み何をまだ捨てているか**を全上場診断で測る（床の妥当性の事後評価材料・ただし結果を見て床を選び直さない）。

---

## 5. 最重要の規律（前判定以上に厳格に）

1. **床は結果前に単一値で凍結**。複数床を走らせない（多重検定回避）。
2. **床以外は一切動かさない**。FAIL でも、特徴量追加・再チューニング・閾値変更・別床での再走をしない。
3. **新スコープで K+=1**。これは「前回 FAIL の延長」ではなく独立判定だが、**K は正しく増える**＝累積多重検定としてバーが上がる。だから**安易な反復は構造的に損**。次にまた別ユニバースを試したくなったら、それも K を食う。
4. **負の結果を受容**：小型でも DSR<0.95 なら FAIL で確定・記録。「前回 0.905・今回も僅差」を見て閾値や床を動かす誘惑が最大化するが、やらない。
5. 開発診断は throwaway。登録は事前登録プロトコルの 1 判定のみ。
6. PIT・purge/embargo・先読み不変は前判定と同一に担保。

---

## 6. 成果物

1. `universe` の config に新床ユニバース（既定 ¥30 億）を追加（前判定 config と並存）。
2. `docs/21-phase4b-preregistration.md`：**結果前に凍結**。前判定からの**唯一の差分（mcap 床）**を明記し、他全項目が同一であることを表で示す。STATUS: FROZEN。
3. `examples/run_phase4b_judgment.py`：前判定スクリプトを新ユニバースで**一度だけ**実行（二重実行ガード継承）。
4. `docs/22-phase4b-results.md`：族別 OOS・選択族・deflated DSR(N=6)・封筒・**小型固有診断（コスト感応度・廃止幅・容量曲線・被覆劣化）**・正直な PASS/FAIL。前判定（docs/20）との並置比較表。
5. テスト：新ユニバースマスク・基準（断面平均）の整合・PIT 先読み不変・purge。既存（現行 436）green 維持。
6. レジストリ：新スコープ phase4b で **K 0→1**（前 scope phase4_gkx_judgment は不変）。`registry_status.py` で二重計上なしを確認。

---

## 7. 受け入れ基準

- 変更が **mcap 床のみ**であることが docs/21 の差分表で明示され、他項目が前判定と同一。
- 床は単一値で結果前に凍結、複数床を走らせていない。
- 新ユニバースで族別 OOS→deflated DSR(N=6)→ 封筒 が一度だけ算出され、**小型固有診断（コスト感応度・廃止幅・容量・被覆）**が併報。
- 合否いずれも正直に記録、**事後変更なし**（diff・registry で確認可）。
- K は新スコープで +1 のみ。前判定スコープ不変。既存テスト不変＋新規 green。

---

## 8. 留意点

- 超過リターンの基準だけは**新ユニバースの断面平均に更新**（整合のため必須・これは「パラメータいじり」ではなくユニバース定義の従属変更）。それ以外の従属でない設定は触らない。
- 小型はコスト・容量・廃止・データ品質が同時に悪化するため、**「紙の Sharpe が前回より上がっても、実装可能性が下がっていないか」を必ずネット・容量込みで評価**。床を下げて Sharpe が上がるのは当然あり得るが、それが取引コストと容量を織り込んで残るかが本質。
- 不明点（新床で増える銘柄数・ADV インパクトモデルの係数・小型の廃止検知精度）は推測で固めず、診断で実測し、判定実行前に**事前登録草案をユーザに見せて確認**してから走らせる。

---

### 一言サマリ（Claude Code へ）

> 前 Phase 4 判定（FAIL・FROZEN）は触らない。**独立した新仮説**＝「mcap 床を下げた小型寄りユニバースで GKX エッジが認定基準を越えるか」を、**唯一の変更点を mcap 床（既定 ¥30 億・単一値で結果前に凍結）**として、**他は前判定と完全同一**（特徴量・N=6 族・ラベル・CPCV k=2 φ=5・embargo 12mo・閾値 DSR≥0.95∧net>0∧ 封筒>0・コスト 15bps・OOS 2018+）で一度だけ検定。小型はバイアスが増幅するので**コスト感応度・廃止封筒幅・容量曲線・被覆劣化を診断併報**。新スコープで **K+=1**、N=6 は DSR が吸収。**床以外は一切動かさず、FAIL でも事後変更なし**。事前登録 docs/21 を結果前に凍結し草案をユーザ承認後、一度だけ実行、docs/22 に正直に記録。
