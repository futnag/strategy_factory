# 依頼プロンプト：回転手法バックテストのローカル実装

> 下記の「===」で囲まれた部分を、そのままコーディングエージェント（Claude Code 等）に貼り付けて渡してください。

---

===================================================================

# タスク：日本株「回転手法」バックテストの実装

あなたはローカル環境で作業するコーディングエージェントです。以下の戦略を Python (pandas/numpy) で実装し、**必ず自分で実行・検証してから**完成とすること。

## 0. 作業ディレクトリと環境

- 作業ディレクトリ: `C:\Users\futos\claude_local_sessions`
  - この配下に `kaiten_backtest.py`（本体）を作成する。
  - 入力データは同ディレクトリ配下の `data\` に置かれる想定（例: `data\7203.csv`）。無ければ後述の合成データで動かせるようにする。
- OS は **Windows**。以下に配慮すること:
  - パスは raw 文字列（`r"..."`）かスラッシュで扱い、バックスラッシュのエスケープ事故を避ける。
  - 仮想環境を作って導入: `python -m venv venv` → `.\venv\Scripts\activate` → `pip install pandas numpy matplotlib`。
  - 日本株CSVは Shift-JIS のことがあるため、読み込みエンコーディングを **Config で切替可能**にする（`utf-8` / `cp932`）。
  - matplotlib で日本語を使うとフォント欠落警告が出るため、**グラフのタイトル・軸ラベルは英語**にする（コンソール出力は日本語で可）。CSV書き出しは `utf-8-sig`（Excelで文字化けしない）。

## 1. 戦略概要（回転手法・ロングのみ）

長期上昇トレンドにある銘柄の一時的な急落（押し目）を買い指値で拾い、数日以内の短期反発で利確する。ショートは行わない。

## 2. トレーディング・ロジック

### ① トレンドフィルター
- 長期移動平均線（デフォルト200日）より当日終値が上のときのみ、買い指値ロジックを有効化する。フィルター自体も ON/OFF 可能に。

### ② 押し目買い（翌営業日の買い指値）
- **その日 t の終値・指標だけ**を使って「翌営業日 t+1 の買い指値」を計算・配置する。
- 指値方式は **2案とも実装し、Config で切替可能**にすること:
  - 案A（`atr`）: `終値 − atr_mult × ATR(atr_period)`
  - 案B（`bb`）: 20日ボリンジャーバンドの下限 `移動平均 − bb_sigma × 標準偏差`（σは 2.5〜3 想定、パラメータ化）
- 指値の有効日数もパラメータ化（デフォルト1＝翌日のみ）。

### ③ 短期反発での利確（イグジット）
- 約定後、`+take_profit_pct`（デフォルト +7%、パラメータ化）到達で利確。
- または `max_hold_days`（デフォルト5営業日、パラメータ化）経過でその日の終値で成り行き決済。

### ④ 資金管理・ロスカット
- `-stop_loss_pct`（デフォルト -10%、パラメータ化）到達で損切り。
- **ポジションサイズはリスクベースで計算**：1トレードの最大許容損失 = 総資金 × `risk_per_trade_pct`（デフォルト1%）。株数 = リスク額 ÷（約定価格 × stop_loss_pct）。
- レバレッジ上限 `leverage`（1.0〜1.5想定）＝ 建玉合計 ≤ 総資産 × leverage。同時保有銘柄数上限もパラメータ化。

## 3. 【最重要】バックテストに嘘をつかせないための防御実装

以下は成績を過大評価させないための必須要件。**必ず実装すること。**

1. **時間順序の厳守（先読み排除）**：指値は t 日終値で確定し、約定判定は t+1 日以降の Low で行う。同じ日の情報で指値を決めてその日に約定させてはならない。
2. **ギャップ約定**：翌日の寄付が指値やストップ/利確線を飛び越えている場合は、指値ではなく**寄値**で約定させる（買い指値は `min(指値, 寄値)`、損切りは寄値が下なら寄値、利確は寄値が上なら寄値）。
3. **同日両ヒットの扱いを Config で切替可能**に：ある日の高値が利確線・安値が損切線を同時に超えた場合の処理を `same_day_priority` で `"stop"`（損切り優先＝保守的・デフォルト）/ `"target"`（利確優先）/ `"auto"`（寄値に近い方を先に約定と推定）から選べるようにする。
4. **エントリー当日は手仕舞い判定しない**：約定した当日の日中経路は曖昧なので、手仕舞い判定は翌日以降に行う。
5. **コスト**：片道手数料 `commission_pct`、片道スリッページ `slippage_pct` をパラメータ化し、エントリー・イグジット双方に適用する。
6. **ポートフォリオは共有資金**で回す（複数銘柄が同じ口座資金を取り合う。1銘柄1ポジションを基本とし、重ね建て可否もパラメータ化）。

> なお生存者バイアス（上場廃止銘柄の欠落）はコードでは防げない。README かコード冒頭コメントに「上場廃止銘柄を含むデータで検証しないと成績が過大評価される」旨の注意書きを残すこと。

## 4. Config 分離（必須）

移動平均期間・指値方式と深さ（atr_mult / bb_sigma）・利確%・損切%・タイムアウト日数・リスク%・レバレッジ・手数料・スリッページ・エンコーディング・同日処理モード等、**すべての調整項目をファイル冒頭の `CONFIG` 辞書に集約**し、そこだけ触れば挙動が変わるようにする。

## 5. データ入力

- CSV 列は `Date, Open, High, Low, Close, Volume` を想定。列名の表記揺れ（日本語列名や大小文字、Adj Close 等）を吸収する正規化を入れる。
- `data\*.csv` のワイルドカードで複数銘柄を読み込めるようにし、ファイル名を銘柄シンボルとして扱う。
- 実データが無い場合に備え、**トレンド＋急落を含む合成データを生成して動作確認できるフォールバック**を用意する。

## 6. 出力要求

- コンソールに戦略パフォーマンスを出力：**総リターン、CAGR、勝率、プロフィットファクター、最大ドローダウン、Sharpe、トレード回数、平均保有日数、イグジット理由の内訳（stop/target/timeout）**。
- **matplotlib で資産推移（エクイティ曲線）とドローダウンを描画**し、`equity_curve.png` に保存。
- トレード履歴を `trades.csv`（`utf-8-sig`）に書き出す。

## 7. 完成の定義（Definition of Done）— 実行して検証すること

以下を**実際にコードを走らせて確認**し、結果を報告してから完了とすること：

1. 合成データで end-to-end 実行が通り、上記メトリクスとグラフ・CSV が生成される。
2. `entry_method` を `atr` / `bb` に切り替えて両方ともクラッシュせず動く。
3. `same_day_priority` を `stop` / `target` / `auto` に切り替え、**同日両ヒットが起きるバーで実際に約定価格が変わる**ことを、小さな単体テスト（自作の1バーで検証）で示す。
4. イグジット理由の内訳が `stop` / `target` / `timeout` に正しく分類される（全部同じ理由に潰れていないこと）。
5. matplotlib のフォント欠落警告が出ていないこと。

各項目の確認結果（数値・内訳）を最後に要約して報告すること。

===================================================================

---

## 補足（エージェントに渡す前に読むメモ）

- すでに動作検証済みの参照実装（`kaiten_backtest.py`）を持っている場合は、上記プロンプトの末尾に「**参照実装として `kaiten_backtest.py` を添付する。これをベースに上記要件を満たすよう調整・検証してよい**」と一行加えて一緒に渡すと、エージェントはゼロから書き直さずに済み、結果が安定します。
- デフォルトの深いATR指値だと約定回数がかなり少なくなります。まず動かし、`atr_mult`（例 1.2〜2.0）や `trend_ma_period`（例 60〜100）を緩めて挙動を掴んでから本番パラメータに寄せるのが実務的です。
- 結果を見るときは「総リターン」より先に **プロフィットファクター・最大ドローダウン・イグジット内訳** を確認してください。`timeout` ばかりなら反発を取れておらず、単に握って戻っているだけのサインです。
