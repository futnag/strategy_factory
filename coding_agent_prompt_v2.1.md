# コーディングエージェント向け依頼プロンプト v2.1：価格・出来高統合型マルチタイムスケール・レジーム検知の実装と検証

> このファイルをそのままコーディングエージェント（Claude Code / Cursor 等）に貼り付けて使ってください。
> 併せて設計書 `regime_detection_design_v2.1.md` を渡せると理想ですが、本プロンプト単体でも実装可能なよう自己完結させています。
> 英語の方が安定するエージェントには英訳して渡しても構いません。**冒頭の【最優先・厳守】は要約・省略しないでください。**

> **v2.1 改訂（v2.0 からの差分・アーキ確定）**
> - **[アーキ1] 方向性はセクター/指数から取得**（§3）。単一銘柄の週足方向は識別が弱いため、方向性アンカーはセクター/指数の週足HMMから取り、日足個別層へ注入する。
> - **[アーキ2] 日足ボラ流動性層は「流動性ティア」で partial pooling 推定**（§3・§5）。pooling 強度は最適化せず弱め固定。括りはセクターでなく流動性。
> - **[アーキ3] 週足HMMの整列軸をボラ→モメンタム符号に付け替え**（§1.5）。整列軸は層別（週足=方向／日足=ボラ・流動性）。
> - **[精緻3C] 決算継ぎ目**：リターン/ボラのみダミー化、流動性チャネルは更新継続（§2・§4）。
> - **[精緻4-1] プラセボ検定はブロックシャッフル／位相ランダム化**（§1・§6）。
> - **[精緻4-2] レジーム依存コストの impact 係数は保守側固定＋感応度**（§6）。
> - **[採用] 決算日 spike 近似は循環的→実開示日を準必須**（§2）。
> - **[採用] 状態数 2→3 は事前登録仮説で判定**（§5）。

---

## 0. 役割と最終目的

あなたは定量ファイナンスの検証エンジニアです。**日本株の単一銘柄**を対象に、**価格と出来高を統合した多変量HMM**で「ボラティリティ＋流動性」レジームを日足で検知し、それを**週足（セクター/指数）の方向性レジームの下位に置くマルチタイムスケール構成**で運用、**価値戦略およびPEAD（Post-Earnings Announcement Drift）**のバックテストに統合して、**過去データで厳密に検証**してください。

最終的に答えを出す問いは1つです：

> **価格・出来高統合の二層レジームシステム（週足セクター/指数の方向性アンカー＋日足ボラ流動性）は、単層（週足のみ／日足のみ）・無フィルタ・固定サイジングのベースラインを、取引コスト控除後・前進検証（walk-forward）で上回るか？**

「レジームらしく分かれて見える」ことはゴールではありません。**下流戦略の純付加価値**がゴールです。

**アーキテクチャ確定事項（v2.1・着手前に固定）**：
- **方向性はセクター/指数から取得**（単一銘柄の週足方向は識別が弱いため）。
- **日足ボラ流動性層は流動性ティアで partial pooling 推定**（単一銘柄のデータ枯渇対策。強度は弱め固定・最適化しない）。
- **週足HMMの整列軸はモメンタム符号**（方向性層）。日足はボラ・流動性で整列。

**実装着手順序（厳守）**：`§1 先読み防止` → `§2 PEAD衝突回避` → `§3 マルチタイムスケール` → `§4 特徴量前処理`。先読みを潰さないと他の検証が信用できず、PEAD衝突を解かないと主戦略の評価が歪むためです。

---

## §1【最優先・厳守】先読み（look-ahead）の完全排除

この検証の成否はここで決まります。1つでも破ると全結果が無効です。全モジュール（特徴量・モデル・バックテスト）に適用してください。

1. **filtered を使う／smoothed を評価に使わない。**
   レジーム判定（シグナル生成・評価）は filtered（オンライン）確率のみ。smoothed は可視化専用。両者を別関数・別カラム（`state_filtered` / `state_smoothed`）に分離し、バックテスト側から `*_smoothed` を参照不能にする。

2. **パラメータは walk-forward で過去のみから再推定。**
   全期間一括推定は禁止。拡大窓またはローリング窓で、その時点までのデータのみから `refit_every`（日足20営業日／週足4週を初期値）ごとに再フィットし、以降を filtered で分類。**流動性ティアのプール推定（§5）も同規律**で、横断パネルも≤t のデータのみ・プール構成はポイントインタイム（上場/廃止考慮・サバイバーシップ回避）。

3. **正規化統計も過去のみ。**
   中央値・MAD・トレンド・分位点・Amihudのフロア・**流動性ティアの分位境界**など、スケーリング/層化に使う統計はすべて過去窓のみから算出。全期間統計でのスケーリングは先読みのため禁止。

4. **週足アンカーのラグ。**
   日足時点 t で参照する週足（セクター/指数）レジームは、t の直前に「完了した」週までで推定したもののみ。進行中の週で当該週レジームを確定しない。

5. **ラベルスイッチング整列（v2.1：層別の整列軸）。**
   再フィットのたびに状態を並べ替えて意味を固定する。**整列軸は層で異なる**：
   - **週足（方向性層・セクター/指数）**：平均週次リターン（または mom 符号）昇順 → bear/neutral/bull。
   - **日足（ボラ流動性層・個別）**：ボラ（または平均Amihud）昇順 → calm/stressed。
   整列キーは過去のみから推定したパラメータ（状態平均）を用い、評価対象時点の観測を混入させない。

6. **初期値依存。** EMは複数ランダム初期値＋k-means初期化で再起動し、最良尤度を採用。

7. **持続性制御（ちらつき対策）。** 抽象的フィルタではなく、(a) sticky HMM（自己遷移確率を高く保つ／切替ペナルティ）、(b) 最小滞在期間（minimum dwell time）、(c) filtered確率の閾値処理（ヒステリシス）を実装し ON/OFF 切替可能にする。

8. **look-ahead 検出テスト＋プラセボ検定（v2.1）。**
   - (a) smoothed使用版と filtered版で性能が明確に乖離することを示す。乖離が小さすぎる場合は先読み漏れを疑い調査する。
   - (b) **プラセボ（null）検定**：将来ラベル/リターンを**ブロックシャッフルまたは位相ランダム化**（自己相関を保持）した系列で、レジームシグナルの edge が ≈ 0 になることを確認する。**素朴な IID シャッフルは自己相関を壊し検定力を失うため不可。** (a) が捉えない正規化統計・refit境界・ラベル整列・プール推定のリークを補完する。

---

## §2【最優先・厳守】PEAD等イベント駆動戦略への適用除外

**問題**：PEADの収益源（決算後ドリフト）と、価格・出来高統合特徴量が最も激しく反応する日（決算日）が一致する。素直にフィルタを適用すると、**PEADが最も稼ぐべき決算直後をフィルタが殺す**自己矛盾が起きる。

以下を**2段構え**で実装すること：

1. **特徴量段階の分離（v2.1：チャネル別に明文化）。** 決算日（および翌営業日）について、
   - **リターン/ボラ・チャネル**（実現ボラ・リターン由来特徴量）：**ダミー化／マスク**（イベントダミーで吸収、または当該観測を欠損扱い）し、「決算だから高ボラ・低流動性レジームに転換した」という誤検知を防ぐ。
   - **流動性チャネル**（Amihud・ロバスト正規化出来高）：**更新を継続**する。決算後の流動性は執行サイズに効く実情報なので殺さない。

2. **適用段階の分離。** PEADのエントリー窓（決算後の数営業日）では、**レジームフィルタによる新規ポジション抑制（エントリー可否）を適用しない**。
   - **ただしレジームは「サイジング」には残す**：「フィルタからは外すがサイズ調整には使う」。決算後ドリフトは取りに行くが、流動性が枯渇していれば小さく入る。
   - **継ぎ目の定義（v2.1）**：決算窓でサイジングが読むのは、上記1で**流動性チャネルが更新され続けた filtered 状態**。リターン/ボラ・ジャンプはダミー化済みなので、サイジング値は決算窓でも一意に定義され、流動性で駆動される。
   - この規定はバックテストの他のフィルタ規則に優先する。非イベント駆動の一次戦略（価値戦略等）には通常フィルタを適用する。

> 決算日（イベント日）リストは**実開示日（TDnet等、引け後/場中の別）を準必須**とする。**出来高・ボラの spike による近似は本設計では循環的**（vol/出来高 spike で vol/出来高モデルの除外日を定義することになる）ため、研究用の暫定にとどめ、**PEAD の最終判定には用いない**。

---

## §3【高優先】マルチタイムスケール構成（v2.1：方向性ソース・pooling を改訂）

1. **役割分担を固定する。**
   - **週足HMM（セクター/指数）**：方向性レジーム（強気/弱気/中立）を推定。**単一銘柄の週足方向は識別が弱い**ため、横断的サポートのあるセクター/指数で方向性を捉える（アーキ1）。
   - **日足HMM（個別銘柄・価格・出来高統合・多変量）**：**ボラ・流動性状態のみ**を検知。**日足に方向性を判定させない**。

2. **結合方式は最低2方式を実装・比較。主軸は方式A。**
   - **方式A（主軸・事前分布注入）**：週足（セクター/指数）filtered レジーム確率を、日足HMM各時点の状態事前分布（初期分布）に注入。週足が弱気なら日足「高ボラ・低流動性（危機）」状態の事前を引き上げる。
   - **方式C（第二候補・外生特徴量）**：週足 filtered 確率を日足観測ベクトルに正規化済み1変数として追加。
   - （方式B：週足状態ごとに日足パラメータを別推定。データ分割消費で不安定になりやすく第三候補。実装は任意。）
   - すべて §1-4 のラグ規則を厳守。

3. **流動性ティア partial pooling（アーキ2）。** 日足ボラ流動性HMMは単一銘柄では推定不安定。
   - **流動性ティア（セクターではない）**で銘柄を層化：ADV（平均売買代金）または Amihud 水準の**分位**。ティア割当はポイントインタイム（過去窓のみ）。
   - **partial pooling**：流動性ティアの銘柄横断パネルから**放出パラメータ（各状態の位置・尺度）を推定**し、個別銘柄はそこへ**固定弱シュリンク**。filtered 推論・遷移は銘柄別。最小実装＝「プール放出＋銘柄別遷移＋銘柄別 filtered」。
   - **pooling 強度は最適化せず弱め固定**（グリッド探索しない＝二重過剰適合の回避）。強度の影響は安定性スイープで**確認のみ**、選択はしない。

4. **セクター・アンカー＝方向性の一次ソース。** セクター（業種）/指数の週足 filtered 確率を、個別銘柄日足HMMの事前として注入する。**「注入あり／なし」を比較し、注入なし版を「日足のみ（方向性アンカー無し）」のベースライン**（§6.3-4）とする。まずは週足セクター/指数→日足個別の二層から。

5. **ヒステリシス。** 日足のハードな全切替ではなく、filtered 確率の連続値でエクスポージャーを滑らかに調整。週足アンカーと整合する向きの切替は低閾値で許し、逆行する切替は高閾値を要求する。

---

## §4【標準】特徴量前処理（ロバスト化＋日本株固有処理）

### 4.1 必須特徴量
- **Realized Volatility**：対数リターンのローリング標準偏差（高安あれば Parkinson 推定量）。窓20〜60日。【リターン/ボラ・チャネル】
- **Robust Normalized Volume**：**対数出来高**を**中央値・MADベースのロバストz-score**化。生のz-score（平均・標準偏差）は決算日1点でスケールが汚染されるため禁止。【流動性チャネル】
- **Amihud Illiquidity**：`|r_t| / max(DollarVolume_t, floor)` を**対数変換 → ロバストz-score**。【流動性チャネル】

### 4.2 出来高の構造成分除去
z-score後も残る曜日効果・月末/月初・SQ週・趨勢的増加トレンドを除去する。最低限**ローリング中央値で割ってトレンド除去**、可能なら**曜日・月内位置ダミーで回帰した残差**を用いる。正規化統計は過去窓のみ（§1-3）。

### 4.3 日本株固有のデータ処理（必須）
- **値幅制限到達日**（ストップ高/安）：リターン打ち切りで Amihud が過小化 → 検出・フラグ化。
- **stale price 日**（出来高僅少）：分母極小で Amihud が過大化 → 検出・フラグ化。
- 上記2種は逆方向に歪むため、**両方を欠損相当としてHMM観測から除外**を既定とし、除外有無を切替可能にする。
- **コーポレートアクション**（分割・併合・配当落ち）：調整済み価格を使用。未調整なら調整処理を実装。
- **決算日（v2.1：チャネル別）**：リターン/ボラ・チャネルはダミー化、**流動性チャネルは更新継続**（§2-1）。決算日は実開示日を準必須、spike近似は循環的なため研究用限定。

### 4.4 オプション特徴量
Dollar Volume Proxy（`Close×Volume` のロバストz-score）、出来高変化率、ローリング歪度・尖度。**増やしたら負けを疑う**（§7 参照）。

---

## §5 レジーム検知モデル

- **主軸**：多変量HMM。観測は §4 の特徴量ベクトル（2〜3次元）＋週足事前（§3）。**推定は流動性ティアで pooling（§3-3）**、filtered 推論は銘柄別。
- **状態数**：**2から開始**。**2→3 拡張は事前登録仮説で判定（v2.1）**＝「**高ボラ×流動的（活況トレンド）**」と「**高ボラ×低流動（危機）**」を 2 状態が 1 軸に潰して分離できない、という具体仮説を直接検証する（BICは自己相関下で過大選択する癖があるので鵜呑みにしない）。この 2 モードの分離はフィルタにも PEAD 執行にも決定的。
- **放出分布**：日足はファットテール・決算ジャンプのため **Student-t 放出**または**ボラ標準化後ガウス**を優先。純ガウスは外れ値を偽状態に吸収しやすい。**ガウス vs t を必ず比較**。
- **共分散**：full と diag を比較。
- **学習**：Baum-Welch（EM）＋複数初期値再起動。
- **出力**：filtered 事後確率（主）、Viterbi、smoothed（可視化専用）。
- **比較基準として GMM を実装**：時間的持続性がなく日替わりでちらつくため、HMM の遷移行列の効果を対比する基準として用いる（GMM単体は主軸にしない）。補助的に Change Point Detection（ruptures/PELT）を合成指標に適用してもよい。

---

## §6 バックテスト・検証プロトコル

### 6.1 一次戦略
- **価値戦略**（非イベント駆動）と **PEAD**（イベント駆動）を用意。
- パラメータは**作り込まず粗く固定**（最適化は過剰適合源）。

### 6.2 レジームの活用3経路
1. **レジームフィルタ**：高ボラ・低流動性で新規抑制/縮小（**PEADは §2 で適用除外**）。
2. **メタラベリング**：HMM filtered 状態確率・ラベルを二次モデル特徴量に加え、「このレジームで一次戦略が勝つか」を学習。
3. **動的サイジング**：filtered 確率で連続調整（イベント駆動でも有効）。

### 6.3 必ず比較するベースライン（本丸）
1. バイ・アンド・ホールド
2. 常時無フィルタ・固定サイジングの一次戦略
3. **週足レジームのみ**（日足層なし）
4. **日足レジームのみ**（週足＝方向性アンカーなし）
5. セクター/指数 注入あり vs なし
6. 固定50/50ブレンド（トレンド/平均回帰を併用する場合）

> 3・4 との比較が本丸。**二層が単層を超えなければ複雑さは正当化されない。**

### 6.4 評価条件（v2.1：レジーム依存コスト）
- **すべて前進検証（walk-forward）かつ取引コスト控除後**。日本株の現実的な手数料・スプレッド・マーケットインパクトを仮定し値を明示。**ターンオーバー・取引回数・切替回数を必ず報告**。
- **コストはレジーム依存とする**：単一銘柄はマーケットインパクト支配で、それは検知対象の低流動性レジームそのもの。フラットコストはフィルタが狙う低流動局面でだけコストを過小評価し、判定をフィルタ有利に歪める。
  - マーケットインパクトを同時点の流動性（Amihud）でパラメータ化。
  - **impact 係数は保守側に固定**（低流動レジームのコストを過小評価しない側）。
  - **感応度テストを併設**し、係数を動かしても「二層＞単層」の結論が反転しないこと（**係数が結論のドライバーでないこと**）を示す。
- リーク防止のメタラベリング評価は **Purged K-Fold / CPCV** を併用。ただし**レジーム検知の主指標は walk-forward の filtered 性能**（オンラインで遅延なく当てられるかが本質のため）。
- 指標：年率リターン、ボラ、Sharpe、Sortino、Calmar、最大DD、ヒット率、レジーム別統計、メタラベリングの AUC/Precision。

### 6.5 安定性・多重検定・プラセボ（v2.1）
- **安定性**：初期値再シード、窓長・refit_every の微変更、開始日ずらし、銘柄入替、**流動性ティアの分位境界・pooling 強度の微変更**に対しレジーム系列と最終性能が頑健か。摂動で結論が反転するなら優位は信頼できない。pooling 強度は**選択せず確認のみ**。
- **多重検定**：方式・パラメータ・銘柄を多数試すため最良結果の単純p値を信用しない。複数銘柄・複数期間での再現性、または簡易 FDR/リアリティチェック的補正で「偶然の勝者」を割り引く。
- **プラセボ**：§1-8(b) のブロックシャッフル／位相ランダム化で edge ≈ 0 を確認。

---

## §7 やってはいけないこと（アンチパターン）

- smoothed 確率でシグナルを作る／評価する。
- 全期間で一度だけパラメータ推定する／全期間統計で標準化する。
- 生のz-score（平均・標準偏差）で出来高を正規化する。
- 値幅制限・stale price・コーポレートアクション・決算日を未処理のまま投入する。
- **決算日のリターン/ボラ・ジャンプをダミー化せずレジーム転換と誤認させる**（流動性チャネルは逆に殺さない）。
- **PEADのエントリー窓にレジームフィルタを素通しで適用する**（収益源を殺す）。
- 日足HMMに方向性を判定させる／**単一銘柄の週足に方向性を推定させる**（セクター/指数から取る）。
- **日足ボラ流動性層を単一銘柄だけで推定する**（流動性ティアで pooling する）／**pooling 強度をグリッド最適化する**。
- **コストをフラット固定し低流動レジームのコストを過小評価する**／impact 係数を結果が良くなる側に調整する。
- **プラセボを素朴 IID シャッフルでやる**（自己相関を壊す）。
- in-sample のシャープ最大化で状態数・特徴量・方式を選ぶ／状態・特徴量を増やしてイン・サンプル当てはまりを上げる（二重の過剰適合）。
- 「ラベルがそれっぽい」だけで成功と判断する／取引コスト・ターンオーバーを無視する。

---

## §8 成果物（deliverables）

1. **再現可能なコード（Python想定）**。設定は単一config（YAML/dict）に集約、乱数シード固定。
2. **モジュール構成（目安）**：
   - `data_loader`（週足/日足整合、調整、値幅制限/stale/決算日 検出・フラグ、**流動性ティア割当・銘柄横断パネル・セクター/指数系列**）
   - `features`（ロバスト正規化・構造成分除去・Amihudフロア処理、**チャネル区分**、過去のみ統計）
   - `regime_models`（多変量HMM：Gaussian/t、GMM比較、sticky/min-dwell、**層別ラベル整列**、複数初期値、filtered/smoothed分離、**流動性ティア pooling**）
   - `multiscale`（**週足セクター/指数 方向性HMM**＋方式A/C＋ヒステリシス＋注入＋ラグ規則）
   - `strategies`（価値戦略・PEAD、レジーム活用3経路、§2適用除外、**決算窓は流動性更新後 filtered でサイジング**）
   - `backtest`（walk-forward、**レジーム依存コスト＋感応度**、ターンオーバー）
   - `evaluation`（指標、ベースライン比較、安定性、多重検定、**look-ahead＋プラセボ検定**）
   - `report`（図表）
3. **図表**：filtered レジーム系列（価格オーバーレイ）、レジーム別リターン分布、各戦略エクイティカーブ、ターンオーバー、安定性スイープ、look-ahead/プラセボテスト結果、コスト感応度。
4. **結果レポート（Markdown）**：上記の問い（二層 vs 単層・固定）への明確な結論、根拠数値表、限界、次の検証案。

---

## §9 進め方（推奨順序）

1. `data_loader` と日本株固有処理（値幅制限/stale/決算）、**流動性ティア割当**、look-ahead＋プラセボ検出テストの土台を**最初に**作る。
2. 単層（週足セクター/指数のみ／日足個別のみ）の多変量HMMを walk-forward で動かし、§6 の評価系を通して配管を固める。
3. Gaussian vs t、full vs diag、sticky 有無、GMM 比較、**流動性ティア pooling 有無**。
4. §2 のPEAD適用除外（チャネル別分離）を実装し、価値戦略・PEADのバックテストを通す。
5. §3 のマルチタイムスケール（方式A/C・ヒステリシス・セクター/指数注入）を実装。
6. 全ベースライン比較・安定性スイープ・**レジーム依存コスト感応度**・多重検定補正を回し、レポート化。

各段階で中間結果を出し、**次に進む前に先読み混入と過剰適合の有無を点検**すること。不明点は仮定を明記して進め、後でまとめて確認できるようにする。
