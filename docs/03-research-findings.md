# 03 — 研究知見と検証ファクトリ（確定版・詳細レポート）

**最終更新**: 2026-06-10 / **対象**: `strategy_factory`（GitHub: futnag/strategy_factory, main, 248テスト）
**前提資料**: `docs/01-knowledge-base.md`（López de Prado 統合ナレッジ）, `docs/02-system-design.md`（システム設計 v0.2）

> 本レポートは、本プロジェクトの**経緯・技術的詳細・用語・全研究結果・確定的な結論**を
> 一冊にまとめた確定版である。初見の読者が単体で理解できるよう、専門用語は §3 用語集で
> 解説し、研究の流れは §2 経緯で時系列に追う。

---

## 目次
1. エグゼクティブ・サマリー
2. 経緯 — 目的の変遷と研究の流れ
3. 用語集 — 重要概念の解説
4. 検証ファクトリ（platform）— 技術的詳細
5. データ資産
6. 研究知見 — 全試行の判定
7. 最有力候補 — value ＋ ロングティルト PEAD
8. 方法論が示したこと（教訓）
9. 正直な限界
10. Phase 2（実運用）への展望
11. 付録 — モジュール地図・再現コマンド

---

## 1. エグゼクティブ・サマリー

- **本プロジェクトの成果物は「検証ファクトリ」**である。すなわち、**任意の投資戦略アイデアを、
  蓄積データと統計的検証メソッドで“偽陽性を排除しながら”厳格に判定するシステム**。
  「いますぐ儲かる戦略を見つける」ことではなく、「アイデアを確実・効率的に検証する装置」を
  作ることが、合意した目的である。
- **広範な探索（暗号資産・日本株・財務・信用空売り・指数・投資部門別フロー・決算イベント）の末、
  唯一の耐久的な候補は `value`（割安, book-to-market）**だった。他の有望に見えた信号
  （size, short_to_long など）は**生存者バイアスのアーティファクト**、もしくはエッジ無しと判明。
  **フレームワークは偽アルファを一度も生成しなかった。**
- **最終的な最有力候補**は、PEAD（決算予想改訂）の OOS 失敗を**診断して経済的に修正**した
  **`value ＋ ロングティルト PEAD` の合成**で、**DSR 0.91（プログラム最良）・OOS Sharpe +0.62・
  最大ドローダウン −19.2%・容量約¥6億**。ただし認定基準 DSR≥0.95 には未達で、修正が
  OOS情報を用いた点（後述の留保）がある。
- **次段は Phase 2（実運用配管）**。研究はこれ以上の在サンプル精緻化が過学習と隣接する地点に
  到達しており、**真の検証は“フォワード（小口の実運用＋監視）”でしかできない**。

---

## 2. 経緯 — 目的の変遷と研究の流れ

### 2.1 出発点：López de Prado の金融機械学習
添付された10文書（学術論文＋日本語ブリーフィング）を統合し（`docs/01`）、その要点を備えた
投資システムを構築するのが当初の依頼だった。中核思想は **「過学習・自己 p-hacking こそ
最大の敵」「方向予測より、ボラ・サイジング・相対価値に ML を使う」「因果的に妥当な特徴量」
「全試行を記録し DSR で誠実に評価する」**。まず検証ハーネス（DSR/CPCV/パージング/試行
レジストリ）とデータ層（情報ドリブン・バー、分数階差分）を実装・テストした。

### 2.2 暗号資産での探索と帰結（bitbank BTC/JPY）
- **方向予測にエッジ無し**：4時間足・素朴特徴／真のドルバー／マイクロ構造特徴のいずれでも
  **DSR ≈ 0.2–0.4**（有意でない）。「OHLCV 特徴を足しても本物のα探索には不十分」と確認。
- **ボラは予測可能**：purged-CV の OOS で **将来ボラ R²=0.256**（将来リターン方向は R²=−0.02）。
  López de Prado の主張をデータで再現。ただし単純な vol-targeting では Sharpe 改善に繋がらず
  （実現ボラは 22%→6% に安定化したが、Moreira-Muir のα非有意）。
- **相対価値（クロスセクション）が最も有望だが非有意**：15 JPYペアの long-short ファクターで
  生 Sharpe 0.57 だが **DSR=0.60**（試行数 K=6 補正後）。多因子合成も単独最良を超えず。
- 結論：暗号資産では簡単なエッジ無し（収穫逓減）。**柱C（因果ファクター投資）の本来の土俵
  ＝日本株＋ファンダ**へ転換。

### 2.3 日本株・ファンダへの転換（J-Quants）
- データ源を **J-Quants V2 API**（APIキー方式）に。当初無料枠で検証し、**`/fins/summary`
  （決算サマリー）が無料枠で取得可能**（実績＋予想＋開示日 DiscDate＝バリュー/クオリティ/
  サイズの材料が揃う）と判明。詳細BS/PL/CF の `/fins/details` は Premium 限定。
- **レート制限**（無料5/分・大幅超過で5分遮断）を実装で自衛（ペーシング＋バックオフ）。
- ユーザーが **Standard プラン（120/分・10年履歴）契約**。10年・全銘柄の取得が現実的に。
- **固定ユニバース（全期間流動性で上位300選抜）での10年クロスセクション研究**：
  最良は size（小型株効果）SR 1.10 / **DSR 0.82**、value は弱く(+0.09)、quality はマイナス、
  素朴合成 COMPOSITE は −0.30。**サブ期間分析**で size は 2016-19 に+3.07→直近+0.05 と
  減衰が見えた。

### 2.4 「生存者バイアス」の発見 — 研究の転回点
ユーザーの「10年前のデータ（HFT前）に意味があるか？」という非定常性への問いを受け、
**時間減衰の重み付け**と**サブ期間安定性検定**を実装。さらに**ポイントインタイム・ユニバース**
（各時点の trailing 流動性で時変選抜＝先読み・生存者バイアスを排除）に切り替えたところ、
**結論が覆った**：

| factor | 固定300（バイアス有） | PITユニバース（補正後） |
|---|---|---|
| size | SR +1.10 / **DSR 0.82** | SR −0.33 / **DSR 0.35**（崩壊） |
| short_to_long（信用） | \|SR\| 1.14 / **DSR 0.68** | −0.22 / **0.24**（崩壊） |
| **value（book/market）** | +0.09 / 弱 | **+0.36（直近+0.87）/ DSR 0.38**（生存・強化） |

**＝最も有望に見えた2信号（size, short_to_long）は生存者バイアスの産物**で、厳密な方法論が
存在しないエッジへの賭けを未然に防いだ。**value だけが補正後も生き残った。**

### 2.5 目的の再定義：検証ファクトリ
ユーザーが方向性を明確化：「いますぐ効く戦略探しではなく、**思いついた戦略を蓄積データと
検証メソッドで効率的・確実に検証するシステムを作る**こと」。4要件（①戦略非依存の判定器
②戦略記述テンプレ ③コアデータ網羅 ④差分更新）を合意。私の批判的提案として
**「判定器の価値はグローバル試行管理＋事前登録＋PIT構造化が9割。DSLは罠（コードIF優先）。
リーク防止はフレームワークが所有。全DLは過剰（コア網羅＋オンデマンド）。現実性を一級市民に」**
を提示し、**MVP判定器を先に／コードIF＋ルール糖衣／コア網羅＋オンデマンド**で合意。

### 2.6 検証ファクトリの構築と戦略の検証
MVP（PITデータビュー→戦略IF→エンジン→判定器）を構築（§4）。続いて多様な戦略アーキタイプ
を判定器にかけた（§6.4）。さらに二大候補 `value` と `PEAD`（予想改訂）を深掘りし、
**PEAD の OOS 失敗を診断→ロングティルト化で修正**し、最有力候補（§7）に到達。研究フェーズを
本レポートで確定する。

---

## 3. 用語集 — 重要概念の解説

本プロジェクトの判断はすべて以下の概念に依拠する。

### 統計・検証
- **Sharpe比（SR）**: 平均超過リターン ÷ リターンの標準偏差。リスク1単位あたりの収益。
  年率Sharpe = per-period Sharpe × √(年間期間数)。月次なら ×√12。
- **PSR（Probabilistic Sharpe Ratio, 確率的シャープ比）**: 観測 Sharpe が与えられたとき、
  **真の Sharpe がベンチマーク（通常0）を超える確率**。標本長・歪度・尖度（非正規性）を補正。
  「この Sharpe は本当に正か？」を確率で答える。
- **E[max SR]（期待最大シャープ／False Strategy 定理）**: 真の Sharpe が全て0の N 個の独立な
  試行から得られる**最大 Sharpe の期待値**。試行を増やすほど「偶然の最良」が大きくなる。
  López de Prado & Lewis (2018)。**多重検定の罠の定量化**。
- **DSR（Deflated Sharpe Ratio, デフレート・シャープ比）**: PSR をベンチマーク=E[max SR] に
  して計算した値＝**「多重検定・非正規性・標本長を補正した“真の Sharpe>0”確率」**。
  本プロジェクトの**最終判定指標**。DSR≥0.95 を「多重検定後も有意（PASS）」とする。
  **試行（K）を増やすほど基準が上がる**ため、人が「通るまで回す」ことを封じる。
- **minTRL（minimum Track Record Length, 最小実績長）**: 観測 Sharpe を 95% 信頼で「正」と
  認定するのに必要な最小観測数。低 Sharpe ほど長い履歴が要る（例：年率0.36なら約241か月≈20年）。
- **IC（Information Coefficient, 情報係数）**: 各時点で「ファクター値 と 翌期リターン」の
  クロスセクション順位相関。**シグナルの予測力**を測る。正で安定なら有効。
- **IS / OOS（In-Sample / Out-Of-Sample）**: 設計・検証に使う期間 / 設計に使わず取っておく
  検証期間。OOSで持続するかが頑健性の証拠。
- **purging（パージング）/ embargo（エンバーゴ）**: 学習と検証の標本が時間的に重なって
  情報が漏れるのを防ぐ除外処理。**CPCV（Combinatorial Purged Cross-Validation）**は
  パージング付きの組合せ交差検証で、複数の train/test 分割（パス）で戦略を評価しレジーム
  依存を露出させる。

### データ・構築
- **ポイントインタイム（PIT）整合**: 各意思決定時点 t で「t 以前に実際に利用可能だった情報」
  のみを使うこと。**先読み（look-ahead）バイアス**＝未来情報の混入を断つ最重要規律。
- **生存者バイアス（survivorship bias）**: 「最後まで生き残った／流動的だった銘柄」だけで
  分析すると、敗者・退場銘柄が抜け落ち、成績が過大評価される歪み。本プロジェクトでは
  **固定ユニバース選抜**がこれを生み、**PITユニバース**（時変選抜）で除去した。
- **セクター中立化（sector-neutral）**: 各時点で同一業種内の平均を引き、業種という共通要因
  （交絡因子）を除いてファクター固有の効果を取り出す。
- **分数階差分（FFD）**: 系列を定常化しつつ「記憶（自己相関）」を最大限保つ前処理
  （López de Prado）。整数階差分（リターン化）は記憶を捨て過ぎる。
- **情報ドリブン・バー（ドルバー等）**: 一定時間でなく一定の売買代金ごとにバーを刻む。
  実データで超過尖度 5.27→0.97 等、**IID正規に近づく**（統計的性質が改善）。

### 戦略・因果
- **クロスセクション・ファクター**: ある時点で全銘柄を指標でランクし、上位ロング/下位ショート。
- **ロングショート / ダラーニュートラル**: 買いと売りを同額に組み、市場全体の動き（ベータ）を
  相殺して銘柄選択の超過収益（α）を取り出す。
- **value（バリュー）**: 割安株（高 book-to-market＝純資産/時価総額が大）が割高株を上回るという
  リスク/行動プレミアム。**PEAD（Post-Earnings-Announcement Drift）**: 決算・予想改訂後に
  株価が漸進的にドリフトする現象（本プロジェクトでは会社予想の改訂率を用いた）。
- **メタラベリング（corrective AI）**: 一次モデルの「方向」に対し、二次モデルが「いつ・
  どれだけ賭けるか（サイズ）」を学習する補正層（López de Prado ch.10）。
- **交絡因子（confounder）/ 合流点（collider）**: 因果ダイアグラム上、制御すべき変数
  （X←Z→Y）と、制御してはならない変数（X→Z←Y）。合流点を回帰に入れると偽相関が生じ
  推定符号が反転しうる。本プロジェクトの因果フィルタ（pairwise LiNGAM）はこれを判定する。
- **容量（capacity）**: 市場インパクトを許容範囲に保ったまま運用できる資金規模の上限。
  ここでは「最も流動性の薄い建玉が participation×ADV（平均売買代金）に達する AUM」と定義。

---

## 4. 検証ファクトリ（platform）— 技術的詳細

`invest_system/research/` が本体。設計思想は **「人が判定器自身を p-hack できないこと」**。

### 4.1 全体アーキテクチャ（層）
```
データ層(data/) ── 差分更新 ──▶ ローカルParquet
        │
        ▼  PITデータビュー(AsOfView) ＝ 戦略に「基準日以前」しか見せない
戦略(Strategy契約) ── target_weights(asof) ──▶ 目標ウェイト
        │
        ▼  エンジン(engine) ＝ コスト・執行ラグ・容量込みで損益系列化
判定器(judge) ── デフレートDSR + 永続レジストリ + PSR/minTRL/サブ期間/容量 ──▶ 判定レポート(md/HTML)
```

### 4.2 PITデータビュー（`data_view.py`）— リーク遮断の核
`AsOfView.asof(t)` は構築時に全パネルを「日付 ≤ t」へスライス済みの `AsOf` を返す。
戦略は `AsOf` からしかデータを読めず、**未来のバーを参照する術が構造的に存在しない**。
＝戦略作者の善意に依存せず、フレームワークが先読みを物理的に不可能にする。

### 4.3 戦略インターフェース（`strategy.py`）— DSL でなくコード契約
`Strategy.target_weights(asof) -> Series(銘柄→比率)` の一契約。実装済みアーキタイプ：
- `GapReversal`（イベント駆動：ギャップ後）、`CrossSectionalStrategy`（ランク→LS、`long_only`で
  市場ヘッジ・ロングティルト可）、`SignalTimingStrategy`（外部シグナル符号で単一銘柄）、
  `CalendarStrategy`（暦日条件）、`PairsStrategy`（2銘柄スプレッドz平均回帰）、
  `EarningsRunup`（日次イベント窓）、`CompositeStrategy`（複数戦略をウェイト合成）。
- 宣言的ルールは薄いクラスで“糖衣”化。複雑戦略（ニュース等）も同契約を実装すれば差せる。

### 4.4 バックテスト・エンジン（`engine.py`）— 現実性を一級市民に
- 各 t で `target_weights` を得て、t→t+1 の実現リターンと内積＝損益。空シグナル日は現金。
- **取引コスト**：回転率 sum|Δw| に片道bpsを乗じて控除。
- **執行ラグ（execution_lag）**：0=決定足の終値で執行、1=翌足で執行＝**観測した終値で建てない**
  （同足クローズの先読みを排除する現実寄り）。
- **容量（capacity_jpy）**：ADVパネルを与えると「最も薄い建玉が participation×ADV に達する
  AUM上限」を算出。**“紙上は効くが規模を持てない戦略”を露出**。
- **値幅制限（no_buy/no_sell・DP15）**：ストップ高/安の**引け張り付き**銘柄（UL/LL フラグ＋
  close==high/low）と出来高ゼロ日は執行不能＝**前回ウェイトをキャリー**（買えない買い・
  売れない売りを翌リバランスへ持ち越し）。回転率・コストも実際に執行できた分のみ計上。
  フラグ構築は `equities/frictions.limit_lock_flags`（日本固有の罠を損益に反映）。
- **ショート貸株コスト（short_borrow_bps・DP15）**：短グロス sum|w<0| に年率bpsを期間按分で
  賦課（制度貸株料≈115bps＋逆日歩バッファの保守見積もり）。貸借銘柄（制度で売建可能）の
  PIT マスクは `frictions.shortable_mask`（週次信用残 IssType==2・公表ラグ込み）。
- 年率係数は日付間隔から自動推定（日次≈252, 月次≈12）。

### 4.5 判定器（`judge.py` ＋ `validation/`）
- `judge_grid(strategies, ...)`：戦略群（=1アイデアのパラメータ格子）をまとめて裁く。各点を
  `TrialRegistry` に**事前登録＋結果記録**（仮説と経済的合理性が必須＝a priori 理論の強制）。
- **DSR は scope の累計試行 K と Sharpe 分散でデフレート**。**パラメータ格子の各点も独立試行**
  ＝1つのアイデアでも閾値・保有・ユニバース等で数十試行に化けることを正しく計数。
- **永続グローバル・レジストリ**（SQLite, `data/research_trials.db`, gitignore）：試行は
  セッションを跨いで累積。**指紋（scope＋戦略＋params）UPSERT で再実行は冪等**（Kを水増し
  できない）、新変種は K を増やし**全戦略の基準を上げる**＝「通るまで回す／黙って試行を増やす」
  が効かない。
- 併せて **PSR・minTRL・サブ期間安定性・回転率・最大DD・容量** を報告し、**PASS/FAIL** を
  md / **HTML（インラインSVGのエクイティ曲線, 依存追加なし）** で出力。

### 4.6 偽陽性排除の二大柱
1. **先読み（構造）**：AsOfView ＋ PITユニバース。
2. **選択バイアス（永続デフレートDSR）**：グローバル試行管理。
加えて **現実性**（コスト・執行ラグ・容量・値幅制限・貸株コスト）と **保留OOS** で多面的に
弾く。テスト 248 件でこれらの性質（先読み排除・冪等・デフレート・容量算出・執行不能キャリー）
を固定。

---

## 5. データ資産（ローカル・gitignore・全件ミラーで約1GB級）

Standard 提供の全データセットを**全営業日 by-date ミラー**（2016-06〜現在）として保持し、差分更新で
最新化する。市場データは J-Quants 利用規約によりコミットせず `data/`（gitignore）にのみ置く。
公式プラン表との照合で **Standard で取得可能なデータはすべて網羅**（下記の Premium 限定を除く）。

| 分類 | 内容 | 取得軸 |
|---|---|---|
| 上場銘柄マスタ | `CoName/S33(業種)/Mkt(市場)` 等 4,443社 | 1スナップ |
| 日次株価四本値 | **全営業日・全銘柄**（AdjC=分割調整, Va=売買代金, UL/LL=制限値幅） | by-date 全件ミラー |
| 財務サマリー | 決算短信（売上/利益/EPS/BPS/純資産＋**予想FEPS**＋**配当 DivFY/DivAnn/配当性向**＋開示日DiscDate） | **by-date 全件ミラー**（`fins_summary/`） |
| 日経225オプション | 全営業日・全契約（**IV**/Theo理論値/Settle清算値/OI建玉/UnderPx原資産/Strike）約2,600日 | by-date |
| 信用取引週末残高 | **全銘柄・週次**（買残LongVol/売残ShrtVol、制度/一般内訳）約205万行 | 金曜date |
| 空売り残高報告 | 大口空売り（対発行株比率）約136万行 | calc_date |
| 日次公表信用 | 規制銘柄の日次信用 約44万行 | date |
| 業種別空売り比率 | 33業種・全期間 約7.9万行 | s33 |
| 株価指数 | 79種（**TOPIX=0000**＝専用 `/indices/bars/daily/topix` と実値一致確認済）約18万行 | by-code |
| 投資部門別フロー | 週次・市場区分別の13主体（海外/個人/投信…）売買 | from/to |

- **ファンダ panel 組立**（`equities/fundamentals.py`）：`load_fundamentals()` が by-date ミラー
  `fins_summary/`（＋旧 by-code `statements/`）を併合・重複除去して長形式で返し、
  `fundamentals_panel()` が `point_in_time`（DiscDate≤t−lag のみ採用）で**全ユニバースの as-of
  パネル**を先読みなし・銘柄別ネットワーク不要で1呼び出し組立。
- **差分更新／一括取得**：`data/updater.py`（`update`一発で by-date＋range-refresh を最新化、冪等・
  再開可能）。大規模初回バックフィルは **`examples/download_jquants.py`**（手元ターミナルで直接実行＝
  Claude を介さず、進捗表示・中断再開可）。
- **Processed (Silver) 層**：Raw(by-date) を分析最適化の **フィールド別 wide パネル**（index=Date,
  col=Code, `processed/equities/wide/<field>.parquet`）に materialize（`data/store.py`：`materialize_wide`／
  `rebuild_adjusted`／`load_wide`／`materialize_long`／`health_check`）。**生(無調整)は純 append・
  調整後(adj_*)は派生**（分割で過去が back-adjust されるため累積係数で再構築。実データの分割銘柄で
  J-Quants の AdjC と一致を確認）。`load_daily_panel(base=None)` は Silver を優先読み（実測
  **11.2s→0.24s ≈47倍**、未生成なら Raw にフォールバック）。`updater.update(materialize=True)` で
  Raw 更新→Silver 整形まで一気通貫。補助に partitioned-long（year パーティション・DuckDB/SQL用）。
  3層＝**Raw(不変)/Processed(派生)/Features**。市場データは `data/`（gitignore）。
- **Features (Gold) 層**：Silver から派生特徴を再計算 materialize（`data/feature_store.py`：
  `build_price_features`／`build_regime`／`materialize_features`／`load_feature`）。価格特徴
  （returns/log_returns/vol_20/momentum_12_1/reversal_5・wide・float32）＋市場レジーム（等加重
  マーケット実現ボラの**拡張窓 percentile 三分位**＋trailing トレンド）。**すべて adj_close から因果
  計算＝先読みなし**（f[t] は ≤t のみ）。分数階差分は研究依存(d)・高コストのため bulk 非材化＝
  `features/frac_diff.py` をオンデマンド適用。実材化 2,439日×5,367銘柄×5特徴＋regime を 7.4s。
- **未取得（要 Premium）**: 売買内訳（日次需給）・財務諸表詳細(BS/PL/CF, `/fins/details`)・先物・
  **225以外のオプション**・配当金詳細(`/fins/dividend`)・前場四本値。
  ※ **基本の配当・予想配当・配当性向は `/fins/summary` に内包**＝Standard でも配当利回り系は構築可。
  **外部要**: ニュース本文（NLP）。
- **重要事実**: 全銘柄の信用残高は「週次」のみ公表（日次は規制銘柄のみ＝JPX開示ルール）。
  決算発表予定 `/equities/earnings-calendar` は**前向きスナップショットのみ**で履歴は無く、
  バックテストの発表日は **DiscDate（実開示日）から復元**する。
- **外部クロスアセット・マクロ（マクロ特徴量用・J-Quantsとは別系統）**：価格＝`data/investers/`
  （investing.com 由来 OHLCV・23系列・**2010〜2026**：指数/指数先物/FX/商品金属）、マクロ＝
  `data/supplemental/macro_extended.parquet`（FRED/yfinance：日米金利・政策金利・CPI・VIX・GBPJPY）。
  正準ローダ `invest_system/data/external.py` が英語キーの wide で返し（`load_external_prices`／
  `load_macro`／`list_external`）、`asof_align`（≤t−lag の as-of・ffill・先読みなし）で JP リバランス日に
  整合して `AsOfView` 特徴量にできる。source-of-truth＝価格は investers・supplemental は重複しないマクロ
  のみ（VIX 重複は正規化）。市場データは ToS によりコミットせず `data/`（gitignore）。取得スクリプト
  （`fetch_*`／`clean_investing_csv`／`merge_macro_data*`）の .env 化・パッケージ化は次段（未統合）。

---

## 6. 研究知見 — 全試行の判定

### 6.1 暗号資産（要約）
方向：DSR≈0.3（エッジ無）。ボラ：予測可（OOS R²0.26）だが単純収益化は不発。相対価値：
最も有望だが DSR0.60（非有意）。→ 日本株へ。

### 6.2 日本株・固定ユニバース（バイアス有）
size が最良に見えた（DSR0.82）が、後述の通り生存者バイアスの産物。

### 6.3 生存者バイアスの発見（最重要・§2.4 再掲）
**PITユニバース**で size(0.82→0.35)・short_to_long(0.68→0.24) が崩壊、**value だけが生存・強化**。
**＝厳密な方法論が、存在しない2つのエッジへの賭けを防いだ。** これが本研究の最重要の教訓。

### 6.4 戦略アーキタイプの検証（検証ファクトリ）
| 戦略ファミリー（scope） | アーキタイプ | 最良DSR | 結論 |
|---|---|--:|---|
| **value** | クロスセクション | 0.79–0.84 | ⭐唯一の耐久候補・OOS持続 |
| **value+pead_lt（ロングティルト合成）** | 合成 | **0.91** | ⭐**最有力**・OOS+0.62（§7） |
| value+pead（旧・LS合成） | 合成 | 0.87 | 全期間は良いが**OOS+0.06に失速** |
| PEAD（予想改訂・LS） | クロスセクション | 0.65–0.72 | IS有望・**OOS脆弱**（§7で解明） |
| pead_longtilt（単独） | クロスセクション | 0.78 | maxDD−9.8%、OOS+0.37 |
| **value_pead_regime（value×ボラレジーム・柱C×レジーム）** | クロスセクション×レジーム | **0.96** | ⭐**value は高ボラで劣後**＝vol≤1 ゲートで DSR0.47→0.96・OOS+0.54・maxDD−37→−8%（§6.9・scope K=7） |
| **value_pead_switch（value↔PEAD 切替・柱C×レジーム）** | レジーム条件付き合成 | **0.99** | 平常=value/混乱=PEAD 切替で SR+0.97・OOS+0.94・被覆≈100%。割当は in-sample（§6.10・scope K=4） |
| **value_pead_wfswitch（walk-forward 適応切替）** | レジーム条件付き合成 | **0.97** | ⭐**最有力**：割当を過去のみから学習しても DSR0.97・OOS+0.92 維持＝静的割当の 86%を再発見＝過学習でない（§6.11・scope K=4） |
| short_interest（空売り残高） | クロスセクション | 0.09 | 空売り残高多＝ショート勢が正・逆張りは弱 |
| gap reversal | イベント駆動 | 0.21–0.25 | 多重検定デフレートの教科書例 |
| flow→TOPIX | 時系列タイミング | 0.28 | 同時相関+0.39だが予測力は弱 |
| calendar（月末効果） | 時系列タイミング | 0.38(SR負) | 本期間で効果消失 |
| pairs（トヨタ/ホンダ） | ペア/相対価値 | 0.02 | スプレッドはトレンドし回帰せず |
| **meanrev_pairs（共和分ペア・柱D）** | ペア/相対価値 | **0.03** | 昇格(CADF+動的ヘッジ)＋全326候補K計上後も耐久エッジ無し（§6.6） |
| **price_factors_gold（価格ファクター・Gold層）** | クロスセクション | **0.06** | momentum/low-vol/reversal いずれも負・regime gate も改善せず（§6.7） |
| **meanrev_regime（レジームMR・柱D）** | ペア/相対価値 | **0.00** | トレンド/ボラの regime ゲートでも全 regime で MR 負＝改善せず（§6.8） |
| earnings run-up（日次） | 日次イベント窓 | 0.21 | 正しく実装してもエッジ不在 |
| size / short_to_long | クロスセクション | 0.35 / 0.24 | 生存者バイアスの産物（崩壊済） |

**永続レジストリ**: 20 scope・累計691試行（うち meanrev_pairs K=326・meanrev_regime K=286＝いずれもペア探索のSBuMTを全候補で計上、value_pead_regime K=7・value_pead_switch/wfswitch 各 K=4）を `data/research_trials.db` に記録。
`examples/registry_status.py` ／ `data/reports/index.html`（ダッシュボード）で俯瞰。

#### gap 戦略に見るデフレートの威力（具体例）
12通りの格子で、生の最良は SR+0.78 / **PSR 0.98**（98%“正”に見える）だが、**12試行で
デフレートした DSR=0.25** → FAIL。素朴なバックテストなら勝ち戦略と誤認するところを、
判定器が best-of-12 のチェリーピックを却下した。

### 6.5 多ファクター breadth — 全ユニバース検証（実行待ち：全件ミラーDL後）
Grinold の能動運用の基本法則 **IR ≈ IC × √breadth** に基づく拡張。相関の低い経済的プレミアを
増やして「独立な賭けの数（breadth）」を稼ぐ方針＝**速度（ティック/HFT）でなく幅（日次ファクター）
で勝つ**。ソロ運用の比較優位は低頻度側にある。
- **シグナル**：value(B/M)・momentum(12-1)・quality(ROE＋**accruals＝利益の質**)・**low_volatility(低ボラ)**。
  low_volatility と accruals は今回追加（`equities/factors.py`）。配当利回りも `/fins/summary` 内包で利用可。
- **全ユニバース組立**：`equities/fundamentals.py` の `fundamentals_panel()` ＝ `load_fundamentals`
  （`fins_summary/` 全件 by-date ミラーを長形式化、旧 by-code も併合・重複除去）→ `point_in_time`
  （DiscDate≤t−lag）で先読みなし as-of。研究例は銘柄別ループから本経路へ移行済み（全11本）。
- **検証**：`examples/research_breadth_factors.py`（単一4ファクター＋合成を判定器で裁定、TOP_N=500）。
  **永続レジストリの試行数K増加で DSR 閾値も上がる**ため、合成がSR最良でも DSR は別要件＝幅を
  増やしても偽陽性防止は緩めない。**実測は全件ミラー（fins_summary / daily_quotes）DL完了後**。

### 6.6 時系列・統計的裁定（共和分ペア）— 実装・フル検証【柱D・Ernie Chan 補完】
平均回帰（スプレッドの定常性を収益化）を**第3の独立アルファ源（柱D）**として検証ファクトリに
実装した。予測（柱B）でも risk-premium 帰属（柱C）でもない別系統で、**市場中立・低相関**ゆえ
§6.5 の breadth（IR≈IC×√breadth）を補強する。原理は `docs/01`§11、設計は `docs/02`§5.2・DP12-14。
- **なぜ今か（既存の素朴 pairs の失敗を踏まえて）**：§6.4 の `pairs（トヨタ/ホンダ）DSR 0.02` は、
  **共和分を検定せず固定ヘッジ比で z 回帰した**素朴版の結果（「スプレッドがトレンドし回帰せず」）。
  これは Chan 手法の不在そのものが原因で、(a) 共和分ゲート未実施 (b) 固定ヘッジ比 (c) 半減期に基づく
  保有期間設計の欠如、を補えば**検証可能な仮説に昇格**する。
- **構築（実装済み）**：`timeseries/` に 半減期(OU)/Hurst/分散比/CADF・Johansen・Kalman。
  戦略は `research/strategies_meanrev.py` の `CointegratedPairs`（素朴 `PairsStrategy` の昇格＝CADF
  ゲート＋AsOf 動的ヘッジ）・`JohansenBasket`・`LinearMeanReversion`。日足 wide は `panel.load_daily_panel`。
- **検証規律（非交渉）**：① **ペア探索は全候補を試行計上**（N(N−1)/2 ＝巨大な多重検定面・DP13）＝
  「共和分ペアを見つけた」を在サンプルの偶然から守る。② ヘッジ比/共和分は **AsOf 推定**（先読み厳禁・
  DP12、Kalman が自然に解決）。③ 保有期間・再校正は**半減期から導出**（DP14）。
- **即実行可能**：取得済みの**日足全銘柄ミラー＋業種マスタ(S33)**で、業種内ペアを追加DLなしにスキャン
  できる。判定は `judge_grid(scope="meanrev_pairs")` でコスト・執行ラグ・容量込み。
- **スクリプト（実装済み）**：`examples/research_meanrev_pairs.py`（業種内ペアを CADF ゲート→全候補を
  registry 計上→DSR デフレート）。`judge_grid(extra_trials=)` でスキャン総数を K に算入（DP13）。
- **フル検証（既定パラメータ・全期間 2016–2026・2439営業日）**：業種内 326 候補（34業種×流動性上位5）を
  走査→44 が形成窓(504日)CADF を通過、**全 326 を K に計上**（建玉44＋スキャン282・V[SR]=0.0003）。最良
  ペアでも **SR(ann)+0.23・DSR0.03（K=326 でデフレート）＝FAIL**、minTRL≈12000か月（事実上認定不能）、
  サブ期間も符号反転（+0.16→−0.31→+0.50＝不安定）。共和分ゲート＋動的ヘッジへの昇格でも**偽アルファは
  生成されず**（§6.4 の素朴 pairs DSR0.02・スモークと整合）＝**厳密な多重検定後に耐久エッジ無し**。意義は
  「検証ファクトリが第3アルファ源(柱D)でも正しく機能した」こと。インフラ確立済み＝今後は変種（Kalman
  ヘッジ・entry/exit 格子・半減期連動保有・Johansen バスケット）を同 scope の追加試行として同一規律で裁ける。
- **変種探索（exploratory・2020+・throwaway registry）**：method∈{rolling_ols, kalman}×entry∈{1.5,2.0,2.5}
  を共和分70ペアで比較。**全セルで中央値SRが負**（大半のペアが損）、Kalman は rolling_ols より明確に劣化
  （中央値SR −1.1〜−1.5・勝率3–6%）、最良セルでも best-of-70 の DSR≤0.42。**変種は raw 経済性を改善せず**
  ＝平均回帰(この型)に耐久エッジ無しを再確認。さらなる変種調律は p-hacking 化のため打ち切り。

### 6.7 Gold 層の価格ファクター＋regime gate — 検証（データ基盤の実証）
Processed/Features 3層（§5）の実証として、Gold 由来の価格ファクターを月次クロスセクションで裁定
（`examples/research_price_factors.py`・API 不要・全 PIT・コスト/容量/DSR 込み・月末121・上位500）。
- **結果（K=4・全期間）**：momentum_12_1(SR −0.38)・low_vol(−0.27)・reversal_5(−0.71)・
  momentum_regime_gated(−0.64) は**いずれも負**で最良 DSR 0.06＝**FAIL**。**momentum が負**は「日本は
  モメンタムの例外」(Asness 等)と整合。reversal は月次×高回転(2.97)で不利、**高ボラ回避の regime gate は
  momentum を改善せず**(−0.38→−0.64)。
- **意義**：Silver/Gold → 判定ファネルの**配管が end-to-end で機能**することを実証（API 不要・高速・PIT）。
  価格単独ファクターに JP 株での耐久エッジ無しという、§6.4-6.6 と整合する正直な結論。レジーム条件付けは
  この素朴版では非有効＝メタラベル化は今後の課題。**別レジーム定義（市場トレンド/ボラ overlay）は §6.8 で検証**。

### 6.8 Regime-Switching Mean Reversion（市場レジームで MR をゲート）— 検証【柱D】
§6.6 の共和分ペア MR の**サブ期間符号反転**を、市場レジーム（トレンド強度・ボラ）で建玉を限定すれば
改善できるか（サブ期間安定性→DSR向上）を正直に検証。実装は `timeseries/regime.py`＋`research.RegimeGated`。
- **レジーム定義（事前固定・PIT）**：① **トレンド強度**＝等加重マーケット水準の Kaufman Efficiency Ratio
  （正味移動/経路長・窓60）、② **ボラ**＝市場実現ボラ。いずれも**拡張窓 percentile の三分位**（0/1/2、≤t 分布で
  閾値＝先読みなし・build_regime と同方式）。regime[t] は close[t] 由来＝z[t] と同 as-of、執行 execution_lag=1
  （同足先読み排除）。**定義は経済的合理性から事前固定＝定義探索で K を水増ししない**（KB §11.7）。
- **設計**：形成窓 CADF で選別した共和分ペアを**等加重プール**（baseline）に束ね、`RegimeGated` で「レンジのみ
  (trend=0)」「強トレンド回避(trend≤1)」「高ボラ回避(vol≤1)」の3変種を生成。baseline＋3変種を**同一 scope** で
  判定（K公正）、スキャン全候補を extra_trials 計上。プール日次ウェイトは一度だけ PIT 生成し再利用（再検定回避）。
- **結果（全期間 2016–2026・1935取引日・K=286）**：4変種すべて **DSR 0.00＝FAIL**（最良 range_only でも
  SR(ann)−0.72・サブ期間 −0.97/−1.70/+0.30 で依然不安定）。決定打は **regime_breakdown**：baseline プールの
  P&L が**全レジームで負**（トレンド別 Sharpe −1.05/−2.13/−1.83、ボラ別 −2.24/−1.61/−0.99）。**「有利レジームに
  退避する」前提が成立しない**（形成後に共和分が崩壊）。順序も仮説と逆（強トレンド・高ボラが最悪ではない）。
  ゲートで“最良”に見えた range_only も**建玉率を 32.9%→13.9% に絞り損失額が小さいだけ**でアルファ生成ではない。
- **意義**：レジームスイッチは**失敗要因②（サブ期間不安定）には作用しうるが、①（多重検定 K）は軽減せず、
  エッジが全レジームで不在なら救済しない**＝§6.6 を補強。設計通り偽アルファを生成しなかった。確立したインフラ
  （`timeseries/regime.py`・`RegimeGated`・`regime_breakdown`）は、**レジーム分離を示す別戦略には無改造で再利用可能**
  ＝市場レジーム overlay はこの型の MR には非有効、という §6.7 の「別レジーム定義」課題への一つの回答。
- **スクリプト（実装済み）**：`examples/research_meanrev_regime.py`（ゲート前に `regime_breakdown` で P&L のレジーム
  分離を診断＝ゲートの当否を建玉前に判定）。先読み不変は `tests/test_regime.py` で固定（未来改変→≤t 不変を assert）。

### 6.9 value/PEAD × レジーム — 分離の発見と vol ゲートの効果【柱C × レジーム・初の陽性】
§6.8 の規律「**ゲート前に regime_breakdown で分離を確認**」を、エッジを持つ value/PEAD に適用したところ、
MR と正反対に**明確な分離**が出た（インフラ導入後、初のレジーム陽性所見）。
- **分離（regime_breakdown・月次・年率Sharpe）**：
  - **value × ボラ**：低 +1.21 / 中 +0.88 / **高 −1.04** ＝**単調分離**。高ボラ（flight-to-quality）で
    バリューが劣後する経済仮説が的中。 ・value × トレンド：−0.01/+0.86/+0.09＝**非単調・分離なし**。
  - **PEAD × ボラ**：−0.76/−0.52/**+1.52** ＝**向きが逆**（混乱期に予想改訂が効く）。value と PEAD で
    ボラ依存が逆という構造的発見。
- **ゲート効果（事前固定・PIT・scope K=7）**：高ボラ回避 `value|vol<=1` が **DSR 0.47→0.96（PASS）・
  SR +0.25→+0.76・OOS +0.36→+0.54・maxDD −37%→−8%・前/後2020 +0.39/+0.91**。`value|trend<=1` は分離が
  無いため DSR 0.46≒baseline＝**無効**。**規律が機能**：診断が「効くゲート(vol)/効かないゲート(trend)」を
  建玉前に正しく判別した。
- **レジーム定義（PIT）**：日次マーケットの実現ボラを拡張窓三分位化（≤t 閾値）、月末に as-of 整合
  （reindex ffill）。regime[t]=月末 close[t] 由来＝因子と同 as-of・先読み無。`RegimeGated`＝規則ベースの
  メタラベル（「この賭けに乗るか」を局面で決定）。
- **解釈と節度**：① **scope 局所 DSR（K=7）**＝この実験面での値。§6.4 の注の通り**全 683 試行でグローバルに
  デフレートすれば基準は上がる**ので「**強い候補**」であって最終GOではない。② だが**分離は経済的に動機づけ
  られ OOS(2024+)も pre/post-2020 も持続**＝ノイズではない。③ 効果の源は value(LS) の高ボラ損失（2020/3 等）を
  外すこと＝**メタラベリングの教科書的成功**（高Recall一次×局面選別で Precision 向上）。
- **含意（次の一手）**：value と PEAD の**ボラ依存が逆**＝レジーム条件付き合成（平常は value・混乱期は PEAD）が
  `value+pead_lt|vol<=1`(DSR0.82) より精緻化の余地。ML メタラベリングは月数≈120 で過学習リスク高→まず本
  規則版を OOS 運用で検証。スクリプト `examples/research_value_pead_regime.py`（baseline＋vol/trend ゲートを
  同一 scope で判定・regime_breakdown 診断・IS/OOS 併記）。

### 6.10 value↔PEAD レジーム条件付き切替 — 相補スリーブの最有力候補【柱C × レジーム】
§6.9 で value と PEAD の**ボラ依存が逆**（value=平常 +1.2、PEAD=混乱 +1.5）と判明。これを利用し、
`RegimeSwitch`（≤t レジームで戦略を切替・PIT）で「**平常(vol≤1)=value、混乱(vol==2)=PEAD**」を一本に束ねた。
- **結果（月次・scope K=4）**：`switch(value@vol<=1, pead@vol==2)` が **DSR 0.99（PASS）・SR +0.97・
  IS +0.97 / OOS +0.94・前/後2020 +0.35/+1.20・maxDD −8.3%・被覆≈100%**。各脚も独立に強い：
  `value|vol<=1`（DSR0.94・OOS+0.54）、`pead_lt|vol==2`（DSR0.93・**OOS+0.96**）。**switch は gate を全軸で
  上回る**（SR/DSR/OOS/被覆）。value が現金待機する高ボラ月をちょうど PEAD が埋める＝**相補**。
- **副産物の発見**：PEAD の「OOS脆弱」(§7) は**レジーム由来**だった。高ボラ月に限定すると OOS +0.96＝混乱期
  （不確実性下で予想改訂の情報価値が増す）に効く。全局面で建てるから OOS で崩れていた。
- **節度（最重要）**：① **scope 局所 DSR（K=4）**＝全 687 試行のグローバル基準では更に厳しい（§6.4 注）。
  ② **割当（PEAD→高ボラ）は §6.9 の全期間 breakdown 由来＝in-sample 設計**で OOS 期間も breakdown に含む→
  ここでの OOS+0.94 は**設計に対する完全な holdout ではない**。真の検証は**将来データ（2026-05 以降）か、各時点で
  過去のみから割当を学習する walk-forward / nested CV**。③ だが各脚が独立に OOS 陽性・経済的物語（value↔平常/
  PEAD↔混乱）が整合＝過学習の単純産物とは考えにくい。→ **最有力候補**として walk-forward 検証と Phase 2 紙トレへ。
- **実装**：`research.RegimeSwitch`（mapping={label:Strategy}・PIT 切替）＋ `examples/research_value_pead_regime.py`
  （scope `value_pead_switch`）。ルーティング/先読み不変は `tests/test_regime.py`。

### 6.11 walk-forward 適応切替 — 静的 switch の in-sample 懸念を解消【柱C × レジーム・検証の検証】
§6.10 の唯一の弱点（割当を全期間 breakdown から固定＝in-sample 設計）を潰すため、
`walk_forward_regime_assignment` で**各時点で過去(s<t)のみから**レジーム別最良 sleeve を学習する適応切替を構築。
- **手法（PIT）**：各 t で regime[t]=r とし、s<t かつ regime[s]==r の各 sleeve（value/PEAD）の過去平均
  リターンを比較し最大かつ正を割当（同一レジーム過去 min_obs=6 未満/全非正は現金）、warmup=24月で初期推定。
  return[s] は s+1 で既知＝s<t のみ参照で先読み無＝**真の walk-forward 追跡**。
- **結果（warmup 後 2018-07〜・scope K=4）**：`wf_switch`（過去学習）**DSR 0.97・SR +0.91・OOS +0.92** ＝
  静的 `switch`（DSR0.99・OOS+0.92・in-sample 割当）と**同等**。差（SR 1.04→0.91）は「未来を覗かない代償」で
  小さく、**OOS は同一(+0.92)**。
- **核心**：walk-forward 割当は §6.10 の静的割当（value@平常/PEAD@混乱）を**独立に 86% 再発見**（現金14%）＝
  パターンは**安定で過去から学習可能**＝静的 switch の好成績は **in-sample 設計の産物ではない**。§6.10 最大の
  caveat を実質解消。
- **残る節度**：① **scope 局所 DSR（K=4）**＝全 691 試行のグローバル基準は更に厳しい。② walk-forward 初期
  （〜2020・warmup 直後）は同一レジーム履歴が薄く脆弱（pre-2020 SR −1.08）＝実運用の cold-start に相当。履歴
  蓄積後（post-2020 +1.13・OOS +0.92）は強い。③ sleeve（value/PEAD 因子）と regime（vol 三分位）定義は固定。
  ④ **真の最終検証は将来データ（2026-05 以降）**。
- **頑健性スイープ（vol窓 × コスト・throwaway・全構成の分布で評価）**：vol窓∈{20,40,60,90,120}×コスト∈
  {10,15,25,40}bps の**全20構成で static/wf とも OOS-SR が全て正**（wf 最悪 +0.04＠vol20/40bps、基準 vol60/15bps
  で +0.92）。**40bps（月次では悲観的）でも崩れない**＝結論は特定 vol窓/コストに依存しない。ただし基準 vol=60 は
  窓レンジ内で**最良寄り**（mild favorable・vol40 は +0.82 と近いが極端窓 20/120 は +0.44〜0.50 に低下）＝無敵では
  ない。頑健性≠最適化ゆえ**最良セルを選ばず分布で報告**（exploratory・throwaway・§6.6 と同規律、永続 K は不変）。
- **総括**：value↔PEAD のボラ条件付き切替は (i) 経済的物語が明確 (ii) 各脚が独立に OOS 陽性 (iii) 割当が過去から
  学習可能（walk-forward で再現）(iv) vol窓/コストに頑健＝**当プログラム最有力候補**。次は Phase 2 紙トレ（小口・実約定）で前進。
- **実装**：`research.walk_forward_regime_assignment`（純関数・PIT・テスト済）＋ 同 example（scope
  `value_pead_wfswitch`・静的割当との一致率も出力）。`§6.4` の value 行系は本 §6.9-6.11 で「レジーム条件付き」
  へ発展＝**唯一の耐久候補 value をレジームで増幅**できたのが一連の収穫。

### 6.12 執行現実性チェック — 値幅制限・貸株コスト・ケリー【Phase 2 前のギャップ解消】
外部フレームワーク（LdP×Chan 日本株統合）との照合評価で特定した3ギャップ（①値幅制限の
執行不能処理 ②ショート貸株コスト・貸借可否 ③ケリー基準）を実装し、**最有力候補が執行現実性に
頑健かを測定**した。規律は §6.6 の変種探索と同じ＝**戦略・パラメータは §6.9-6.11 から一切変更せず**、
エンジンの現実性のみ切替（新たな選択なし・K 不変・永続レジストリ不使用の throwaway 測定）。
- **実装**：`equities/frictions.py`（`limit_lock_flags`＝ストップ高/安の引け張り付き判定、
  `shortable_mask`＝貸借銘柄の PIT マスク）＋ `engine.py` の no_buy/no_sell（執行不能＝前回
  ウェイト・キャリー）/ short_borrow_bps（短グロスへ年率賦課）＋ `portfolio/kelly.py`
  （フラクショナル・ケリー）。`judge_grid` にパススルー。スクリプト
  `examples/research_value_pead_realism.py`。設計原則 DP15/DP16 として docs/02 に追加。
- **結果（月次119回・2016-07〜2026-05・シナリオA=従来仮定が §6.10-6.11 を正確に再現）**：

| シナリオ | switch SR(全/OOS) | wf_switch SR(全/OOS) | 備考 |
|---|---|---|---|
| A: 従来仮定(15bps) | +0.97 / +0.94 | +0.91 / +0.92 | §6.10-6.11 の再現＝測定の土台 |
| B: +値幅制限 | +0.97 / +0.94 | +0.90 / +0.92 | 執行不能 4-5件/119月＝**実質無影響** |
| C: +貸株115bp（制度実勢） | +0.87 / +0.84 | +0.80 / +0.82 | 約 −0.10 |
| D: +貸株200bp（逆日歩込み保守） | +0.81 / +0.77 | +0.75 / +0.75 | 約 −0.16 |
| E: +貸株400bp（ストレス） | +0.64 / +0.61 | +0.60 / +0.58 | **全セルで OOS 正＝頑健** |

- **値幅制限はこの戦略型では効かない**：月末リバランス×流動性上位300では引け張り付きに
  当たる注文が 119ヶ月で 3-8 件（PEAD 脚が最多の 8 件＝決算系の想定どおり）。ΔSR≤0.03。
  「罠に近づかない設計」が定量確認された一方、**日次イベント系を裁く際は必須の控除**として
  エンジンに常備された。
- **貸株コストは効く（が switch は耐える）**：value 単独は短グロス≈0.96 のためほぼ全額が
  SR を直撃し（115bp で +0.25→+0.16、400bp で負へ）、**ショートを実名で張る戦略の貸株コスト
  は無視できない**ことを確認。switch/wf は高ボラ月に PEAD（市場ヘッジ・ショート約0.8）へ
  退避する構造のため劣化が緩やか。**ストレス400bpでも OOS +0.58〜0.61 で正**。
- **貸借カバレッジ**：value ショート脚の想定元本の **平均95.4%／最低84.2%（2023-07）** が
  制度信用で売建可能（IssType==2・PIT）。残り≈5%は一般信用等の高コスト枠＝シナリオ D の
  バッファが概ね吸収する規模。
- **ケリー（シナリオC基準・DP16）**：switch のハーフケリー f≈4.3、クォーター f≈2.1
  （μ≈+9.0%、σ≈10.2% 年率）。＝**Phase 2 を等倍（レバ1.0）で始める計画はケリー上限の
  1/4 未満**であり、過大賭けの構造的余地はない。実運用では実測ネットで月次再推定し、
  ハード限度（最大DD・グロス上限）と併用する。
- **総括**：3ギャップはコード（エンジン控除・PIT マスク・レバ導出器）として常設され、
  最有力候補（value↔PEAD 切替）の結論は**執行現実性を入れても維持**。Phase 2 の損益計画は
  シナリオ C〜D（SR +0.75〜0.87 帯）を基準とするのが誠実。

---

## 7. 最有力候補 — value ＋ ロングティルト PEAD

### 7.1 value（割安, book-to-market）— 耐久的なコア
- **生存者バイアス補正後も生存**し、**保留OOS(2024-)でも持続**（OOS SR +0.49 ≥ IS +0.32）、
  **PSR(真SR>0)=0.87**。＝全プログラムで最も信頼できる単独信号。
- だが **年率 Sharpe ~0.36 は低く、minTRL≈241か月（≈20年）**＝**単独では認定（DSR≥0.95）に
  必要な履歴が不足**。10年では「本物らしいが未証明」。

### 7.2 PEAD（会社予想の改訂）と OOS 失敗の解明
- value と PEAD は**平均クロスセクション相関 −0.08（ほぼ独立）**。等加重合成（旧 LS合成）は
  全期間 DSR 0.87・最大DD−27.4% と単独を上回ったが、**保留OOSで +0.06 に失速**。
- **診断（`research_pead_oos_diagnose.py`）**：
  - ① IC が **IS +0.010 → OOS −0.035** と反転（予測力が失われた）。
  - ② **脚分解が決定的**：上方修正の**ロング脚はOOSも堅調（超過 +2.74%→+3.57%）**、
    下方修正の**ショート脚が崩壊（−2.50%→+3.00%＝もはや売れない）**。
  - ③ value との相関が **−0.06→−0.12** と対立を強めた。
- **機構**：下方修正銘柄＝割安・ディストレス＝**value がロングする側**。2024-26 のバリュー
  復権レジームでこれらが反発し、**PEAD のショート（下方修正売り）が value のロング（割安買い）
  と正面衝突して負けた**。

### 7.3 修正：ロングティルト化（診断駆動）
- PEAD のエッジは**ロング側（上方修正）にあり堅牢**、ショート側が脆く value と対立する、
  という診断に基づき、**PEAD をロングオンリー（上位ロング × ユニバース等加重ショート＝市場
  ヘッジ・下位を名指しで売らない）**に変更し、`CompositeStrategy` で value(LS) と 50/50 合成。

### 7.4 最終結果と留保
| | DSR | OOS SR | 最大DD | 容量 |
|---|--:|--:|--:|--:|
| **value + ロングティルトPEAD** | **0.91** | **+0.62** | **−19.2%** | ¥6億 |
| value 単独 | 0.82 | +0.49 | −43.6% | ¥6,574万 |
| 旧 value+PEAD(LS) | 0.87 | +0.06 | −27.4% | ¥7,033万 |

- **OOS が IS より強い（+0.43→+0.62）**＝旧合成の失速を完全に解消。診断どおり、value と
  衝突する「下方修正ショート」を外しただけ。**DSR 0.91（プログラム最良）・最大DD −19.2%
  （value の半分以下）・容量約¥6億**。

**正直な留保（重要）**：
1. **この修正は OOS を見て設計した（post-hoc）**。よって OOS +0.62 は厳密には“真のOOS”では
   ない。**ただし経済的合理性は健全**（割安・ディストレス銘柄をショートで叩くと value と構造的
   に対立する、という機構の修正＝カーブフィットではない）。
2. **DSR 0.91 はこの scope 局所（K=3）**。全プログラムの試行でグローバルにデフレートすれば
   下がる。**なお 0.95 未達。**
3. **真の検証は“新データ＝フォワード（実運用＋監視）”でしかできない。**

---

## 8. 方法論が示したこと（教訓）

- **偽陽性は2つの源から来る**：先読み（リーク）と選択バイアス（提出までの試行数）。本システム
  は前者を **PIT 構造＋生存者バイアス除去**で、後者を **永続デフレートDSR**で封じた。
- **“立派に見える偽物”は本当に出る**：size/short_to_long の DSR0.82/0.68、gap の PSR0.98。
  いずれも厳密な方法論が偽物と見抜いた。**素朴なバックテストは危険**。
- **合成は「逆位相だと相殺、低相関＋同符号だと分散効果」**：value×short_to_long は逆位相で
  失敗、value×PEAD(ロングティルト)は低相関で成功。**機械的な因子合成は失敗（通算複数回）**、
  効くのは**経済的に筋の通った合成**だけ。
- **失敗の“理由”まで見えると修正できる**：PEADのOOS失敗を脚分解で「ショート脚が value と
  衝突」と特定し、経済的に修正して最良候補に。判定器の診断（IC・脚分解・相関・容量・回転）が
  鍵だった。

---

## 9. 正直な限界

- **単一の ~0.36 Sharpe 因子は、現有10年では認定不能**（minTRL≈20年）。これは日本株ファクター
  投資の現実であり、フレームワークの欠陥ではない。
- **最有力候補の OOS は post-hoc**（§7.4）。真の確証はフォワードが必要。
- **データの制約**：信用残高は全銘柄“週次”止まり、決算発表予定は履歴なし、売買内訳・先物・
  詳細財務は Premium、ニュースは外部。より長い履歴（Premium 20年）や日次需給で再検証の余地。
- **研究の継続は p-hacking 化リスクと隣接**。永続デフレートDSRが新試行のたびに基準を上げて
  自制を強制するが、最終的には**フォワードでの確認に勝る検証は無い**。

---

## 10. Phase 2（実運用）への展望

- **実運用候補**：`value ＋ ロングティルトPEAD`（PITユニバース上位300・セクター中立・月次・
  ロングショート＋ロングティルト）。容量約¥6億＝小口運用には十分。
- **段階的ロールアウト（設計 D2/D3）**：**小口（〜¥50万）＋ライブ監視**で track record を
  積み増し、DSR が基準に近づくかを**実データ（フォワード）で確認**。事前登録＋月次再校正。
- **執行現実性は織込み済み（§6.12）**：値幅制限・貸株コスト込みでも候補は OOS 正（損益計画は
  シナリオ C〜D 基準）。レバレッジは**フラクショナル・ケリー（DP16）**で導出（等倍開始＝
  クォーターケリーの半分未満で保守的）。貸借カバレッジ≈95% ＝ショート脚は概ね制度信用で執行可。
- **設計の更新点（D1）**：研究は暗号資産→**日本株**に着地。執行は **日本株ブローカー**が必要
  （元設計の bitbank＝暗号・公開データ専用とは別系統）。**未決だった Phase3 ブローカー
  （IBKR か 国内ネット証券）の決定**が Phase 2 の最初のゲート。
- **流用**：検証ファクトリの `Strategy`／エンジン／永続レジストリを、そのまま **「紙トレ→小口
  実弾」の判定ゲート**として使える（同じ戦略コードでシグナル生成→発注→約定/コスト記録→
  判定の更新）。

---

## 11. 付録 — モジュール地図・再現コマンド

### 主要モジュール
- 検証ファクトリ: `invest_system/research/{data_view, strategy, engine, judge, report_html}.py`
- 検証メソッド: `invest_system/validation/{dsr, cpcv, purge_embargo, registry, cv_score}.py`
- 執行フリクション（DP15）: `invest_system/equities/frictions.py`（値幅制限の張り付き判定・
  貸借銘柄 PIT マスク）→ `engine.py` no_buy/no_sell/short_borrow_bps、検証 §6.12
- レバレッジ導出（DP16）: `invest_system/portfolio/kelly.py`（フラクショナル・ケリー）
- 因子/イベント: `invest_system/equities/{universe, panel, fundamentals, factors, events, margin, flows, stability}.py`
  - 全ユニバース財務: `fundamentals.load_fundamentals` / `fundamentals_panel`（全件ミラーから先読みなし as-of 組立）
  - 追加ファクター: `factors.low_volatility`（低ボラ）・`factors` 内 `accruals`（利益の質）
- データ: `invest_system/data/{catalog, updater}.py`, `invest_system/data/sources/{jquants, bitbank}.py`
  - 一括取得: `examples/download_jquants.py`（手元ターミナルで直接実行＝Claude非経由、進捗表示・中断再開）
- **柱D 時系列・統計的裁定／Chan 補完（実装済み）**: `invest_system/timeseries/{mean_reversion, cointegration, kalman}.py`
  ＋ `research/strategies_meanrev.py` の `CointegratedPairs`/`JohansenBasket`/`LinearMeanReversion`
  ＋ `equities/panel.load_daily_panel`（日足 wide パネル）＋ `examples/research_meanrev_pairs.py`。
  原理 `docs/01`§11・設計 `docs/02`§5.2/DP12-14・検証 §6.6。

### 代表的な実行例（Windows / Linux 併記は README 参照）
```
# 全件ミラーの一括取得（手元ターミナルで直接実行＝Claude非経由・進捗/再開可）
.venv\Scripts\python.exe examples\download_jquants.py --list   # 取得状況の一覧
.venv\Scripts\python.exe examples\download_jquants.py --all    # 欠損を全件バックフィル
# データ最新化（差分のみ）
.venv\Scripts\python.exe examples\update_data.py
# 最有力候補の検証（value + ロングティルトPEAD）
.venv\Scripts\python.exe examples\research_value_pead_longtilt.py
# 多ファクター breadth の全ユニバース検証（全件ミラーDL後）
.venv\Scripts\python.exe examples\research_breadth_factors.py
# PEAD OOS失敗の診断
.venv\Scripts\python.exe examples\research_pead_oos_diagnose.py
# 執行現実性チェック（値幅制限・貸株コスト・ケリー＝§6.12）
.venv\Scripts\python.exe examples\research_value_pead_realism.py
# 試行台帳の俯瞰 / ダッシュボード生成
.venv\Scripts\python.exe examples\registry_status.py
.venv\Scripts\python.exe examples\build_report_index.py   # → data/reports/index.html
```

### 判定レポート（HTML）
`data/reports/<scope>.html`（各戦略のエクイティ曲線・指標・PASS/FAIL）と
`data/reports/index.html`（全 scope のダッシュボード）。

### 環境
Python 3.13 / numpy・pandas・scipy・scikit-learn・statsmodels・pyarrow。テスト: `pytest -q`（248件）。
APIキーは `.env`（gitignore）。Standard プランは `J_QUANTS_MIN_INTERVAL=0.7` 推奨。

---

*本レポートは研究フェーズの確定版である。次段は Phase 2（実運用配管）であり、その最初の意思決定は
「日本株ブローカーの選定」、最終的な戦略確証は「フォワードでの小口運用＋監視」となる。*
