# 作業依頼：EDINET三表のPIT統合とキャッシュフロー/バランスシート系特徴量の追加（Phase 2）

## 0. このプロンプトの位置づけ

`claude_local_sessions`（strategy_factory）を GKX（Gu, Kelly & Xiu 2020）型の機械学習×クロスセクション予測へ育てる計画の **Phase 2**。Phase 1（J-Quants Standard データだけで作れる価格系・基本財務系の特徴量ライブラリ拡充）に続き、本タスクは **EDINET の財務三表明細を Point-in-Time でパネルに統合**し、Standard データでは作れなかったキャッシュフロー系・バランスシート明細系の特徴量（FCF利回り・CF/P・粗利益性・ROIC・資産成長・アクルーアル・EBITDAマージン・レバレッジ・純株式発行・R&D集約度・セグメント系）を解禁することを目的とする。

これらは将来の ML 推定器（Phase 4）の入力特徴量であり、**戦略探索ではない**。Phase 1 と同じく **K（永続レジストリの試行数）は消費せず、判定もせず、診断は throwaway** で行う。

---

## 1. データ取得アーキテクチャ（確定方針）

**主＝金融庁 公式 EDINET API v2（無料）／補助＝edinetdb.jp API（Free または Light）** の二段構え。

### 1-A. 主ソース：公式 EDINET API v2
- 全期間・全銘柄の三表バックフィルは **公式 API を一次ソース**とする（無料・商用可）。
- フロー：
  1. **書類一覧API**（`GET https://api.edinet-fsa.go.jp/api/v2/documents.json?date=YYYY-MM-DD&type=2&Subscription-Key=KEY`）を営業日ごとに叩き、メタデータ（docID, docTypeCode, edinetCode, secCode, 提出日時, filerName, 期間 等）を **by-date ミラー**として追記保存。
  2. 有価証券報告書（docTypeCode=120。四半期は 140・半期 160。**コードは仕様書で確認**）に絞り、(edinetCode, 会計年度) で名寄せ。**訂正報告書は別管理**（PITでは原報告を原提出日で採用し、訂正は訂正提出日以降にのみ反映）。
  3. **書類取得API**（`GET .../api/v2/documents/{docID}?type=5&...`）で **XBRL→CSV変換済みデータ**（ZIP内CSV・UTF-16・タブ区切り・要素ID/コンテキストID/値/単位/小数桁）を取得。生XBRLの自前パースは不要だが、**要素ID→正準フィールドのマッピングは自前で作る**。
- **レート制限の扱い**：公式の数値上限は非公表。実態は **1リクエストあたり3〜5秒間隔**を空けないと接続が切られる「間隔絞り」型。`EDINET_MIN_INTERVAL`（既定3〜5秒）＋指数バックオフ＋**中断再開可能（resumable）・進捗表示**で実装。日次クォータは無いので、間隔さえ守れば数日の連続ジョブで完走できる。
- **ローリング10年窓に注意**：公式APIの保持は「直近約10年」。2026年時点で窓の末端は約2016年＝**狙う期間の最古年（2016年、およびFY2015を2016年提出した有報）は窓の縁にあり、これから取得不能になっていく**。→ **最古年から優先的にバックフィル**する（古い年が落ちる前に確保）。
- **APIキーは無料登録**（v2で必須）。`.env` に `EDINET_API_KEY`。サーバサイドのみ（ブラウザ/JS不可）。
- **大規模バックフィルは手元ターミナルで直接実行**（Claude 非経由）。`examples/download_jquants.py` と同じ流儀で `examples/download_edinet.py`（進捗・中断再開・追記専用）を用意する。

### 1-B. 補助ソース：edinetdb.jp API（Free / Light＝100リクエスト/日）
- **バルクには使わない**（100/日上限）。以下の限定用途のみ：
  1. **公式APIでパースした数値の独立クロスチェック**（要素マッピングのバグ検出）。
  2. **公式の10年窓から既に落ちた古い年の穴埋め**（edinetdb は過去取得分を蓄積しており、公式現行窓より古い年〔例：FY2011-2015〕を持つ）。
  3. セグメント等、自前パースが重い箇所のスポット取得（`get_segments` 等）。
- Claude Code 側は **edinetdb の REST API を直接**叩く（`.env` の `EDINETDB_API_KEY`、Free/Light キー）。**1日100回の上限を超えない**ようスロットルし、超過時は翌日に回す設計。チャット側MCPコネクタには依存しない。
- **スクレイピングは禁止**。Monex/銘柄スカウター・Buffett Code 等のレンダリング済みページのスクレイプは規約・リポジトリ規律（HANDOFF）に反するため使わない。アナリスト・コンセンサス系は Phase 3（ifis/QUICK/I/B/E/S 等のPIT履歴つき正規ソースをライセンス）に分離。

---

## 2. 最重要の規律（破ると本プロジェクトの価値が壊れる）

1. **PIT・提出日アンカー厳守**。各意思決定時点 t では「**提出日（書類一覧メタデータの提出日時）≤ t − lag**」の財務のみ採用。決算期末や報告義務発生日は使わない。HANDOFF の「提出日アンカー厳守」と同一規律。**決算期（FYE）は銘柄ごとに不均一**（例：アサヒは12月決算・3月下旬提出）なので、as-of 整合は **FYE 非依存**で組む。
2. **会計基準の不均一を明示処理**。JGAAP / IFRS / US-GAAP で要素ID（タクソノミ prefix：`jppfs_cor`＝JGAAP財務諸表、`jpigp_cor`＝IFRS、`jpcrp_cor`＝共通開示 等）が異なる。IFRS商社・US-GAAP企業は**営業利益がnull**、一部IFRS金融は**売上がnull**になる。**フィールド別・基準別の null 処理**を必須化。
3. **会計基準移行の断絶を検出してNaN化**。JGAAP→IFRS移行年は遡及再表示で BS 構造が不連続（アサヒは FY2015→FY2016 で総資産・流動資産が段差）。**移行年をまたぐ成長率・資産成長・アクルーアルは壊れるので計算せず NaN**にする。基準タグの変化と総資産等の不連続で移行年を検出。
4. **加工済み比率を取り込まない＝生ライン項目から自前計算**。ROE/ROIC/PER 等はベンダー間で定義が食い違う（EDINET 7.5% vs Monex 7.49% vs Buffett 6.8% 等を実確認済み）。**各比率は生のライン項目から一度だけ定義して自前計算**する。edinetdb 等が返す既算出比率はクロスチェック用にのみ参照。
5. **K不変・反p-hacking**。本タスクは**データ基盤の整備**であり戦略探索ではない。**`judge_grid` で登録・判定しない／永続レジストリ（K）は不変**。特徴量の予測力を見たい場合は IC・被覆率の**診断（throwaway・レジストリ不使用）**に留める。新規DSR判定は Phase 4 で一度だけ。
6. **生存者バイアス除去**。by-date ミラーは、期間内に後で上場廃止した企業の提出書類も自然に捕捉する。廃止銘柄を落とさず取り込む（窓から落ちた古い年は 1-B で補完）。評価は PIT ユニバース上で。
7. **PIT先読み不変テスト必須**。各特徴量に「未来を改変しても ≤t の値が不変」を assert するテストを追加。

---

## 3. 構築する特徴量（§4-B）と必要なライン項目

公式API type=5 の CSV から下記ライン項目を抽出・正準化し、`fundamentals.py` の as-of パネルに統合したうえで特徴量化する。各特徴量は raw／クロスセクション [-1,1] ランク／セクター中立版を Phase 1 と同じ流儀で用意。

| 特徴量 | 必要ライン項目 |
|---|---|
| FCF利回り | 営業CF・投資CF（または設備投資）、時価総額。**FCF=営業CF+投資CF か 営業CF−capex のどちらかに定義固定** |
| CF/P | 営業CF |
| 粗利益性 | 売上総利益、総資産 |
| EBITDAマージン | 営業利益、減価償却費、売上 |
| ROIC | 営業利益、実効税率（または税引前利益と税金）、有利子負債、自己資本 |
| 資産成長（investment軸） | 総資産の時系列（前年比） |
| アクルーアル | （簡易）純利益・営業CF・総資産／（精緻）売掛金・棚卸資産・買掛金・流動資産・流動負債・減価償却 |
| レバレッジ / D/E | 有利子負債、総負債、自己資本 |
| 純株式発行 | 発行済株式数の前年比（**分割調整必須**） |
| R&D集約度 | 研究開発費、売上 |
| セグメント系 | セグメント別 売上/利益/資産（分散・地域エクスポージャ・セグメント利益率のばらつき。注記から、または補助で edinetdb `get_segments`） |

**FCFの外れ値処理**：FCF は M&A 年に大きく振れる（アサヒ FY2017/2020 が買収で大幅マイナス）。FCF利回りは**複数年平均・ウィンソライズ等の正規化**を入れ、M&A歪み年をフラグする。

---

## 4. 要素ID→フィールドのマッピングと検証

1. **マッピング辞書**を `data/sources/edinet_taxonomy.py`（仮）に作る。キー＝(タクソノミ基準, 正準フィールド)、値＝候補要素IDの優先リスト（年度・基準でのゆれを吸収）。当期・連結を優先、コンテキストID（連結/個別・期間/時点）で正しく選択。
2. **既存資産でのクロス検証（推奨・強力）**：売上・営業利益・純利益・総資産・純資産など **J-Quants fins_summary と重複するフィールドは、EDINETパース値と突合**して許容差内かをテスト（要素マッピングのバグ検出）。さらに 1-B の edinetdb 値とも一部突合。突合基準は事前固定（例：相対差>1% で WARN）。リポジトリの §6.20（cross-engine 突合）と同じ流儀。
3. **コード・クロスウォーク**：EDINETコード（E始まり）↔ secCode（5桁）↔ J-Quantsコード（4桁）↔ `equities_master` の対応表を作り、価格パネルと結合できるようにする。

---

## 5. 成果物（Deliverables）

1. `data/sources/edinet.py`：公式 EDINET API v2 クライアント。書類一覧の by-date ミラー（追記専用・seqや日付で冪等・再開可）、type=5 取得・解凍・CSV読込、`EDINET_MIN_INTERVAL` スロットル＋バックオフ。HANDOFF のトラックA記載の `edinet.py` 構想を本実装する。
2. `examples/download_edinet.py`：**手元ターミナルで直接実行**する大規模バックフィル（進捗・中断再開・最古年優先オプション）。Claude 非経由。
3. `data/sources/edinet_taxonomy.py`：要素ID→正準フィールドのマッピング（基準別）＋コンテキスト選択ロジック。
4. `data/sources/edinetdb.py`：補助用 edinetdb REST クライアント（Free/Light・**100回/日スロットル**・クロスチェック/古年穴埋め/セグメント用）。
5. `equities/fundamentals.py` 拡張：EDINET由来の三表明細を **提出日アンカーの as-of パネル**として `fundamentals_panel()` に統合（fins_summary と同じ PIT 機構・FYE非依存）。基準タグ・移行断絶フラグを列として保持。
6. `equities/factors.py`（または `characteristics.py`）に §3 の特徴量を純関数で追加（PIT・完全窓要求・基準別null・移行断絶NaN・FCF正規化込み）。
7. **テスト**：①先読み不変、②被覆率（年別・基準別）、③J-Quants/edinetdb とのクロス突合（許容差）、④移行断絶のNaN化、⑤コード・クロスウォークの整合。現行277件に追加し全パス。
8. **ドキュメント**：`docs/`（例 `docs/14-edinet-fundamentals.md`）に、取得アーキテクチャ・要素IDマッピング表（基準別）・**年別/基準別の被覆率**・既知の限界（IFRS null・移行断絶・ローリング窓の最古年・FCFのM&A歪み・訂正報告の扱い）を記載。docs/03 §5 のデータ資産節からリンク。
9. **診断レポート（throwaway・K不変）**：新特徴量の月次クロスセクションIC・被覆率・分布、および official vs edinetdb vs J-Quants の突合サマリを出力（`examples/diag_edinet_fundamentals.py`。判定ではない旨を明記）。
10. データは `data/edinet/`（**gitignore**）。コミット可はコード・マッピング表・コード・クロスウォークのみ（EDINET取得物・市場データはコミットしない）。

---

## 6. 受け入れ基準（Definition of Done）

- 公式API由来の三表明細が **提出日アンカーの as-of パネル**として fundamentals に統合され、PITユニバース上で月末断面に抽出できる。
- §3 の特徴量が PIT・基準別null・移行断絶NaN・FCF正規化込みで生成でき、ランク/セクター中立版もある。
- J-Quants fins_summary（重複フィールド）との突合が許容差内で、要素マッピングの妥当性がテストで固定されている。
- 先読み不変・被覆率・突合・移行断絶のテストが全て green（既存277件も不変）。
- `examples/download_edinet.py` が手元ターミナルで再開可能に動き、最古年優先でバックフィルできる。
- 補助 edinetdb クライアントが **100回/日上限を超えない**ことがテスト/設計で担保されている。
- docs に要素マッピング・年別/基準別被覆率・既知の限界が揃う。
- **永続レジストリ（K）がタスク前後で不変**であることを `examples/registry_status.py` で確認できる。

---

## 7. 運用上の留意点

- 純Python・OS非依存・相対パス/`pathlib`・改行LF・float32・pyarrow を維持。
- 既存インフラ（`fundamentals_panel`/`AsOfView`/PITユニバース/`sector_neutralize`/feature_store）を再利用し重複実装を避ける。
- **規約遵守**：公式EDINETは自動取得可だが「節度ある利用」＝3〜5秒間隔・バックオフ厳守。edinetdb は Free/Light の100/日と規約を厳守。**Monex/Buffett等のスクレイプはしない**。
- 比率の定義・窓長などは §3 の標準で**事前固定**し、「良くなるまで動かす」探索はしない（p-hacking回避）。
- 不明点（docTypeコード・要素IDのゆれ・連結/個別コンテキストの選択・訂正報告の優先規則・コードクロスウォークの欠損）は、**推測で近似せず仕様書/実データのスキーマを確認**し、判断が要る箇所は質問する。
- アナリスト・コンセンサス／予想・相対バリュエーションは**本タスクの対象外**（Phase 3・正規PITソースをライセンス）。Premium履歴延伸は Phase 5。ML推定器は Phase 4。

---

### 一言サマリ（エージェントへ）
> GKX 化の Phase 2 として、**金融庁 公式 EDINET API v2（type=5 CSV）を一次ソースに、全期間・全銘柄の財務三表を「提出日アンカーの PIT パネル」として統合**し、FCF利回り・ROIC・資産成長・アクルーアル・レバレッジ等の特徴量を解禁してほしい。**edinetdb API は Free/Light（100/日）で、クロスチェック・公式10年窓から落ちた古年の穴埋め・セグメント補助にのみ使用**。要点：①提出日アンカー＋FYE非依存のPIT、②IFRS/JGAAP別のnullと移行断絶のNaN化、③加工済み比率は使わず生ライン項目から自前計算（J-Quantsと突合検証）、④ローリング10年窓のため最古年（2016）を優先バックフィル、⑤大規模取得は手元ターミナルでresumable実行、⑥これはデータ基盤なのでK不変・判定なし・診断はthrowaway、⑦スクレイプ禁止・アナリスト系はPhase 3。