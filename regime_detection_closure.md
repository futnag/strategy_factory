# 価格・出来高レジーム検知プロジェクト — 打ち切り記録（closure）

> 事前登録された中核仮説を機械適用し、救命なしで打ち切る。撤退として一度完結させ、次の問いは
> 区切ってから腰を据えて再設計する。本書はその最終記録。

## 最終結論（一段引いた事実）
**価格・出来高統合のレジーム検知は、日本株単一銘柄・日足・10銘柄・2.3年というこの設定で、エッジを
持つリバーサル一次に対してすら無フィルタを超える正味価値を示さなかった。** 信号は存在するとしても
弱く標本依存で、この解像度では取りに行く価値が立たない。**設計の失敗ではなく、事前登録された問いへの
正確な答え。**

## 判定の経過（すべて事前登録・結果を見る前に基準固定・機械適用）
| 問い | 結果 | 補足 |
|---|---|---|
| 二層 vs 単層（方式A・本物アンカー） | **棄却** | 本丸①② 各 <2/3・DSR 0/10 |
| 単層 vs 無フィルタ（momentum 一次） | **棄却** | どの単層も無フィルタ超えず。但し momentum は単体エッジ無し＝「良い一次の不在」で判定不能寄り |
| 単層 vs 無フィルタ（**reversal 一次・単体エッジ 7/10 有り**） | **棄却** | 単層①② 各 3/10・中央値負・DSR 0/10。**エッジある一次でも無価値＝クリーンな negative** |

3度の指標反転（gap-CV → filtered出力 → 窓外保留LL）を経ても、各段階で基準を**事前固定**し結果を機械適用。
最終的に事前登録 #5「良い一次でも効かなければこの方向への投資を止める（救命ループに入らない）」に該当。

## PEAD は分離保全：**未判定（疎）**——「エッジなし」ではない
本ループの **FY 年1回 sue_recent 設計**では、10銘柄中5銘柄が正サプライズ・イベント皆無で**判定適格な
標本が立たなかった**。PEAD のエッジ有無は**未判定**。**打ち切り結論は「reversal 一次でレジーム無価値」に
対するものであり、「PEAD にエッジなし」を含まない**（将来「PEAD は検証済みで効かない」と誤参照しない
こと）。四半期サプライズ設計は**将来の独立した別問題**として残す。

## 保全された汎用資産（レジームの成否と独立に価値・次のどの方向でも再利用可）
`invest_system/research/daily_regime/`（24 テスト全緑）：
- **契約ベースのリーク防止**：WeeklyDirectionalAnchor / TierMembership・available_from ラグ・AsOf 橋。
- **恒久 leak_tests**（毎回実行・意図的リークで赤を確認済）：filtered/smoothed 分離・ブロック/位相
  プラセボ・ティア遡及再付番・held-out LL の PIT・**ファンダ期末日アライン**。
- 検知器（Student-t HMM・sticky/min-dwell・状態数 config）・**流動性ティア partial pooling**（平滑中央値
  ＋スティッキー帰属）・方式A 結合・**レジーム依存コスト**・§6.3 ベースライン完全統制・DSR/CPCV 配線。
- **per-stock fallback・固定λ・サンプリングロック**の規律（優雅な劣化＝効かなくても無フィルタ/B&H へ）。
- ファンダ PIT（DiscDate アンカー・`fundamentals.point_in_time` 再利用）。

## 方法論的成果
薄く標本依存な信号を相手に、**事前登録・救命なし・層別採否・契約ベースの PIT** の規律が「勝たせにいく」
誘惑（指標漁り・閾値最適化・負け銘柄の事後除外・λ調整）を一貫して防いだ。3度の反転と最終棄却を経ても
**負の結論が precise に記録され、無フィルタ/B&H へ優雅に劣化**したこと自体が、本プロジェクトの成果。

## 次（区切ってから・救命ループでない）
打ち切りを撤退として完結。次の問いは**設定そのものを変える方向**でのみ正当：
- **クロスセクション化**（単一銘柄でなく横断 L/S）。
- **真の Dollar Bars**（Tick データ取得後・日足解像度の限界を外す）。
両者とも評価枠組みの再設計／Tick 取得という相当な準備を要し、撤退の末尾に滑り込ませない。腰を据えて再設計。

## 事前登録・記録の所在
- `regime_detection_design_v2.1.md` / `coding_agent_prompt_v2.1.md`（設計）
- `regime_detection_connection_v2.1.md`（既存リポへの接続計画）
- `regime_detection_pooling_preregistration.md`（pooling 採否裁定）
- `regime_detection_core_judgment_preregistration.md`（中核判定・二層棄却・レジーム全面棄却）
- `regime_detection_loop_a_preregistration.md`（一次実体化ループ・reversal 棄却）
- 本書 `regime_detection_closure.md`（打ち切り記録）
