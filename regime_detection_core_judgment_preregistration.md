# 中核判定の事前登録（step 5）— 結果を見る前に固定する

> この設計の存在意義そのものを問う中核判定に入る前に、**指標・対立・勝敗条件・割引・棄却条件**を
> 完全に固定する。step 2→LL 裁定で3度反転したのは、走らせる前に基準を固定しなかったから。
> ここから先は「**事前登録した基準に、結果を機械的に当てるだけ**」。事後の基準変更は禁止。

## 中核の問い
**価格・出来高統合の二層レジームシステム（方式A・本物の週足方向性アンカー＋pooled 日足ボラ流動性）
は、単層（日足のみ／週足のみ）・無フィルタを、取引コスト控除後・前進検証で上回るか？**

---

## 事前登録（5点）

### 1. 主指標を1つに固定
- **主指標＝コスト控除後・前進検証の Sharpe**（年率換算）。**採否はこれだけで決める。**
- 年率リターン・最大DD・Calmar・ターンオーバーは**記述統計として併記するが採否に使わない**。
  「Sharpe は負けたが DD で勝った」式の事後的な指標乗り換えを**禁止**。

### 2. 判定対象の対立を固定（本丸2比較）
- **本丸①：二層 vs 日足のみ**（＝週足層の限界価値）。`ΔSharpe_週足層 = Sharpe(二層) − Sharpe(日足のみ)`。
- **本丸②：二層 vs 週足のみ**（＝日足層の限界価値）。`ΔSharpe_日足層 = Sharpe(二層) − Sharpe(週足のみ)`。
- 参照：二層 vs 無フィルタ（レジーム全体の価値）。**B&H は参照のみ**（トレンド銘柄でフィルタが
  exposure を削れば負けて当然＝主判定に使わない）。

### 3. 単一銘柄では結論を出さない（多銘柄の符号一貫性）
- 1銘柄の大勝/大敗は**標本1**（LL が Δ+0.052 の薄さで反転した教訓）。
- **複数銘柄・複数ティア（最低 T2/T3 から各複数、可能なら流動性ティア横断）**で、本丸①②の
  **ΔSharpe の符号が正である銘柄割合**を数える。
- **採用条件（固定）：本丸①②**の双方が、銘柄パネルの **≥ 2/3 で正**、**かつ** 各 ΔSharpe の
  **中央値が正**。単一銘柄の大勝より、多数銘柄での弱い一貫した正を信頼する。

### 4. 多重検定の割引を事前宣言
- 本物アンカー投入後、状態数(2 vs 3)・結合方式・銘柄を複数試す＝最良構成の素 Sharpe は信用しない。
- **複数銘柄での再現性（上記#3）を一次の割引**とし、加えて可能なら **Deflated Sharpe（既存
  `validation/dsr.py`）／CPCV（`backtest/cpcv_backtest.py`）** で「偶然の勝者」を割り引く。

### 5. 負けた時の解釈を固定（棄却条件）
- 本丸①②が #3 の採用条件を満たさなければ、**機械的に「複雑さは正当化されない＝二層棄却・単層採用」**
  と結論する。「アンカーが粗い」「一次戦略の閾値が」と**後付けで救命しない**。
- 救うなら、それは**事前登録した別の改良仮説として次ループへ**回し、**現判定は棄却で確定**。
- どの単層も無フィルタを超えなければ「レジームはこの戦略系に価値を加えない」と確定（レジーム棄却）。
- **中核仮説が棄却される可能性を、今、明示的に受け入れる。**

---

## 釘（逸脱しない）
1. **λ（注入強度・pooling 強度）は固定**。中核判定で λ を動かして二層を勝たせにいくのは全工程の
   規律違反。
2. **状態数 2→3 は事前登録した分離仮説で判定**＝「**高ボラ×流動 vs 高ボラ×非流動の分離回復**」が
   必要か、で選ぶ。**中核 Sharpe を上げるために 3 状態を選ばない。**
3. **本物アンカーの PIT/ラグを確認**＝ sector_regime の walk-forward **filtered** を available_from
   ラグ付きで asof 橋に通す（スタブと同形だが本物は実データでリーク面が増える）。**意図的リークで
   赤を1本**通してから中核判定に入る。

---

## 手順（固定）
1. 本 doc を確定（済）。
2. 本物方向性HMM 置換（filtered・モメンタム整列）＋方式A 注入。**PIT 確認（意図的リーク→赤）**。
3. PEAD エンドツーエンド backtest を**並行**実装（§2 除外がコスト控除後の実損益で機能するかを、
   価値戦略と**同じ判定窓**で読む。判定ループは1本に束ねる）。
4. ベースライン5本＋PEAD を**複数銘柄**で再走。
5. 上記#1〜#5 に**機械的に照らして**中核判定（採用 or 棄却）を確定。

## サンプリング・ロック（結果非依存・多銘柄判定の母集団）

> 「9999除外」の口実で後から負けた業種/銘柄を落とすことを**構造的に禁止**するため、選択ルールを
> **性能と無関係に**宣言し以後固定する。除外の正当化は事前の構造的理由のみ。

- **キャッチオール除外**：S33=9999（「その他」・方向性が構造的に未定義）を除外。**理由は「方向性が
  定義できない」のみ**で、本丸①の符号とは無関係。他のキャッチオール的バケットがあれば同基準で除外。
- **適格業種**：ユニバース内の構成銘柄数 ≥ 20 かつ非キャッチオール。
- **抽出（決定的・性能非依存）**：適格業種を**構成銘柄数の多い順に上位5業種**（性能でなく母集団規模で
  決定）。各業種から **T2 から1銘柄・T3 から1銘柄**（as-of 最終日のティア）を**コード昇順**で機械抽出、
  日次観測 ≥ 412 を満たすもの。→ 約10銘柄。
- **ロック**：得られた銘柄リストを固定し、**全数を判定に含める**。脱落は**客観基準（観測長不足）のみ**、
  **性能基準での脱落は不可**。本丸の符号を見てからの追加/除外は禁止。
- **PEAD は同一リスト・同一窓**で並行（価値戦略と1本のループ）。
- λ・状態数・閾値・refit/窓は**全て固定**（公正化はサンプルの是正のみ・モデル不変）。

## 最初に提示するもの（多銘柄展開の前）
PIT 確認が緑（意図的リークで赤）かつ本 doc 固定後、**1〜2銘柄での中核判定の素の数字＋アンカーの
filtered 系列（スタブからどう変わったか）**を提示する。そこで多銘柄展開に進むか構成を見直すか判断。

---

## 結果（事後記録）— 二層棄却（確定）
多銘柄（10銘柄・5実業種×T2/T3・公正サンプル）で事前登録基準を機械適用：
- **本丸①（週足層）正 6/10(60%)<2/3・中央値+0.003 → NO。**
- **本丸②（日足層）正 4/10(40%)・中央値−0.012 → NO。**
- **DSR(two-layer)>0.95：0/10。**
→ **二層棄却**（複雑さは正当化されない）。事前登録 #5 どおりの機械的判定（救命なし）。
- **PEAD §2除外**：機構は両側ユニットテストで正しいが、コスト後 P&L にほぼ無効（diff≈0 多発＝
  除外が bind する場面〔決算窓×高ストレス・フィルタ抑制の同時発生〕が稀）。除外の**正しさ**は確認済、
  **価値**は粗 PEAD 一次戦略では未実証。

## (b) 単層 vs 無フィルタの事前登録（レジーム全体の採否・(a)前に必ず通す）
- 日足のみ／週足のみ それぞれを **単層 − 無フィルタ** で測る（二層−ではない）。
- **採用：≥2/3 で符号正・中央値正・DSR(単層)>0.95 生存。** 満たす単層があれば「レジーム全体は二層棄却
  だが、その単層は採用」。
- **どれも満たさなければ レジーム検知そのものを棄却**＝無フィルタ(/B&H)が本サンプル・本一次戦略の
  正しい着地（**今、受容**）。
- λ・状態数・閾値固定・既存推定の集計のみ（新規自由度なし）。no-filter は同じ engine/AsOf 経路＝PIT。

### (b) 結果（事後記録）— レジーム検知 全面棄却
- **単層① 日足のみ−無フィルタ**：正 5/10(50%)・中央値 −0.005・DSR>0.95 0/10 → NO。
- **単層② 週足のみ−無フィルタ**：正 4/10(40%)・中央値 −0.001・DSR>0.95 0/10 → NO。
→ どの単層も無フィルタを超えず・DSR 全滅 → **レジーム検知そのものを棄却**。無フィルタ/B&H が本サンプル
（10銘柄・5実業種・2.3年）・本一次戦略（粗モメンタム）の正しい着地。全 Sharpe は per-period ±0.08 内＝
**信号は実質ノイズ水準**。中核仮説は当該条件下で**全面棄却**（事前登録 #5 どおり・救命なし・着手前に受容）。

## 記録すべき限定結論（一段引いた事実）
「価格・出来高統合の二層レジーム検知が、**粗いモメンタム一次戦略・10銘柄・2年強**のサンプルで、単層を
超える証拠は得られなかった」。**設計の死亡宣告でなく、事前登録された中核仮説が当該条件下で棄却された
限定的・正確な事実。** 3度の指標反転＋最終棄却が示すのは、信号が（あるなら）弱く標本依存ということ。
次の投資は「二層をどう勝たせるか」でなく「**一次戦略を実体化し、そもそもレジーム検知に取りに行く価値が
あるかを問い直す**」方向。per-stock fallback・固定λ・契約ベースのリーク防止＝優雅な劣化設計のおかげで、
この棄却の上に次を積んでも手戻りは最小。

## (a) 次ループの事前登録（(b)後に確定・改良仮説は1つだけ）
- **筆頭候補：一次戦略を粗モメンタム→実 value/PEAD シグナル（`fundamental_factors`・`events`）へ差し替え。**
  理由：今回棄却の最大交絡が一次の粗さ（フィルタは良い一次の exposure を賢く削る装置＝一次がランダム
  同然なら価値が出ない。PEAD除外が効かないのも粗 PEAD 一次で除外が bind しないため＝一次の問題）。
  アンカー粒度改良より一次の実体化が先（フィルタの価値は一次の質に上限を規定される）。
- (a)は「二層の再挑戦」でなく**独立した新判定ループ**：差し替えた一次で、まず単層 vs 無フィルタ → 超えたら
  二層 vs 単層、と同じ階段を上り直す（順序を飛ばさない）。改良仮説は**1つだけ**（自由度を増やさない）。
- step 0–1 の PIT/leak/契約・検知器・pooling・結合は不変（触れたら回帰テストで赤を確認）。一次差し替えは
  バックテスト層のみ。
