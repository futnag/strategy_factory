# 日足データを用いた価格・出来高統合型レジーム検知システム 設計書 v2.1

> **v2.1 改訂（本パッチ）— アーキテクチャ確定3点＋精緻化4点＋残り採用2点**
> アーキテクチャを変える3点を先に確定し、識別性・検出力・先読み防止の土台を固める。
>
> - **[アーキ1] 方向性レジームはセクター/指数から取得**（第1部1.3／第5部5.1・5.3）。単一銘柄の週足方向は識別が弱い（日足ドリフト非識別性の軽度版）。方向性アンカーはセクター/指数の週足HMMから取り、日足個別層へ注入する。**週足個別の方向推定はしない。**
> - **[アーキ2] 日足ボラ流動性層は「流動性ティア」で partial pooling 推定**（第5部5.4／第7部／第8部／第10部）。単一銘柄のデータ枯渇に対し、流動性ティア（ADV/Amihud 水準の分位、ポイントインタイム）で括ったプールで放出パラメータを推定し、filtered 推論は銘柄別。**pooling 強度は最適化せず弱め固定**（二重過剰適合の回避）。括りは**セクターでなく流動性**。
> - **[アーキ3] 週足HMMの整列軸をボラ→モメンタム符号に付け替え**（第2部2.5／第5部5.1）。整列軸は層別：**週足＝方向（平均週次リターン／mom 符号）→ bear/neutral/bull**、**日足＝ボラ・流動性（vol/Amihud 昇順）→ calm/stressed**。
> - **[精緻3C] 決算継ぎ目を明文化**（第4部4.4／第6部6.3）。決算日＋翌日は**リターン/ボラ・チャネルのみダミー化、流動性（Amihud/出来高）チャネルは更新継続**。PEAD サイジングは流動性更新後の filtered 状態を読む。
> - **[精緻4-1] プラセボ検定はブロックシャッフル／位相ランダム化**（自己相関を保持）で null edge ≈ 0 を確認（第2部2.9／第9部）。素朴 IID シャッフルは不可。
> - **[精緻4-2] レジーム依存コストの impact 係数は保守側固定＋感応度テスト併設**（第6部6.4）。係数が結論のドライバーにならないことを示す。
> - **[採用] 決算日フォールバックの循環性**（第2部／第4部4.4）。spike 近似は本設計では循環的（vol/出来高 spike で vol/出来高モデルの除外日を定義）。PEAD 検証では実開示日（TDnet・引け後/場中の別）を**準必須**、近似は研究用限定。
> - **[採用] 状態数 2→3 は事前登録仮説で判定**（第5部／第7部／第11部）。「高ボラ×流動的（活況）」と「高ボラ×低流動（危機）」の分離が必要かを直接検証（BIC 任せにしない）。

> **v2.0 からの主要改訂（要約・継続有効）**
> - **第2部に「先読み防止（ポイントインタイム規律）」を構造的前提として新設**。filtered一本化・walk-forward再推定・ラベル整列・sticky HMM を実装レベルで規定。
> - **第6部にPEAD等イベント駆動戦略への「レジームフィルタ適用除外規定」を新設**。決算スパイクの特徴量分離＋「フィルタからは外すがサイジングには残す」原則。
> - **第5部にマルチタイムスケール構成を新設**。方向性は週足、日足はボラ・流動性軸に割り切り、週足filtered確率を日足HMMへ事前分布注入（方式A主軸）。
> - **第4部の特徴量正規化を全面改訂**。生z-scoreを廃し、ロバスト正規化（中央値・MAD）＋構造成分除去＋Amihudのフロア/対数/フラグ処理。
> - 優先実装順序：**第2部（先読み）→ 第6部（PEAD衝突）→ 第5部（マルチスケール）→ 第4部（前処理）**。

---

## 第1部 背景・目的（Intent）

### 1.1 背景
- ユーザーは日足データのみを保有し、TickベースのDollar Bars構築は現時点で困難。
- 価値戦略および**PEAD（Post-Earnings Announcement Drift）**を中心とした日本株戦略において、ボラティリティと流動性の変化が戦略パフォーマンスに大きく影響する。
- López de Prado（AFML）の「情報駆動型サンプリング」の精神を、日足レベルで近似的に実現したい。
- 価格のみのレジーム検知（リターン・実現ボラのみ）では、出来高・流動性の変化を捉えきれない。

### 1.2 目的
- 価格と出来高を統合し、市場の「活動水準」と「リスク状態」を包括的に表現する。
- 日足で実現可能な範囲で、**ボラティリティレジーム＋流動性レジーム**を同時検知する。
- 検知レジームを、戦略フィルタリング・メタラベリング・動的ポジションサイジングに活用し、過学習低減とリスク調整後リターンの改善を図る。
- 将来のTickデータ取得時に真のDollar Barsへ移行できる拡張性を確保する。

### 1.3 v2.1で確定した設計思想（最重要）
1. **日足に方向性を判定させない。** 日足単一銘柄ではドリフト（強気/弱気）が日次分散に対し統計的に識別困難。日足HMMは**ボラ・流動性状態の検知器**に割り切る。
2. **方向性アンカーはセクター/指数から取る（アーキ1）。** 単一銘柄の**週足**方向も識別が弱い（日足非識別性の軽度版）。横断的サポートのあるセクター/指数の週足層を方向性アンカーとし、日足個別層へ注入する（第5部）。週足個別の方向推定はしない。
3. **日足層は単一銘柄では推定が痩せる→流動性ティアで partial pooling（アーキ2）。** 多変量・ファットテール・walk-forward 再推定を1銘柄で回すとレジーム系列がノイジーになり、それが判定材料（ターンオーバー・コスト）に伝播する。流動性ティアで括ったプールで放出を推定し、filtered 推論のみ銘柄別にする（第5部5.4）。
4. **先読み防止は機能ではなく前提。** これが崩れると他の全結果が無効（第2部）。
5. **レジームフィルタはPEADの収益源と決算日で衝突する。** イベント駆動戦略では適用を構造的に分離する（第6部）。

---

## 第2部 先読み防止（ポイントインタイム規律）— 全モジュール共通の最優先前提

> 本部の規律は Feature Engineering・モデル・バックテストの**全段階**に適用される。1つでも破ると以降の検証結果はすべて無効とみなす。

### 2.1 filtered と smoothed の厳密分離
- レジーム判定（シグナル生成・性能評価）は、その時点までの情報のみで推定する **filtered（オンライン）確率**でのみ行う。
- 全系列を使う **smoothed 確率**は**可視化・事後分析専用**。シグナル/評価系に混入させない。
- 実装上、両者を**別関数・別カラム名**（例: `state_filtered`, `state_smoothed`）に分離し、バックテスト側が `*_smoothed` を参照できない構造にする。

### 2.2 walk-forward 再推定（過去のみ）
- HMM/GMM のパラメータ（各状態の平均・共分散・遷移確率）は、**全期間で一度だけ推定しない**。
- 拡大窓または固定長ローリング窓で、**その時点までのデータのみから** `refit_every` 間隔で再フィット（推奨初期値：日足20営業日ごと、週足4週ごと）。再フィット後、以降の新規データを filtered で分類する。
- **プール推定（第5部5.4）も同規律**：流動性ティアの銘柄横断パネルも≤t のデータのみ、`refit_every` で再推定。プールの銘柄構成も**ポイントインタイム**（上場/廃止を考慮しサバイバーシップを避ける）。

### 2.3 正規化統計も過去のみ
- z-score／ロバスト正規化／Amihudのフロア・分位点に使う**中央値・MAD・トレンド・分位点**は、**拡大窓またはローリング過去窓のみ**から算出。全期間統計でのスケーリングは先読みのため禁止。
- **流動性ティアの分位境界**も過去窓のみから算出（ティア割当の先読み禁止）。

### 2.4 週足アンカーのラグ（第5部と連動）
- 日足時点 t で参照する週足（セクター/指数）レジームは、**t の直前に完了した週**までで推定したもののみ。進行中の週の終値で当該週レジームを確定しない。

### 2.5 ラベルスイッチング整列（v2.1：整列軸を層別化）
- 再推定のたびに状態番号が入れ替わる（label switching）。各再フィット後に状態の意味を固定する。**整列軸は層によって異なる**：
  - **週足（方向性層・セクター/指数）**：**平均週次リターン（または mom 符号）昇順**で並べ替え → bear / neutral / bull に固定。
  - **日足（ボラ流動性層・個別）**：**実現ボラ（または平均 Amihud）昇順**で並べ替え → calm / stressed に固定。
- 整列キーは**過去のみから推定したパラメータ（状態の平均・尺度）**を用い、整列に評価対象時点の観測を混入させない。

### 2.6 初期値依存への対処
- EMは初期値敏感。**複数のランダム初期値＋k-means初期化**で再起動し、最良尤度の解を採用する。

### 2.7 持続性制御（旧「Persistenceフィルタ」の具体化）
- 過剰検知（日替わりのちらつき）対策を、抽象的フィルタではなく以下で実装：
  - **sticky HMM**：遷移行列の自己遷移確率を高く保つ（切替へのペナルティ／ディリクレ事前）。
  - **最小滞在期間（minimum dwell time）**：一度入った状態に最低保持日数を課す。
  - **状態確率の閾値処理**：filtered確率が閾値を超えるまで切替を確定しない（ヒステリシス）。

### 2.8 検証の二本立て
- **Purged K-Fold / CPCV**（情報リーク防止、パラメータ評価の分散低減）に加え、**必ず walk-forward（時系列前進）でも検証**する。
- レジーム検知の本質は「遅延なくオンラインで状態を当てられるか」なので、**前進検証での filtered 性能を実運用に最も近い主指標**とする。

### 2.9 look-ahead 検出テスト＋プラセボ検定（v2.1：プラセボ追加）
- **(a) filtered/smoothed 乖離テスト**：smoothedを使った版と filtered版で性能が明確に乖離することを確認。乖離が小さすぎる場合、どこかで先読みが漏れている疑いがあるため調査する。
- **(b) プラセボ（null）検定**：レジームシグナルが**ブロックシャッフルまたは位相ランダム化**（自己相関を保持）した null 系列に対し edge ≈ 0 になることを確認する。**素朴な IID シャッフルは自己相関を壊して検定力を失うため不可。** (a) が捉えない**正規化統計・refit 境界・ラベル整列・プール推定**のリークを補完する。

---

## 第3部 システム概要（Pipeline Overview）

```
Raw Daily / Weekly OHLCV（個別銘柄＋セクター/指数＋流動性ティア用の銘柄横断パネル）
        ↓
[第2部 規律を全段に適用：filtered / walk-forward / 過去のみ正規化 / PIT ティア割当]
        ↓
Feature Engineering（第4部：ロバスト正規化＋イベント分離）
    - Realized Volatility
    - Robust Normalized Volume（median/MAD）＋構造成分除去
    - Amihud Illiquidity（フロア＋対数＋フラグ）
    - 値幅制限/stale price フラグ
    - 決算: リターン/ボラはダミー化・流動性チャネルは更新継続（4.4）
        ↓
Regime Detection（第5部：マルチタイムスケール）
    - 週足HMM（セクター/指数）：方向性レジーム（強気/弱気/中立）／整列=リターン符号
    - 日足HMM（個別・多変量）：ボラ・流動性状態／整列=ボラ・Amihud
        ← 流動性ティアで partial pooling 推定（弱め固定強度）
        ← 週足セクター/指数 filtered 確率を事前分布注入（方式A）
    - filtered 状態確率系列を出力
        ↓
Regime-aware Application（第6部）
    - レジームフィルタ（※イベント駆動戦略は適用除外規定）
    - メタラベリング特徴量（HMM事後確率）
    - 動的ポジションサイジング（決算窓は流動性更新後 filtered を使用）
    - レジーム依存コスト（impact 係数 保守側固定＋感応度）
    - レジーム別パフォーマンス分析
```

---

## 第4部 Feature Engineering 仕様（v2.0改訂：ロバスト化＋イベント分離）

### 4.1 必須特徴量

| 特徴量 | 定義・計算式 | 窓幅(推奨) | 目的 |
|---|---|---|---|
| Realized Volatility | 対数リターンのローリング標準偏差、または高安があればParkinson推定量 | 20〜60日 | ボラレジームの基盤 |
| Robust Normalized Volume | **対数出来高**を**中央値・MADベースのロバストz-score**化（後述の構造成分除去後） | 20〜60日 | 活動水準（外れ値・歪度に頑健） |
| Amihud Illiquidity | `|r_t| / max(DollarVolume_t, floor)` を**対数変換**後ロバストz-score化 | - | 流動性悪化の代理（除算発散を抑制） |

> **チャネル区分（4.4・第6部6.3と連動）**：Realized Volatility とリターン由来特徴量を**リターン/ボラ・チャネル**、Robust Normalized Volume と Amihud を**流動性チャネル**と呼ぶ。決算窓ではチャネル別に扱いを変える（4.4）。

### 4.2 出来高の正規化（v1.0の生z-scoreを廃止）
出来高分布は右に強く歪み外れ値が多いため、**平均・標準偏差ベースのz-scoreは決算日1点でスケールが汚染される**。以下を必須とする：
1. **対数変換** → **中央値・MAD（中央絶対偏差）ベースのロバストz-score**。
2. **構造成分の除去**：曜日効果・月末/月初・SQ週・趨勢的増加トレンドが z-score後も残る。最低限**ローリング中央値で割ってトレンド除去**、可能なら**曜日・月内位置ダミーで回帰した残差**を用いる。
3. 正規化に使う統計は**過去窓のみ**（第2部 2.3）。

### 4.3 Amihud の日本株固有処理
- **分母フロア**：`max(DollarVolume_t, floor)` で除算発散を防止 → 対数化 → ロバストz-score。
- **値幅制限到達日**：リターンが打ち切られAmihudが**過小**に出る。
- **stale price 日（出来高僅少）**：分母が極小でAmihudが**過大**に出る。
- 上記2種は**逆方向に歪む**ため、両方を検出して**フラグ化し、HMM観測からは欠損相当として除外**するのを既定とする（感応度確認のため除外有無を切替可能に）。

### 4.4 イベント（決算）成分の分離 ← 第6部6.3と連動（v2.1：チャネル別に明文化）
- 決算日（および翌営業日）の出来高・ボラの巨大スパイクは**レジーム転換ではなく点的イベント**。これを**チャネル別**に処理する：
  - **リターン/ボラ・チャネル**（実現ボラ・リターン由来特徴量）：決算日＋翌営業日を**ダミー化／マスク**（イベントダミーで吸収、または当該観測を欠損扱い）し、「決算だから高ボラ・低流動性レジームに転換した」という誤検知を防ぐ。
  - **流動性チャネル**（Amihud・ロバスト正規化出来高）：**更新を継続する**。決算後の流動性変化は執行サイズに効く実情報であり、これを殺さない。
- 結果として決算窓でも filtered 状態は**流動性で駆動され続け**、第6部6.3 のサイジングが読む値が一意に定まる。
- **決算日リストは実開示日（TDnet等、引け後/場中の別）を準必須**とする。**spike 近似は本設計では循環的**（vol/出来高 spike で vol/出来高モデルの除外日を定義することになる）ため、研究用の暫定にとどめ、**PEAD の最終判定には用いない**。

### 4.5 オプション特徴量
- Dollar Volume Proxy（`Close × Volume` のロバストz-score）
- 出来高変化率のロバストz-score
- ローリング歪度・尖度（分布形状変化の捕捉）

---

## 第5部 マルチタイムスケール・レジーム検知（v2.1：方向性ソース・整列軸・pooling を改訂）

### 5.1 役割分担（v2.1：方向性はセクター/指数）
- **週足HMM（セクター/指数）**：方向性レジーム（強気/弱気/中立）を推定。**単一銘柄の週足方向は識別が弱い**ため、横断的サポートのあるセクター/指数で方向性を捉える（アーキ1）。整列は**リターン/mom 符号**（第2部2.5）。
- **日足HMM（個別銘柄・本設計書の価格・出来高統合HMM）**：**ボラ・流動性状態のみ**を検知。方向性は持たせない。整列は**ボラ/Amihud**（第2部2.5）。推定は流動性ティアで pooling（5.4）。

### 5.2 結合方式：事前分布注入（方式A）を主軸
- 週足（セクター/指数）の **filtered レジーム確率**を、日足HMMの各時点の**状態事前分布（初期分布）に注入**する。例：週足が「弱気」のとき、日足の「高ボラ・低流動性（危機）」状態の事前を引き上げる。
- **方式A推奨の理由**：外生特徴量化（方式C）だとEM内で週足情報が他特徴量に希釈されるのに対し、事前分布注入は「週足が上位・日足が下位」の階層関係を明示的に保持でき、本設計書が重視する**解釈性**を維持できる。
- **方式C（外生特徴量）は第二候補**、**方式B（週足状態ごとに日足パラメータを別推定）は第三候補**（データ分割消費で個別銘柄の限られた履歴では推定が不安定になりやすいため）。
- 注入時は必ず**第2部 2.4 のラグ規則**（直前に完了した週の確率のみ使用）を守る。

### 5.3 方向性アンカー＝セクター/指数（v2.1：一次ソースに格上げ）
- セクター（業種）/指数レジームは、個別銘柄の固有ノイズを平均化する中間アンカーであると同時に、**方向性アンカーの一次ソース**（アーキ1）。
- 理想は **週足セクター → （任意）週足個別 → 日足個別** の階層だが、**まずは 週足セクター/指数 の filtered 確率を日足個別HMMの事前に注入する二層**から始める。
- 「セクター/指数 注入あり／なし」を比較し、**注入なし版を「日足のみ（方向性アンカー無し）」のベースライン**（第9部9.2）として位置づける。これがノイズ平均化＋方向性付与の付加価値の検証になる。

### 5.4 【新設】流動性ティア partial pooling（アーキ2）
- **動機**：日足ボラ流動性HMMは単一銘柄では推定が不安定（walk-forward 初期は数百obsで多変量・ファットテール・full共分散）。レジーム系列のノイズがターンオーバー/コストへ伝播し、最終判定を汚す。
- **括りは流動性ティア（セクターではない）**：ボラ・流動性レジームの放出分布は業種よりも**流動性水準で相似**なため、ADV（平均売買代金）または Amihud 水準の**分位**で銘柄を層化する。ティア割当は**ポイントインタイム**（過去窓のみ、第2部2.2・2.3）。
- **pooling 方式（partial pooling）**：流動性ティアの**銘柄横断パネルから放出パラメータ（各状態の位置・尺度）を推定**し、個別銘柄はそこへ**固定弱シュリンク**。最小実装は「プール放出パラメータ＋銘柄別遷移行列＋銘柄別 filtered 推論」、強化版は「プールを事前とした銘柄別 MAP に小さな固定擬似カウントでシュリンク」。
- **pooling 強度は最適化せず弱め固定**（例：個別:プールの実効比を一定の小さな値に固定し、グリッド探索しない）。**最適化は二重過剰適合源**。強度の影響は安定性スイープ（第9部9.3）で**確認するに留め、選択はしない**。
- 週足方向性アンカー（5.1〜5.3）は別途セクター/指数で推定し、日足個別層へ方式Aで注入する。pooling は日足ボラ流動性層の推定法、方向性注入は別経路、と役割を分ける。

---

## 第6部 バックテスト・戦略統合（v2.0改訂：イベント駆動の適用除外を明文化）

### 6.1 レジームフィルタ
- 原則：高ボラ・低流動性状態では新規ポジション抑制またはサイズ縮小。
- **ただし下記6.3のイベント駆動適用除外規定が優先される。**

### 6.2 メタラベリング
- 一次戦略のシグナルに対し、HMMの**filtered状態確率・状態ラベル**を二次モデルの特徴量に加え、「このレジームで一次戦略が勝ちやすいか」を学習。
- 二次モデルの学習・評価は **Purged K-Fold / CPCV**（第2部 2.8）でリーク防止。

### 6.3 PEAD等イベント駆動戦略へのレジームフィルタ適用除外規定（v2.1：継ぎ目を明文化）
**問題**：PEADの収益源（決算後ドリフト）と、価格・出来高統合特徴量が最も激しく反応する日（決算日）が一致する。素直にフィルタを適用すると、**PEADが最も稼ぐべき決算直後をフィルタが殺す**自己矛盾が生じる。

**方針（2段構え）**：
1. **特徴量段階の分離**（第4部 4.4）：決算日・翌日について、**リターン/ボラ・チャネルのみダミー化、流動性チャネルは更新継続**。決算スパイクをレジーム転換と誤認させない一方、流動性情報は失わない。
2. **適用段階の分離**：PEADのエントリー窓（決算後の数営業日）では、**レジームフィルタによる新規ポジション抑制（エントリー可否）を適用しない**。
   - **ただしレジームは「サイジング」には残す**：「フィルタからは外すが、ポジションサイズ調整には使う」。決算後ドリフトは取りに行くが、流動性が枯渇していれば小さく入る。
   - **継ぎ目の定義（v2.1）**：決算窓でサイジングが読むのは、**4.4 で流動性チャネルが更新され続けた filtered 状態**。リターン/ボラ・ジャンプはダミー化済みのため、サイジング値は「決算窓でも一意に定義され、流動性で駆動される」。

> この規定は第6部の他のフィルタ規則に優先する。価値戦略など非イベント駆動の一次戦略には通常のフィルタ（6.1）を適用する。

### 6.4 動的ポジションサイジング＋レジーム依存コスト（v2.1：コストを改訂）
- filtered状態確率に応じてサイズを連続調整（ハードな全切替を避けヒステリシスを持たせる）。イベント駆動戦略でも本サイジングは有効（6.3）。
- **コストはレジーム依存とする**：単一銘柄はマーケットインパクト支配で、それは検知対象の**低流動性レジームそのもの**。フラットコストはフィルタが狙う低流動局面でだけコストを過小評価し、**判定をフィルタ有利に歪める**。
  - マーケットインパクトを同時点の流動性（Amihud）でパラメータ化する。
  - **impact 係数は保守側に固定**（低流動レジームのコストを過小評価しない側）。
  - **感応度テストを併設**し、係数を動かしても「二層＞単層」の結論が反転しないことを示す（**係数が結論のドライバーでないこと**を保証）。

### 6.5 レジーム別パフォーマンス分析
- 各状態ごとに Sharpe・最大DD・Calmar・ターンオーバー・取引回数を集計し、戦略のレジーム依存性を可視化。

---

## 第7部 レジーム検知モデル設計（詳細）

### 7.1 主推奨：多変量 Gaussian/Student-t HMM
- 観測変数：第4部の必須特徴量ベクトル（2〜3次元）＋週足事前（第5部）。
- 状態数：**2から開始**（初期推奨）。**2→3 拡張は事前登録仮説で判定**（v2.1）＝「**高ボラ×流動的（活況トレンド）**」と「**高ボラ×低流動（危機）**」を 2 状態が 1 軸に潰して分離できない、という具体仮説を直接検証する（BIC/尤度任せにしない。BIC は自己相関下で過大選択する癖がある）。この 2 モードの分離はフィルタにも PEAD 執行にも決定的。
- 放出分布：日足はファットテール・決算ジャンプのため、**Student-t放出、またはボラ標準化後ガウス**を優先（純ガウスは外れ値を偽状態として吸収しやすい）。**ガウス vs t を必ず比較**。
- 共分散：full（柔軟性）または diag（安定性）。両者を比較。
- 学習：Baum-Welch（EM）、複数初期値再起動（第2部 2.6）。**推定は流動性ティアで pooling（第5部5.4）**、filtered 推論は銘柄別。
- 出力：filtered事後確率系列（主）、Viterbi最尤系列、smoothed（可視化専用）。

### 7.2 代替・補完
- **GMM**：時間的持続性がなく日替わりでちらつくため、**HMMの遷移行列の効果を対比する基準**として実装・比較（GMM単体は主軸にしない）。
- **Change Point Detection（PELT/ruptures）**：合成指標（ボラ＋Amihudの主成分など）に適用し、変化点タイミングの明確化に補助利用。
- **Wasserstein K-means**：分布形状変化を捉えたい場合のオプション。

---

## 第8部 データ・環境要件（v2.1：横断パネル・実決算日を追加）

- 入力：日足・週足OHLCV（必須：Close, Volume。高安があればParkinson推奨）。**コーポレートアクション調整済み**価格を前提（未調整なら調整処理を実装）。
- **横断パネル（v2.1）**：流動性ティア構築用に、対象銘柄と同ユニバースの**銘柄横断 ADV/Amihud パネル**（ポイントインタイム、上場/廃止考慮）。
- **方向性アンカー（v2.1）**：セクター/業種指数または TOPIX/Nikkei225 の週足系列。
- **実決算開示日（v2.1）**：TDnet 等の開示日（引け後/場中の別を含む）。PEAD 戦略の検証に準必須。
- 環境：Python（pandas, numpy, hmmlearn or statsmodels, ruptures, scikit-learn）。
- 前処理：欠損処理、ロバスト正規化、値幅制限/stale price/決算日のフラグ化、流動性ティア割当、**全窓計算でポイントインタイム厳守**。
- 乱数シード固定、設定は単一configに集約。

---

## 第9部 評価指標・検証方法

### 9.1 レジーム自体の評価
- 状態の持続性（persistence）。
- 経済的解釈可能性（高ボラ・低流動性状態でリターンが負/ボラが高い等）。
- 状態遷移と実イベント（例：2020年3月、決算ショック）の一致度。**ただし filtered での一致**を見る（smoothedで「当たって見える」のは確証バイアス）。

### 9.2 戦略への寄与評価（本丸）
- レジームフィルタ/サイジング適用前後の Sharpe・Calmar・最大DD の改善。
- メタラベリングモデルの AUC・Precision 向上。
- レジーム別サブサンプルでの一次戦略パフォーマンス差。
- **必ず比較するベースライン**：バイ・アンド・ホールド／常時無フィルタ・固定サイジング／**週足のみ**／**日足のみ（方向性アンカー無し＝セクター注入なし）**／固定50-50ブレンド。**二層・統合が単層・固定を取引コスト控除後・前進検証で上回るか**を判定基準とする。**3（週足のみ）・4（日足のみ）との比較が本丸。**

### 9.3 安定性テスト（v2.1：pooling 強度を含む）
- 初期値再シード、窓長・refit_every の微変更、開始日のずらし、銘柄入替、**流動性ティアの分位境界・pooling 強度の微変更**に対し、レジーム系列と最終性能が頑健か。摂動で結論が反転するなら優位は信頼できない。pooling 強度は**選択せず確認のみ**（第5部5.4）。

### 9.4 多重検定・プラセボ（v2.1：プラセボ参照を追加）
- 方式・パラメータ・銘柄を多数試すため、最良結果の単純p値を信用しない。複数銘柄・複数期間での再現性、または簡易なFDR/リアリティチェック的補正で「偶然の勝者」を割り引く。
- **プラセボ（null）検定**（第2部2.9(b)）：ブロックシャッフル／位相ランダム化系列で edge ≈ 0 を確認。

---

## 第10部 リスク・制約・緩和策（v2.1：単一銘柄検出力を追加）

| リスク | 内容 | 緩和策 |
|---|---|---|
| データ粒度の限界 | 日足では真の情報駆動型サンプリング不可 | 特徴量レベルで統合近似。将来Tick取得時にDollar Barsへ移行 |
| **単一銘柄の検出力不足** | 日足多変量HMMが1銘柄では推定不安定→レジーム系列がノイジー | **流動性ティアで partial pooling（弱め固定）**（第5部5.4） |
| **週足方向の識別困難** | 単一銘柄の週足方向もSNRが低い | **方向性はセクター/指数から取得**（第5部5.1・5.3） |
| 過剰検知・ノイズ | 短期出来高変動で頻繁な状態切替 | sticky HMM＋最小滞在＋確率閾値（第2部 2.7） |
| **決算日衝突** | レジーム入力とPEAD収益源が決算日で競合 | チャネル別分離（リターン/ボラはダミー化・流動性は更新継続）＋フィルタ適用除外＋サイジング併用（第4部4.4・第6部 6.3） |
| レジームの非定常性 | 長期でレジームの性質が変化 | walk-forward再学習・オンライン更新（第2部 2.2） |
| **先読み・過学習** | レジーム検知自体がバックテストで過学習 | filtered一本化・walk-forward・ラベル整列・Purged/CPCV＋**プラセボ検定**（第2部） |
| **コスト過小評価** | 低流動レジームでフラットコストが判定をフィルタ有利に歪める | **レジーム依存コスト（impact 保守側固定）＋感応度**（第6部6.4） |
| Amihud不安定 | 値幅制限/stale priceで歪み・除算発散 | フロア＋対数＋フラグ除外（第4部 4.3） |

---

## 第11部 次ステップ・未決定事項（v2.1更新）

1. 状態数の最終決定（2 or 3）：**事前登録仮説（高ボラの2モード分離）**で確定（BIC 任せにしない）。
2. Amihudのフロア値・対数変換パラメータの実データ調整。
3. 出来高の構造成分除去の方式（ローリング中央値割り vs ダミー回帰残差）の選択。
4. 週足→日足の事前分布注入の具体的な重み付け関数の確定（方式A）。
5. **流動性ティアの設計**：分位数（何分位）・更新頻度・最小銘柄数。
6. **pooling 強度の弱め固定値**：最適化せず固定値を決め、安定性スイープで確認のみ。
7. **方向性アンカーのソース**：業種指数 vs セクターバスケット vs TOPIX/Nikkei225 の選択。
8. セクター二層 → 三層（週足セクター→週足個別→日足個別）への拡張可否。
9. 将来のTickデータ取得時の真のDollar Barsへの移行計画。

---

### 実装着手順序（再掲）
**第2部（先読み防止）→ 第6部 6.3（PEAD衝突）→ 第5部（マルチスケール）→ 第4部（前処理ロバスト化）** の順で確定させる。先読みを潰さないと他の検証が信用できず、PEAD衝突を解かないと主戦略の評価が歪むため。

> **v2.1 のアーキ確定（着手前に固定）**：①方向性はセクター/指数から取得、②日足層は流動性ティアで partial pooling（弱め固定）、③週足HMMの整列軸はモメンタム符号。この3点は配管（第5部・接続設計）の形を決めるため、実装前に確定させる。
