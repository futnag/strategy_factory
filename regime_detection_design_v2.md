# 日足データを用いた価格・出来高統合型レジーム検知システム 設計書 v2.0

> **v1.0 からの主要改訂（要約）**
> - **第2部に「先読み防止（ポイントインタイム規律）」を構造的前提として新設**（旧・第8節のリスク一項目から格上げ）。filtered一本化・walk-forward再推定・ラベル整列・sticky HMM を実装レベルで規定。
> - **第6部にPEAD等イベント駆動戦略への「レジームフィルタ適用除外規定」を新設**。決算スパイクの特徴量分離＋「フィルタからは外すがサイジングには残す」原則。
> - **第5部にマルチタイムスケール構成を新設**。方向性は週足、日足はボラ・流動性軸に割り切り、週足filtered確率を日足HMMへ事前分布注入（方式A主軸）。
> - **第4部の特徴量正規化を全面改訂**。生z-scoreを廃し、ロバスト正規化（中央値・MAD）＋構造成分除去＋Amihudのフロア/対数/フラグ処理。
> - 優先実装順序：**第2部（先読み）→ 第6部（PEAD衝突）→ 第5部（マルチスケール）→ 第4部（前処理）**。

---

## 第1部 背景・目的（Intent）

### 1.1 背景
- ユーザーは日足データのみを保有し、TickベースのDollar Bars構築は現時点で困難。
- 価値戦略および**PEAD（Post-Earnings Announcement Drift）**を中心とした日本株戦略において、ボラティリティと流動性の変化が戦略パフォーマンスに大きく影響する。
- López de Prado（AFML）の「情報駆動型サンプリング」の精神を、日足レベルで近似的に実現したい。
- 価格のみのレジーム検知（リターン・実現ボラのみ）では、出来高・流動性の変化を捉えきれない。

### 1.2 目的
- 価格と出来高を統合し、市場の「活動水準」と「リスク状態」を包括的に表現する。
- 日足で実現可能な範囲で、**ボラティリティレジーム＋流動性レジーム**を同時検知する。
- 検知レジームを、戦略フィルタリング・メタラベリング・動的ポジションサイジングに活用し、過学習低減とリスク調整後リターンの改善を図る。
- 将来のTickデータ取得時に真のDollar Barsへ移行できる拡張性を確保する。

### 1.3 v2.0で明確化した設計思想（最重要）
1. **日足に方向性を判定させない。** 日足単一銘柄ではドリフト（強気/弱気）が日次分散に対し統計的に識別困難。日足HMMは**ボラ・流動性状態の検知器**に割り切り、方向性は週足層が担う（第5部）。
2. **先読み防止は機能ではなく前提。** これが崩れると他の全結果が無効（第2部）。
3. **レジームフィルタはPEADの収益源と決算日で衝突する。** イベント駆動戦略では適用を構造的に分離する（第6部）。

---

## 第2部 先読み防止（ポイントインタイム規律）— 全モジュール共通の最優先前提

> 本部の規律は Feature Engineering・モデル・バックテストの**全段階**に適用される。1つでも破ると以降の検証結果はすべて無効とみなす。

### 2.1 filtered と smoothed の厳密分離
- レジーム判定（シグナル生成・性能評価）は、その時点までの情報のみで推定する **filtered（オンライン）確率**でのみ行う。
- 全系列を使う **smoothed 確率**は**可視化・事後分析専用**。シグナル/評価系に混入させない。
- 実装上、両者を**別関数・別カラム名**（例: `state_filtered`, `state_smoothed`）に分離し、バックテスト側が `*_smoothed` を参照できない構造にする。

### 2.2 walk-forward 再推定（過去のみ）
- HMM/GMM のパラメータ（各状態の平均・共分散・遷移確率）は、**全期間で一度だけ推定しない**。
- 拡大窓または固定長ローリング窓で、**その時点までのデータのみから** `refit_every` 間隔で再フィット（推奨初期値：日足20営業日ごと、週足4週ごと）。再フィット後、以降の新規データを filtered で分類する。

### 2.3 正規化統計も過去のみ
- z-score／ロバスト正規化／Amihudのフロア・分位点に使う**中央値・MAD・トレンド・分位点**は、**拡大窓またはローリング過去窓のみ**から算出。全期間統計でのスケーリングは先読みのため禁止。

### 2.4 週足アンカーのラグ（第5部と連動）
- 日足時点 t で参照する週足レジームは、**t の直前に完了した週**までで推定したもののみ。進行中の週の終値で当該週レジームを確定しない。

### 2.5 ラベルスイッチング整列
- 再推定のたびに状態番号が入れ替わる（label switching）。各再フィット後、**状態を実現ボラ昇順**（または平均Amihud昇順）で並べ替えて意味を固定し、レジーム系列の連続性を担保する。

### 2.6 初期値依存への対処
- EMは初期値敏感。**複数のランダム初期値＋k-means初期化**で再起動し、最良尤度の解を採用する。

### 2.7 持続性制御（旧「Persistenceフィルタ」の具体化）
- 過剰検知（日替わりのちらつき）対策を、抽象的フィルタではなく以下で実装：
  - **sticky HMM**：遷移行列の自己遷移確率を高く保つ（切替へのペナルティ／ディリクレ事前）。
  - **最小滞在期間（minimum dwell time）**：一度入った状態に最低保持日数を課す。
  - **状態確率の閾値処理**：filtered確率が閾値を超えるまで切替を確定しない（ヒステリシス）。

### 2.8 検証の二本立て
- **Purged K-Fold / CPCV**（情報リーク防止、パラメータ評価の分散低減）に加え、**必ず walk-forward（時系列前進）でも検証**する。
- レジーム検知の本質は「遅延なくオンラインで状態を当てられるか」なので、**前進検証での filtered 性能を実運用に最も近い主指標**とする。

### 2.9 look-ahead 検出テスト（必須の自己点検）
- smoothedを使った版と filtered版で性能が明確に乖離することを確認するテストを1つ用意。乖離が小さすぎる場合、どこかで先読みが漏れている疑いがあるため調査する。

---

## 第3部 システム概要（Pipeline Overview）

```
Raw Daily / Weekly OHLCV
        ↓
[第2部 規律を全段に適用：filtered / walk-forward / 過去のみ正規化]
        ↓
Feature Engineering（第4部：ロバスト正規化＋イベント分離）
    - Realized Volatility
    - Robust Normalized Volume（median/MAD）＋構造成分除去
    - Amihud Illiquidity（フロア＋対数＋フラグ）
    - 値幅制限/stale price フラグ
        ↓
Regime Detection（第5部：マルチタイムスケール）
    - 週足HMM：方向性レジーム（強気/弱気/中立）
    - 日足HMM（多変量）：ボラ・流動性状態 ← 週足filtered確率を事前分布注入
    - filtered 状態確率系列を出力
        ↓
Regime-aware Application（第6部）
    - レジームフィルタ（※イベント駆動戦略は適用除外規定）
    - メタラベリング特徴量（HMM事後確率）
    - 動的ポジションサイジング
    - レジーム別パフォーマンス分析
```

---

## 第4部 Feature Engineering 仕様（v2.0改訂：ロバスト化＋イベント分離）

### 4.1 必須特徴量

| 特徴量 | 定義・計算式 | 窓幅(推奨) | 目的 |
|---|---|---|---|
| Realized Volatility | 対数リターンのローリング標準偏差、または高安があればParkinson推定量 | 20〜60日 | ボラレジームの基盤 |
| Robust Normalized Volume | **対数出来高**を**中央値・MADベースのロバストz-score**化（後述の構造成分除去後） | 20〜60日 | 活動水準（外れ値・歪度に頑健） |
| Amihud Illiquidity | `|r_t| / max(DollarVolume_t, floor)` を**対数変換**後ロバストz-score化 | - | 流動性悪化の代理（除算発散を抑制） |

### 4.2 出来高の正規化（v1.0の生z-scoreを廃止）
出来高分布は右に強く歪み外れ値が多いため、**平均・標準偏差ベースのz-scoreは決算日1点でスケールが汚染される**。以下を必須とする：
1. **対数変換** → **中央値・MAD（中央絶対偏差）ベースのロバストz-score**。
2. **構造成分の除去**：曜日効果・月末/月初・SQ週・趨勢的増加トレンドが z-score後も残る。最低限**ローリング中央値で割ってトレンド除去**、可能なら**曜日・月内位置ダミーで回帰した残差**を用いる。
3. 正規化に使う統計は**過去窓のみ**（第2部 2.3）。

### 4.3 Amihud の日本株固有処理
- **分母フロア**：`max(DollarVolume_t, floor)` で除算発散を防止 → 対数化 → ロバストz-score。
- **値幅制限到達日**：リターンが打ち切られAmihudが**過小**に出る。
- **stale price 日（出来高僅少）**：分母が極小でAmihudが**過大**に出る。
- 上記2種は**逆方向に歪む**ため、両方を検出して**フラグ化し、HMM観測からは欠損相当として除外**するのを既定とする（感応度確認のため除外有無を切替可能に）。

### 4.4 イベント（決算）成分の分離 ← 第6部と連動
- 決算日（および翌営業日）の出来高・ボラの巨大スパイクは**レジーム転換ではなく点的イベント**。
- これらの日を**HMMの学習・推定入力から除外、またはイベントダミーで吸収**し、「決算だから高ボラ・低流動性レジームに転換した」という誤検知を防ぐ。

### 4.5 オプション特徴量
- Dollar Volume Proxy（`Close × Volume` のロバストz-score）
- 出来高変化率のロバストz-score
- ローリング歪度・尖度（分布形状変化の捕捉）

---

## 第5部 マルチタイムスケール・レジーム検知（v2.0新設）

### 5.1 役割分担
- **週足HMM**：方向性レジーム（強気/弱気/中立）を推定。中期の方向性は週足で捉える。
- **日足HMM（本設計書の価格・出来高統合HMM）**：**ボラ・流動性状態のみ**を検知。方向性は持たせない。

### 5.2 結合方式：事前分布注入（方式A）を主軸
- 週足の **filtered レジーム確率**を、日足HMMの各時点の**状態事前分布（初期分布）に注入**する。例：週足が「弱気」のとき、日足の「高ボラ・低流動性（危機）」状態の事前を引き上げる。
- **方式A推奨の理由**：外生特徴量化（方式C）だとEM内で週足情報が他特徴量に希釈されるのに対し、事前分布注入は「週足が上位・日足が下位」の階層関係を明示的に保持でき、本設計書が重視する**解釈性**を維持できる。
- **方式C（外生特徴量）は第二候補**、**方式B（週足状態ごとに日足パラメータを別推定）は第三候補**（データ分割消費で個別銘柄の限られた履歴では推定が不安定になりやすいため）。
- 注入時は必ず**第2部 2.4 のラグ規則**（直前に完了した週の確率のみ使用）を守る。

### 5.3 セクター・アンカー
- セクター（業種）レジームは個別銘柄の固有ノイズを平均化する中間アンカーとして有効。
- 理想は **週足セクター → 週足個別 → 日足個別** の三層だが、まずは **週足セクターのfiltered確率を日足個別HMMの事前に注入する二層**から始める。
- 「セクター注入あり／なし」を比較し、ノイズ平均化の付加価値を検証する。

---

## 第6部 バックテスト・戦略統合（v2.0改訂：イベント駆動の適用除外を明文化）

### 6.1 レジームフィルタ
- 原則：高ボラ・低流動性状態では新規ポジション抑制またはサイズ縮小。
- **ただし下記6.3のイベント駆動適用除外規定が優先される。**

### 6.2 メタラベリング
- 一次戦略のシグナルに対し、HMMの**filtered状態確率・状態ラベル**を二次モデルの特徴量に加え、「このレジームで一次戦略が勝ちやすいか」を学習。
- 二次モデルの学習・評価は **Purged K-Fold / CPCV**（第2部 2.8）でリーク防止。

### 6.3 【新設】PEAD等イベント駆動戦略へのレジームフィルタ適用除外規定
**問題**：PEADの収益源（決算後ドリフト）と、価格・出来高統合特徴量が最も激しく反応する日（決算日）が一致する。素直にフィルタを適用すると、**PEADが最も稼ぐべき決算直後をフィルタが殺す**自己矛盾が生じる。

**方針（2段構え）**：
1. **特徴量段階の分離**（第4部 4.4）：決算日・翌日をレジームHMMの入力から分離し、決算スパイクをレジーム転換と誤認させない。
2. **適用段階の分離**：PEADのエントリー窓（決算後の数営業日）では、**レジームフィルタによる新規ポジション抑制（エントリー可否）を適用しない**。
   - **ただしレジームは「サイジング」には残す**：「フィルタからは外すが、ポジションサイズ調整には使う」。決算後ドリフトは取りに行くが、流動性が枯渇していれば小さく入る。
   - これによりPEADの収益機会を潰さず、執行リスクのみ低減する。

> この規定は第6部の他のフィルタ規則に優先する。価値戦略など非イベント駆動の一次戦略には通常のフィルタ（6.1）を適用する。

### 6.4 動的ポジションサイジング
- filtered状態確率に応じてサイズを連続調整（ハードな全切替を避けヒステリシスを持たせる）。
- イベント駆動戦略でも本サイジングは有効（6.3）。

### 6.5 レジーム別パフォーマンス分析
- 各状態ごとに Sharpe・最大DD・Calmar・ターンオーバー・取引回数を集計し、戦略のレジーム依存性を可視化。

---

## 第7部 レジーム検知モデル設計（詳細）

### 7.1 主推奨：多変量 Gaussian/Student-t HMM
- 観測変数：第4部の必須特徴量ベクトル（2〜3次元）＋週足事前（第5部）。
- 状態数：**2から開始**（初期推奨）。3への拡張はデータの尤度・解釈性・ホールドアウト安定性で判断（第9部）。
- 放出分布：日足はファットテール・決算ジャンプのため、**Student-t放出、またはボラ標準化後ガウス**を優先（純ガウスは外れ値を偽状態として吸収しやすい）。
- 共分散：full（柔軟性）または diag（安定性）。両者を比較。
- 学習：Baum-Welch（EM）、複数初期値再起動（第2部 2.6）。
- 出力：filtered事後確率系列（主）、Viterbi最尤系列、smoothed（可視化専用）。

### 7.2 代替・補完
- **GMM**：時間的持続性がなく日替わりでちらつくため、**HMMの遷移行列の効果を対比する基準**として実装・比較（GMM単体は主軸にしない）。
- **Change Point Detection（PELT/ruptures）**：合成指標（ボラ＋Amihudの主成分など）に適用し、変化点タイミングの明確化に補助利用。
- **Wasserstein K-means**：分布形状変化を捉えたい場合のオプション。

---

## 第8部 データ・環境要件

- 入力：日足・週足OHLCV（必須：Close, Volume。高安があればParkinson推奨）。**コーポレートアクション調整済み**価格を前提（未調整なら調整処理を実装）。
- 対象：日本株個別銘柄、所属セクター指数/バスケット、必要に応じTOPIX/Nikkei225（価値ファクター計算対象と整合）。
- 環境：Python（pandas, numpy, hmmlearn or statsmodels, ruptures, scikit-learn）。
- 前処理：欠損処理、ロバスト正規化、値幅制限/stale price/決算日のフラグ化、**全窓計算でポイントインタイム厳守**。
- 乱数シード固定、設定は単一configに集約。

---

## 第9部 評価指標・検証方法

### 9.1 レジーム自体の評価
- 状態の持続性（persistence）。
- 経済的解釈可能性（高ボラ・低流動性状態でリターンが負/ボラが高い等）。
- 状態遷移と実イベント（例：2020年3月、決算ショック）の一致度。**ただし filtered での一致**を見る（smoothedで「当たって見える」のは確証バイアス）。

### 9.2 戦略への寄与評価（本丸）
- レジームフィルタ/サイジング適用前後の Sharpe・Calmar・最大DD の改善。
- メタラベリングモデルの AUC・Precision 向上。
- レジーム別サブサンプルでの一次戦略パフォーマンス差。
- **必ず比較するベースライン**：常時無フィルタ／週足のみ／日足のみ／セクター注入なし／固定サイジング。**二層・統合が単層・固定を取引コスト控除後・前進検証で上回るか**を判定基準とする。

### 9.3 安定性テスト
- 初期値再シード、窓長・refit_every の微変更、開始日のずらし、銘柄入替に対し、レジーム系列と最終性能が頑健か。摂動で結論が反転するなら優位は信頼できない。

### 9.4 多重検定の意識
- 方式・パラメータ・銘柄を多数試すため、最良結果の単純p値を信用しない。複数銘柄・複数期間での再現性、または簡易なFDR/リアリティチェック的補正で「偶然の勝者」を割り引く。

---

## 第10部 リスク・制約・緩和策

| リスク | 内容 | 緩和策 |
|---|---|---|
| データ粒度の限界 | 日足では真の情報駆動型サンプリング不可 | 特徴量レベルで統合近似。将来Tick取得時にDollar Barsへ移行 |
| 過剰検知・ノイズ | 短期出来高変動で頻繁な状態切替 | sticky HMM＋最小滞在＋確率閾値（第2部 2.7） |
| **決算日衝突** | レジーム入力とPEAD収益源が決算日で競合 | 特徴量分離＋フィルタ適用除外＋サイジング併用（第6部 6.3） |
| レジームの非定常性 | 長期でレジームの性質が変化 | walk-forward再学習・オンライン更新（第2部 2.2） |
| **先読み・過学習** | レジーム検知自体がバックテストで過学習 | filtered一本化・walk-forward・ラベル整列・Purged/CPCV（第2部） |
| Amihud不安定 | 値幅制限/stale priceで歪み・除算発散 | フロア＋対数＋フラグ除外（第4部 4.3） |

---

## 第11部 次ステップ・未決定事項

1. 状態数の最終決定（2 or 3）：実データの尤度・解釈性・ホールドアウト安定性で確定。
2. Amihudのフロア値・対数変換パラメータの実データ調整。
3. 出来高の構造成分除去の方式（ローリング中央値割り vs ダミー回帰残差）の選択。
4. 週足→日足の事前分布注入の具体的な重み付け関数の確定（方式A）。
5. セクター二層 → 三層（週足セクター→週足個別→日足個別）への拡張可否。
6. 将来のTickデータ取得時の真のDollar Barsへの移行計画。

---

### 実装着手順序（再掲）
**第2部（先読み防止）→ 第6部 6.3（PEAD衝突）→ 第5部（マルチスケール）→ 第4部（前処理ロバスト化）** の順で確定させる。先読みを潰さないと他の検証が信用できず、PEAD衝突を解かないと主戦略の評価が歪むため。
