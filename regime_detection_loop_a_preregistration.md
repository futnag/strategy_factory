# (a) ループ 事前登録 — 一次戦略の実体化（独立した新判定ループ）

> 二層棄却・レジーム全面棄却（粗モメンタム一次・10銘柄・2.3年）を受けた**独立した問い直し**。
> 「良い一次でもレジームが効くか」を、結果を見る前に基準を固定して問う。**期待を片側に置かない**
> ——勝たなければ結論は強化され、打ち切りもありうる（受容済）。改良仮説は**1つだけ**（一次の実体化）。

## 最優先の地雷：ファンダ/決算データの PIT（着手の最初に塞ぐ）
- 財務は**開示日 DiscDate でアライン**（**期末日 CurFYEn でない**）。期末→開示は数ヶ月ラグ＝期末日で
  使うと未公表財務でトレード＝深刻なリーク。既存 `equities.fundamentals.point_in_time`
  （DiscDate≤t−lag・lag_days=1）を**再利用**。
- 決算サプライズの確定時点も実開示日（`submitDateTime`＝場中/引け後つき）。`lag_days=1` で開示当日
  価格には反映させない保守。
- **恒久 leak_test**：期末日（CurFYEn）アライン版が**赤**になることを既存スイートに常時追加（以降の
  全ループで回す）。価格・出来高の PIT 配管とは別の新リーク面なので、ここを最初に塞ぐ。

## 具体設計（結果非依存で事前固定・最適化しない）
- **value 一次（時系列タイミング）**：earnings_yield = (DiscDate アンカー EPS_asof) / close。
  **long when E/P ≥ 自己の trailing 756 営業日 中央値**（自己史比で割安）・否なら現金。窓 756 固定。
- **PEAD 一次（イベント）**：earnings surprise（actual EPS − 直近予想＝`sue_recent_raw`、DiscDate
  アンカー）が **>0（正サプライズ）**のとき、**開示翌営業日から D=20 営業日ロング**・否なら現金。
- **固定パラメータ（事前登録対象）**：value 窓 756・割安判定=自己中央値超・PEAD D=20・サプライズ
  符号>0・lag_days=1。**いずれも性能を見てから動かさない**（一次を過剰適合源にしない）。

## サンプリング（前ループと同一・再ロック）
- **同じ 5 実業種 × T2/T3 の同じ 10 銘柄**（負け業種を落とす/勝ち業種を足す、をしない）。一次差し替えの
  効果を、サンプル変更の交絡なしに測る。

## 階段（順序を飛ばさない・**新たな最初の段を追加**）
0. **一次そのものの素のエッジ**（レジーム抜き・無フィルタ）：実 value/PEAD が**そもそも正のコスト後
   エッジを持つか**。持たなければレジーム比較は前回同様「良い一次の不在」で無意味＝**ここで打ち切り判断**。
1. 0 をクリアして初めて **単層 vs 無フィルタ**（実一次にレジームが価値を足すか）。
2. 単層が無フィルタを超えて初めて **二層 vs 単層**。

## 判定基準（二層と同じ枠・固定）
- 主指標＝コスト後 walk-forward Sharpe（記述統計併記・採否に使わない）。
- 各層 **≥2/3 で符号正・中央値正・DSR(層)>0.95 生存**。**層別**採否。
- **棄却条件**：実一次でもレジームが無フィルタを超えなければ「この戦略系にレジーム検知の価値なし」と
  **より強く確定**。良い一次でも効かなければ**この方向への投資を止める**判断もありうる（救命ループに
  入らない）。
- λ・状態数・閾値・サンプリング固定。

## 不変条件（釘）
- step 0–1 の PIT/leak_tests/契約・検知器・pooling・結合機構は**一切不変**。一次差し替えは
  `strategy.py` の一次クラス追加＋ファンダ・データ配線のみ。検知器層に触れたら**回帰テストで赤**を確認。
- ファンダ PIT leak_test を**恒久追加**（毎ループ実行）。

## 手順
1. 本 doc 確定（済）。
2. **ファンダ PIT leak_test（期末日アラインで赤）を最初に通す**（恒久追加）。
3. 実 value/PEAD 一次を実装（PIT as-of 配線）。
4. **段0：一次単体の素のエッジ（無フィルタ）**を 10 銘柄で提示。
5. エッジ確認後、段1（単層 vs 無フィルタ）→ 段2（二層 vs 単層）へ、同じ基準を機械適用。

---

## 結果（事後記録）

### 段0：一次単体の素エッジ（無フィルタ・コスト後）
- **value（⚠価格逆張り寄り）：正 7/10(70%)・中央値 +0.023 → 段1進出資格 YES**（初めて一次が単体エッジを持つ）。
- **PEAD：判定不適格・未判定（疎）。** 10銘柄中5銘柄がFY正サプライズ・イベント皆無、残りも少数＝
  2/5 は標本として判定適格ですらない。**「エッジなし」ではない**——FY年1回 sue_recent 設計の限界で
  **エッジの有無は未判定**。四半期サプライズ設計は別ループの単一仮説候補（記録のみ）。

### 段1：逆張り value × レジーム（単層 vs 無フィルタ・10銘柄）
- **単層① 日足−NF：正 3/10(30%)・中央値 −0.007・DSR 0/10 → NO。**
- **単層② 週足−NF：正 3/10(30%)・中央値 −0.007・DSR 0/10 → NO。**
- 抑制割合 平均 0.33。**「suppress 高→悪化」相関なし**（最高 suppress 0.65 の 26670 は単層①最良＝一次が
  負の銘柄でフィルタが損失を削った結果）。負けは「逆張り×ボラ抑制の方向不整合」アーティファクトでなく、
  **「フィルタが decent な一次の exposure を削る」一般動学**（悪い一次で助け・良い一次で害）。救命されず。

### 段1 棄却（事前登録 #5・救命なし）
**レジーム検知は逆張り value 一次にも価値を足さない**（両層 NO・DSR 0/10）。単層棄却・段2に進まない。
射程は逆張り戦略系限定だが、これで **momentum（エッジなし一次→判定不能）と reversal（エッジある
一次→レジーム無価値）の2クラス**でレジーム価値が立たず。**reversal は「単体エッジを持つ一次でも
効かない」より強い負の結果**。

### ループ(a) 帰結・打ち切り判断
事前登録「良い一次でも効かなければこの方向への投資を止める判断もありうる（救命ループに入らない）」に
該当。**エッジある一次（reversal）でもレジーム検知が無価値**と確定 → **この方向（価格・出来高レジーム
検知が日本株単一銘柄戦略に正味価値を足す）への投資を止める**のが規律上の着地。次ループ（四半期PEAD・
アンカー粒度・メタラベリング等）は救命ループになりうるため、回すなら**「打ち切り」を覆す再挑戦でない
独立仮説**であることを明示してから。
