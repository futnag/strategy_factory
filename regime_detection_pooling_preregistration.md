# 流動性ティア partial pooling 採否の事前登録（v2.1・step 2 / ③）

> 事後に基準を動かさないための**事前登録**。判定指標・閾値・ゾーン・帰結仮説を、データ枯渇帯
> （#2）で走らせる**前**に固定する。実装は `invest_system/research/daily_regime/pooling_eval.py`
> の定数に対応（`GAP_CV_RATIO=0.8`, `MAJORITY=2/3`, `MIN_WEIGHT_FLOOR=0.05`, `COND_CEILING=1e4`）。

## 背景（なぜ測り直すか）
T2（流動・2024–2026）の per-stock vs pooled 比較は**判定不能**だった。原因は2つ：
1. flip/dwell/jump は **sticky+min-dwell の下流**で、放出の不安定を隠す。
2. pooled=t混合 / per-stock=HMM-FB で**推定器が違い**、土俵が揃っていない。
T2 では per-stock が既に安定（pooling の動機＝データ枯渇が無い）でもあった。

## 修正した測定（#1＋#3）
- **指標**：filtered 出力でなく、**refit ごとの放出パラメータ**＝ stressed クラスタ平均・
  calm–stressed gap の **横断 CV**（持続性機構の*前*）。低 CV＝放出が安定＝pooling が効いている。
- **推定器統制**：per-stock も pooled も**同一の t 混合**で放出推定。唯一の差を
  「自銘柄窓 vs ティア・プール窓（as-of r・≤r）」に限定。
- **退化指標**：状態 weight < 0.05（collapse）または共分散 条件数 > 1e4（near-singular）の
  refit 比率。データ枯渇で per-stock が崩れる率。

## 事前登録した判定基準（固定）
あるゾーンで pooling を**採用**するのは、per-stock（同一 t 混合）に対し pooled が：
- ターゲット銘柄の **≥2/3** で **gap-CV を 0.8 倍以下に低減**（≥20% 安定化）、**かつ**
- 退化率を**悪化させない**（pooled ≤ per-stock）

とき。安定ゾーン（T2 等）で上記を満たさない＝ pooling 不要（バイアスで僅かに悪化を許容）。

## 帰結仮説（事前登録）
**「全ティア一律 pooling」ではなく、「データ枯渇ティアのみ pooling・安定ティアは per-stock」の
条件付き適用**が正しい着地である、と予測する。判定はゾーン別：
- **T3（非流動）・短履歴・walk-forward 初期窓**＝枯渇帯 → ここで採用基準を満たすか。
- **T1/T2（安定帯）** → per-stock を既定、pooling は不要（適用しない）。

## 釘（逸脱しない）
- **λ（pooling 強度）は固定・非最適化**。CV が下がらないからλを上げて下げにいく、はしない。
  本評価は「pooled（全プール）vs per-stock」の両端で*効くか効かないか*を観測するのみ。
- **PIT 厳守**：t 混合は obs バッグ（順序非依存・時間越え推定なし）。多系列 HMM 化 (b) を後で
  入れる場合は refit 境界リークが無いことを別途検証してから。

## 出力（#2 後に一度提示）
T2（対照・安定帯）と T3（主戦場・枯渇帯）で per-stock vs pooled の {gap-CV, 退化率} を出し、
上記**事前登録基準**に機械的に照らした採否（`verdict()`）を提示する。step 3 以降の go はそこで。

---

## 結果（事後追記・基準は動かしていない）

### 指標の健全性（PIT ゲート）
保留 LL の窓分割を step 0–1 の leak 機構に通して確認（決定的指標自身の健全性チェック）：
- 窓定義に ≤r 混入 → `assert_future_block` が `LookAheadError`（赤）。
- 標準化に未来統計混入 → LL が膨張（指標はリーク感応＝≤r 統計が load-bearing）。
→ **窓外定義にリーク無し**。LL 裁定は汚染されていない。

### 3指標の経過（解釈の異なる量で結論が動いた）
1. **gap-CV（分散のみ）**：T2 採用 7/8（pooled 0.268<per-stock 0.32–0.67）／T3 非採用 1/8
   （pooled 0.441>大半の per-stock）。事前仮説（枯渇帯のみ pooling）を**反転**。駆動要因は
   サンプルサイズでなく**プール一貫性**（T2 均質→安定、T3 異質＋churn→不安定）。
2. **filtered 出力（分散＋バイアスだが持続性機構の下流）**：T2 で pooled が僅かに悪い（バイアス由来）。
3. **窓外保留 LL（分散＋バイアスの正味・上位指標）**：**T2 pooled 7/8（Δ+0.116）／T3 pooled 6/8
   （Δ+0.052）→ 両ティアとも pooled。** gap-CV/filtered は副次。LL の符号で確定。

### 裁定（事前登録ルールの機械適用）
- **条件付き pooling 仮説は棄却。** ティア平均/中央値 LL の高い方＝**pool-both**（全ティア pooling）。
- 単一銘柄の日足放出推定はティアを問わずデータ枯渇（T2 でも per-stock gap-CV 0.3–0.7）。partial
  pooling の分散低減がバイアスを上回る＝教科書的な shrinkage 結果。

### 正直な格付け（記録）
- pool-both は **8銘柄×2年・T3 Δ+0.052 の薄い標本**に乗った**暫定既定**であり頑健な法則ではない。
  **3度の反転**自体が信号の弱さ・標本依存性を示す。
- ゆえに **per-stock を ablation/fallback として配線したまま**にする（実施済）。pooling が規模拡大で
  効かなくなってもアーキは優雅に劣化する＝薄い標本の上に積んでも下流リスクは小。

### 釘（逸脱しない）
- **λ 固定**：T3 が marginal だからλを上げて救う、はしない。
- **ティア単位ルールを貫く**：負だった2 T3銘柄（13840・14330）だけ per-stock に残す事後切替はしない。
- **T3 churn（0.155 > T2 0.107）は将来改良として log**：スティッキー境界化。**先に直さない**
  （ティア帰属を変えると pooled 放出が変わり T3 再裁定ループ＝分析麻痺。かつ multiscale と
  アーキ独立で後回しの手戻りゼロ）。T3 を multiscale 評価で**独立集計可能に保ち**、後で問題が
  出たとき結合(方式A)起因かティア帰属(churn)起因かを切り分けられるようにする。
