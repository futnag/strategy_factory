# operator ブリーフ 2026-07-04

## ⚠ 通知
- **incident 発生・即修正済み（P-7）**: operator 初回実運用起動で loop_lint L6 が
  HARD 誤発火。ロックの started_at が tz 付き（`date -Iseconds`）だと
  `datetime.now()`（naive）との減算で TypeError → **headless 運用なら毎回クラッシュする
  致命バグ**だった。初週テストが即座に検出。

## 実行アクション（本セッション）
- **incident-fix**（規則2 HARD 対応＝研究サイクルは走らせず修正を優先）:
  loop_lint L6 の tz 処理を純関数 `lock_age_hours()` に切出し naive/aware 両対応に修正・
  operator SKILL のロック仕様を naive local 推奨に明記・回帰テスト `tests/test_loop_lint.py`
  3本追加（全緑）。修正後 loop_lint = **全ガードレール通過**。

## PENDING open
- P-6 [config] 週次K上限 12→48（初週テスト・〜07-11 目安で復帰判断）
- P-7 [incident] loop_lint L6 tz バグ＝**本セッションで修正済み**（要 push・要人間確認）

## 明日の見込みアクション
- 規則5 該当（BUDGET解除・BACKLOG ⬜2）→ 先頭 ⬜ `topix_staged_flow` の
  **red-team revise 反映 → /research-cycle**（設計系＝強いモデル推奨）。
  ※ topix_staged_flow は red-team 済み（verdict=revise・同日DiD/2軸統制）＝設計反映が先。

## 予算・鮮度
- 週次 K: 32/48（研究サイクル可）。data 鮮度 OK（daily 最新 20260703）。
- 夜間ワークフロー: 復活済み（gate 修正・P-5）。monitor/ops-review 今月実施済み。
